function n = ndims(~)
    %NDIMS Number of matrix dimensions represented by dpvar.
    %
    %   Syntax:
    %     n = ndims(P)
    %
    %   Example:
    %     P = dpvar(2, {[0 1]});
    %     n = ndims(P);

    n = 2;
end
