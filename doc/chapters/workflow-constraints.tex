\section{From a Continuous Cell Condition to Direct}
\label{sec:cellwise-solver-path}
\index{residual}\index{Direct certificate}\index{finite certificate}

Fix any physical cell $\mathcal H_{\vect{c}}$. After all local operations have been aligned to the assembled residual degree $\vect{M}$, the pullback of the \cref{eq:general-pdlmi} has the general form on each cell:
\begin{gather*}
    \mathcal F^{(\vect{c})}(\vect{\alpha})
    =\sum_{\vect{i}\in\prod_{s=1}^{\ell}\{0,\ldots,M_s\}}
    B_{\vect{i}}^{\vect{M}}(\vect{\alpha})
    \mathcal C^{(\vect{c})}[\vect{i}]
    \preceq0.
\end{gather*}
Here $\vect{\alpha}=\phi_{\vect{c}}^{-1}(\vect{\rho})$, $B_{\vect{i}}^{\vect{M}}$ is the direction-wise degree-$\vect{M}$ tensor-product Bernstein basis, and $\mathcal C^{(\vect{c})}[\vect{i}]$ is the corresponding residual coefficient. Define the sign-normalized target by
\begin{gather*}
    S^{(\vect{c})}(\vect{\alpha})
    =-\mathcal F^{(\vect{c})}(\vect{\alpha})
    =-\sum_{\vect{i}\in\prod_{s=1}^{\ell}\{0,\ldots,M_s\}}
    B_{\vect{i}}^{\vect{M}}(\vect{\alpha})
    \mathcal C^{(\vect{c})}[\vect{i}].
\end{gather*}
The theoretical target is $S^{(\vect{c})}(\vect{\alpha})\succeq0$ on the unit box. The Direct certificate imposes
\begin{gather*}
    -\mathcal C^{(\vect{c})}[\vect{i}]\succeq0
    \quad \forall \vect{i}\in\prod_{s=1}^{\ell}\{0,\ldots,M_s\}.
\end{gather*}
Because every $B_{\vect{i}}^{\vect{M}}$ is nonnegative on the unit box and the basis functions sum to one,
\begin{gather*}
    \bigl[-\mathcal C^{(\vect{c})}[\vect{i}]\succeq0\ \text{for all }\vect{i}\bigr]
    \quad\Longrightarrow\quad
    S^{(\vect{c})}(\vect{\alpha})\succeq0
    \quad\text{for all }\vect{\alpha}\in[0,1]^\ell.
\end{gather*}
Repeating this implication for every physical cell and every stored rate row produces finitely many YALMIP constraints that suffice for the continuous-domain condition. The implication is only sufficient because a positive semidefinite matrix polynomial may have an indefinite Bernstein coefficient. Therefore, failure of Direct is inconclusive. The following alternatives provide additional sufficient certificates that may succeed when Direct fails.

\subsection{Opt-in alternatives}

Direct is the default finite certificate. The package also provides opt-in \hyperref[sec:pdlmi-polya]{P\'olya}, \hyperref[sec:pdlmi-putinar]{Putinar}, \hyperref[sec:pdlmi-sparse-putinar]{SparsePutinar}, \hyperref[sec:pdlmi-sparse-full-box]{SparseFullBox}, and \hyperref[sec:pdlmi-full-box]{FullBox} alternatives. Their orders, Gram constructions, support rules, and validation boundaries belong to the detailed \code{pdlmi} chapter and Appendix~\ref{app:certificates}. Each alternative supplies a sufficient continuum certificate that is separate from parameter sampling and the solver call.

\section{The Complete Path to YALMIP}
\index{YALMIP}\index{solver status}\index{rhodiff@\texttt{rhodiff}}

The full workflow is as follows: represent known data, create continuous decision functions, differentiate them cell by cell when rate terms are present, assemble and compare a residual, select a finite certificate, export it, solve, validate the status, and only then recover numerical values.

\begin{center}
    \begin{tikzpicture}[node distance=5mm and 4mm,font=\scriptsize]
        \node[dp/known,align=center,minimum width=28mm,minimum height=12mm] (known) {known data};
        \node[dp/decision,below=of known,align=center,minimum width=28mm,minimum height=12mm] (decision) {decision variables\\with given degree};
        \node[dp/stage,right=15mm of known,minimum width=20mm] (assemble) {assemble};
        \node[dp/contribution,below=of assemble,align=center,minimum width=28mm,minimum height=12mm] (differentiate) {optional cell-wise\\\code{rhodiff}};
        \node[dp/stage,right=of assemble,minimum width=24mm] (residual) {residual\\comparison};
        \node[dp/stage,right=of residual,minimum width=23mm] (certificate) {finite\\certificate};
        \node[dp/stage,right=of certificate,minimum width=27mm] (export) {\code{toYalmip}\\and \code{optimize}};
        \draw[dp/arrow] (known)--(assemble);
        \draw[dp/arrow] (decision)--(differentiate);
        \draw[dp/arrow] (decision.east) to[out=20,in=200] (assemble.south west);
        \draw[dp/arrow] (differentiate.north) to (assemble.south);
        \draw[dp/arrow] (assemble)--(residual);
        \draw[dp/arrow] (residual)--(certificate);
        \draw[dp/arrow] (certificate)--(export);
        \node[dp/result,below=7mm of export,minimum width=29mm,minimum height=12mm,align=center,font=\scriptsize] (recover) {status $=0$, then\\recover values};
        \draw[dp/arrow] (export.south) to (recover.north);
    \end{tikzpicture}
\end{center}

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
grid = {[0 0.5 1]};
rateBounds = [-0.2 0.2];

a = pdmat(grid, @(rho) 1 + rho + rho.^2, ...
    Degree=2, RateBounds=rateBounds);
P = pdvar(1, grid, Degree=2, RateBounds=rateBounds);
dP = rhodiff(P);

residual = P - 0.1 * dP - a;
certificate = residual >= 1e-6;
F = certificate.toYalmip();
opts = sdpsettings('solver', 'mosek', 'verbose', 0);
sol = optimize(F, [], opts);
assert(sol.problem == 0, sol.info)
solvedP = value(P);
\end{lstlisting}

Here \code{rhodiff} evaluates the affine derivative term at the vertices of the stored rate box. That vertex reduction is exact. The comparison creates a Direct \code{pdlmi} by default, and an opt-in certificate selector could be inserted before \code{toYalmip}. Solver settings and objectives remain ordinary YALMIP choices. Recovery of \code{solvedP} requires \code{sol.problem == 0}. Any infeasible, numerical, unknown, or otherwise nonzero status requires diagnosis before decisions or objectives are retained. A failed sufficient certificate leaves feasibility of the original continuum condition inconclusive.

\needspace{18\baselineskip}
\section{Manual Outline}

\begin{center}
    \begin{tikzpicture}[node distance=4mm,font=\scriptsize]
        \node[dp/known,minimum width=25mm,minimum height=12mm,align=center] (setup) {setup and\\verification};
        \node[dp/decision,minimum width=25mm,minimum height=12mm,align=center,right=of setup] (objects) {known data and\\decisions};
        \node[dp/certificate,minimum width=25mm,minimum height=12mm,align=center,right=of objects] (certificates) {finite\\certificates};
        \node[dp/result,minimum width=25mm,minimum height=12mm,align=center,right=of certificates] (examples) {worked\\models};
        \node[dp/neutral,minimum width=25mm,minimum height=12mm,align=center,right=of examples] (mathematics) {mathematical\\appendices};
        \draw[dp/arrow] (setup)--(objects);
        \draw[dp/arrow] (objects)--(certificates);
        \draw[dp/arrow] (certificates)--(examples);
        \draw[dp/arrow] (examples)--(mathematics);
    \end{tikzpicture}
\end{center}

\Cref{sec:setup} covers installation and verification. The \code{pdbase} chapter explains shared backend geometry, after which Chapters~\ref{sec:pdmat} and~\ref{sec:pdvar} develop known data and decision functions. \Cref{sec:pdlmi} gives the complete certificate-selection and YALMIP-export contracts, the worked examples assemble application-specific residuals and objectives, and Appendices~\ref{app:bernstein-math} and~\ref{app:certificates} derive the Bernstein and certificate mathematics.
