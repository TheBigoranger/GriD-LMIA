\chapter{Install and Verify the Package}
\label{sec:setup}

\section{One-Call Installation}
\index{installation}\index{path conflict}\index{YALMIP dependency}
\label{sec:installation}

Start \matlab{} in the checked-out repository and run the root-level installer once. The installer adds only the repository root to the end of the MATLAB path. MATLAB resolves the package's class, package, and private directories from that root. Dependency installation and \code{startup.m} configuration remain user-managed operations.

\begin{lstlisting}[style=dpmatlab]
report = install_pd_lmi();
\end{lstlisting}

The returned structure records the package root, the YALMIP root discovered on the current MATLAB path, the working SDP solver, the paths added by this run, and whether the path was persisted:

\begin{lstlisting}[style=dpsyntax]
report.PackageRoot
report.YALMIPRoot
report.Solver
report.AddedPaths
report.Persisted
\end{lstlisting}

YALMIP must already be on the active MATLAB path.  The installer validates \code{yalmip}, \code{sdpvar}, \code{sdpsettings}, \code{optimize}, and \code{getavailablesolvers}. It then probes available SDP solvers in the fixed commercial-first order MOSEK, COPT, SeDuMi, SDPT3, and LMILAB using a tiny bounded SDP.  A failed probe falls through to the next candidate.  If any precondition, shadow check, solver probe, or path persistence operation fails, the installer restores the original MATLAB path and stops with one of these stable errors:

\begin{lstlisting}[style=dpsyntax]
install_pd_lmi:MissingYALMIP
install_pd_lmi:IncompleteYALMIP
install_pd_lmi:NoWorkingSDPSolver
install_pd_lmi:PathConflict
install_pd_lmi:PathSaveFailed
\end{lstlisting}

Repeated successful calls are idempotent. When the repository root is already present, the installer preserves the single path entry and returns an empty \code{AddedPaths}.

\section{Validate the Installation}
\index{validation}\index{solver probe}
\label{sec:verification}

After the installer has selected and recorded a working solver, run the current MATLAB test entry point from the repository root:

\begin{lstlisting}[style=dpmatlab]
results = tests.run_all();
\end{lstlisting}

The entry point covers installer rollback and persistence behavior, helper utilities, \code{pdbase}, \code{pdmat}, \code{pdvar}, and \code{pdlmi}. YALMIP-backed tests and the solver smoke tests use the same automatic commercial-first solver policy as the installer. Their diagnostics identify the actual solver used.

\section{Run a Minimal Solver Example}
\label{sec:quick-start}

The installer already rejects shadowed package classes before it persists the path.  To inspect the selected solver and exercise the solver-facing assembly boundary, use the report together with a small known-data example:

\begin{lstlisting}[style=dpmatlab]
report.Solver
A = pdmat({[0 1]}, {1, 3}, Degree=1);
T = bernTable(A, "oneLine");
disp(T)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
    CellSubscript          Expression
    _____________    _______________________

        {[1]}        "(1-alpha)*1 + alpha*3"
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, {[0 1]}, "symmetric", Degree=4);
C = P >= 0;
F = toYalmip(C);
Cputinar = C.usePutinar();
Fputinar = toYalmip(Cputinar);
CspPutinar = C.useSpPut(2);
FspPutinar = toYalmip(CspPutinar);
sparsePutinarState = [CspPutinar.UseSparsePutinar, ...
    CspPutinar.SparsePutinarOrder, CspPutinar.CliqueSize, ...
    numel(CspPutinar.Constraints)]
Csp = C.useSpBox(2);
Fsp = toYalmip(Csp);
Cbox = C.useFullBox();
Fbox = toYalmip(Cbox);
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
sparsePutinarState = 1×4 double
     1     2     2     8
\end{lstlisting}

\code{F} is the direct coefficient certificate. For the same stored residual, \code{Fputinar} is dense Putinar, \code{FspPutinar} is clique-size-two SparsePutinar, \code{Fsp} is width-two SparseFullBox, and \code{Fbox} is FullBox. Any one can be passed to ordinary YALMIP calls. Use the selected solver from the report when an explicit solver setting is useful:

\begin{lstlisting}[style=dpsyntax]
opts = sdpsettings('solver', report.Solver, 'verbose', 0);
sol = optimize(F, [], opts);
\end{lstlisting}

\section{Citing GriD-LMIA}
\label{sec:citing-grid-lmia}

If \pkg{} supports published work, cite the software paper \cite{xu2026gridlmia}:

\begin{quote}
Yicheng Xu and Faryar Jabbari, ``GriD-LMIA: A Gridding-Based Assembler for Solving Differentiable Parameter-Dependent Linear Matrix Inequalities,'' arXiv:2608.03175, 2026. \href{https://arxiv.org/abs/2608.03175}{https://arxiv.org/abs/2608.03175}.
\end{quote}

The corresponding reusable BibTeX entry uses citation key \code{xu2026gridlmia}:

\begin{lstlisting}[style=dpsyntax]
@article{xu2026gridlmia,
  title   = {GriD-LMIA: A Gridding-Based Assembler for Solving
             Differentiable Parameter-Dependent Linear Matrix Inequalities},
  author  = {Xu, Yicheng and Jabbari, Faryar},
  journal = {arXiv preprint arXiv:2608.03175},
  year    = {2026},
  url     = {https://arxiv.org/abs/2608.03175}
}
\end{lstlisting}

\section{Navigate the Reference}
\label{sec:global-lookup}

\begin{tabularx}{\textwidth}{@{}lY@{}}
\toprule
Category & Entries \\
\midrule
Backend &
\hyperref[sec:pdbase]{\code{pdbase}},
\hyperref[sec:pdbase-constructor]{constructor},
\hyperref[sec:pdbase-inspection]{\code{cells}},
\hyperref[sec:pdbase-inspection]{\code{lbls}},
\hyperref[sec:pdbase-inspection]{\code{coeffs}},
\hyperref[sec:pdbase-counts]{\code{ncell}},
\hyperref[sec:pdbase-counts]{\code{ncoeff}},
\hyperref[sec:pdbase-counts]{\code{npar}},
\hyperref[sec:pdbase-counts]{shape protocols},
\hyperref[sec:pdbase-matrix-ops]{shared matrix operations} \\
Known data &
\hyperref[sec:pdmat]{\code{pdmat}},
\hyperref[sec:pdmat-construction]{construction},
\hyperref[sec:pdmat-evaluate]{\code{evaluate}},
\hyperref[sec:pdmat-table]{\code{bernTable}},
\hyperref[sec:pdmat-display]{\code{disp}/\code{display}},
\hyperref[sec:pdmat-plot]{\code{plot}},
\hyperref[sec:pdmat-algebra]{operators},
\hyperref[sec:pdmat-matrix-ops]{matrix methods} \\
Decision variables &
\hyperref[sec:pdvar]{\code{pdvar}},
\hyperref[sec:pdvar-construction]{construction},
\hyperref[sec:pdvar-rhodiff]{\code{rhodiff}},
\hyperref[sec:pdvar-algebra]{affine/mixed algebra},
\hyperref[sec:pdvar-matrix-ops]{matrix methods},
\hyperref[sec:pdvar-comparison]{comparisons} \\
LMIs &
\hyperref[sec:pdlmi]{\code{pdlmi}},
\hyperref[sec:pdlmi-construction]{construction},
  \hyperref[sec:pdlmi-direct]{direct coefficient assembly},
  \hyperref[sec:pdlmi-polya]{\code{usePolya}},
  \hyperref[sec:pdlmi-putinar]{\code{usePutinar}},
  \hyperref[sec:pdlmi-sparse-putinar]{\code{useSpPut}},
  \hyperref[sec:pdlmi-sparse-full-box]{\code{useSpBox}},
  \hyperref[sec:pdlmi-full-box]{\code{useFullBox}},
\hyperref[sec:pdlmi-toyalmip]{\code{toYalmip}},
\hyperref[sec:examples]{solver examples} \\
\bottomrule
\end{tabularx}

\section{Choose an API from a Task}
\label{sec:task-api-lookup}

The table below begins with a reader goal and points to the smallest public entry that owns it. The class chapters then give the complete syntax, validation boundary, executable example, and related mathematics.

\begin{tabularx}{\textwidth}{@{}p{0.31\textwidth}p{0.27\textwidth}Y@{}}
\toprule
Goal & Start with & Continue with \\
\midrule
Represent known parameter data & \hyperref[sec:pdmat-construction]{\code{pdmat}} & \hyperref[sec:pdmat-evaluate]{\code{evaluate}}, \hyperref[sec:pdmat-table]{\code{bernTable}}, and \hyperref[sec:pdmat-plot]{\code{plot}} \\
Create a polynomial decision & \hyperref[sec:pdvar-construction]{\code{pdvar}} & \hyperref[sec:pdvar-algebra]{affine algebra} and \hyperref[sec:pdvar-matrix-ops]{matrix operations} \\
Add scheduling-rate terms & \hyperref[sec:pdvar-rhodiff]{\code{rhodiff}} & \hyperref[sec:bernstein-differentiation]{differentiation} and \hyperref[sec:bernstein-rate-vertex-storage]{rate rows} \\
Build a finite certificate & \hyperref[sec:pdlmi-construction]{comparison operators} & \hyperref[sec:pdlmi-direct]{Direct} or an \hyperref[sec:pdlmi-lookup]{opt-in selector} \\
Export solver constraints & \hyperref[sec:pdlmi-toyalmip]{\code{toYalmip}} & \hyperref[sec:pdlmi-solver-use]{solver-status checks} \\
Inspect cell-local storage & \hyperref[sec:pdbase-inspection]{\code{cells}, \code{lbls}, \code{coeffs}} & \hyperref[sec:pdbase-fields]{read-only metadata} \\
Extend package internals & \hyperref[sec:helpers]{\code{helper.*}} & \hyperref[app:bernstein-math]{exact Bernstein operations} \\
\bottomrule
\end{tabularx}

\section{Find a Representative Example}
\label{sec:example-map}

Each row identifies the object type, parameter dimension, certificate family, and result that the example demonstrates. The reference chapters contain smaller per-function examples, while \cref{sec:examples} develops complete solver-facing workflows.

\begin{tabularx}{\textwidth}{@{}p{0.23\textwidth}p{0.16\textwidth}p{0.22\textwidth}Y@{}}
\toprule
Example & Object and dimension & Certificate & Demonstrated result \\
\midrule
Quick start in \cref{sec:quick-start} & scalar \code{pdmat} and matrix \code{pdvar}, one parameter & Direct and four Gram alternatives & coefficient inspection, selected state, and exported constraints \\
\hyperref[sec:pdmat-construction]{Known-data construction} & scalar \code{pdmat}, two parameters & certificate-free & anisotropic degree and a selected-cell expression \\
\hyperref[sec:pdvar-rhodiff]{Rate differentiation} & matrix \code{pdvar}, one and two parameters & certificate-free & derivative degree, exact re-elevation, and rate-row order \\
\hyperref[sec:full-box-example]{Certificate selection} & scalar \code{pdvar}, one and two parameters & P\'olya, Putinar, SparsePutinar, SparseFullBox, FullBox & canonical selector state and deterministic constraint counts \\
\hyperref[sec:parameter-dependent-lyapunov-smoke]{Lyapunov model} & matrix \code{pdvar}, one parameter & Direct & solver-status validation and recovered \code{pdmat} coefficients \\
\hyperref[sec:solver-example]{Rate-dependent block model} & matrix and scalar \code{pdvar}, one parameter & Direct with two rate rows & assembly counts, finite solve, and recovered objective \\
\bottomrule
\end{tabularx}

\section{Locate Diagnostics and Boundaries}
\label{sec:diagnostic-lookup}

Stable error identifiers preserve the owning API in their prefix. This index routes each common failure class to the detailed explanation beside the call that raises it.

\begin{tabularx}{\textwidth}{@{}p{0.29\textwidth}p{0.26\textwidth}Y@{}}
\toprule
Diagnostic family & Typical identifier & Owning explanation \\
\midrule
Installation and solver probe & \codeerr{install_pd_lmi}{MissingYALMIP}, \codeerr{install_pd_lmi}{NoWorkingSDPSolver} & \cref{sec:installation} \\
Grid, degree, storage, and rate metadata & \codeerr{pdbase}{InvalidGrid}, \codeerr{pdbase}{InvalidDegree}, \codeerr{pdbase}{InvalidRateBounds} & \cref{sec:pdbase-constructor} \\
Known-data source and coefficient evidence & \codeerr{pdmat}{InvalidSource}, \codeerr{pdmat}{FunctionOnlyAlgebra} & \cref{sec:pdmat-construction,sec:pdmat-limits} \\
Decision-affinity and multiplication & \codeerr{pdvar}{InvalidAddition}, \codeerr{pdvar}{InvalidMultiplication} & \cref{sec:pdvar-algebra,sec:pdvar-limits} \\
Matrix indexing and assignment & \codeerr{pdmat}{InvalidSubscript}, \codeerr{pdvar}{InvalidAssignment} & \cref{sec:pdmat-indexing,sec:pdvar-indexing} \\
Certificate order and selector state & \codeerr{pdlmi}{InvalidOrder}, \codeerr{pdlmi}{InvalidCliqueSize}, \codeerr{pdlmi}{InvalidBandWidth} & \cref{sec:pdlmi-polya,sec:pdlmi-putinar,sec:pdlmi-sparse-putinar,sec:pdlmi-sparse-full-box,sec:pdlmi-full-box} \\
Solver result interpretation & \code{sol.problem}, \code{sol.info} & \cref{sec:pdlmi-solver-use,sec:solver-example} \\
\bottomrule
\end{tabularx}

\section{Compare Object Capabilities}
\label{sec:capability-matrix}

This matrix distinguishes accepted operands, decision dependence, rate-row behavior, certificate ownership, and returned objects before the detailed reference entries introduce their local cases.

\begin{tabularx}{\textwidth}{@{}p{0.13\textwidth}p{0.22\textwidth}p{0.16\textwidth}p{0.16\textwidth}Y@{}}
\toprule
Owner & Accepted data or operands & Decision dependence & Rate rows & Main returns \\
\midrule
\code{pdbase} & normalized coefficient storage & numeric or affine payload metadata & preserved or constructed by \code{rhodiff} & backend value objects and inspection data \\
\code{pdmat} & function, global grid, nested local data, numeric and compatible \code{pdmat} operands & known data & preserved through compatible algebra & \code{pdmat}, numeric matrices, tables, and plots \\
\code{pdvar} & decision construction, numeric constants, affine \code{sdpvar}, coefficient-backed \code{pdmat} & affine decisions & ordinary rows or derivative vertex rows & \code{pdvar}, \code{pdlmi}, symbolic tables, and valued \code{pdmat} \\
\code{pdlmi} & compatible residual and relation plus one certificate selector & affine residual required & each row receives an independent certificate & value-semantic wrapper and YALMIP constraints \\
\code{helper.*} & normalized developer data for the named utility & utility-specific & enumeration and storage support & numeric plans, normalized metadata, or nested data \\
\bottomrule
\end{tabularx}

\section{Choose a Modeling or Certificate Control}
\label{sec:control-comparison}

The controls below act at different stages. Their separation helps a reader choose the parameter that matches the intended change.

\begin{tabularx}{\textwidth}{@{}p{0.22\textwidth}p{0.31\textwidth}Y@{}}
\toprule
Control & Changes & Preserves \\
\midrule
Grid refinement & physical-cell partition and available piecewise decision freedom & represented known data under exact subdivision and the physical parameter box \\
Modeled Bernstein degree & polynomial decision space and local coefficient count & chosen physical grid \\
Degree elevation & coefficient representation and target label count & represented polynomial and existing decisions \\
P\'olya increment & elevated coefficient test used by the selected certificate & original residual, decision space, physical cells, and rate rows \\
Absolute Gram order & Gram basis sizes, matched target degree, and block dimensions & original residual and certificate family \\
SparsePutinar clique size & overlapping Putinar tensor-window basis blocks & active Putinar terms and exact target-coefficient identities \\
SparseFullBox window size & overlapping FullBox tensor-window basis blocks & active FullBox masks and exact target-coefficient identities \\
Rate-vertex reduction & number of distinct affine rate rows after equal-bound directions are collapsed & complete rate-box condition through exact convex reduction \\
\bottomrule
\end{tabularx}
