\chapter[From LPV Models to Cellwise Bernstein Data]{\texorpdfstring{From LPV Models to\\Cellwise Bernstein Data}{From LPV Models to Cellwise Bernstein Data}}
\label{sec:canonical-pdlmi}
\label{sec:bernstein-motivation}

This chapter fixes the vocabulary and data model used throughout the manual.
It begins with a continuous control inequality, separates the physical grid
from local polynomial labels, and ends at the exact cell-local Bernstein data
consumed by the algebra in \cref{sec:cellwise-solver-path}.

\section{LPV Systems and Parameter-Dependent LMIs}

A \emph{linear parameter-varying (LPV) system}\index{LPV system} is a linear
state-space model whose matrices depend on a measurable scheduling vector
\(\boldsymbol\rho(t)=(\rho_1(t),\ldots,\rho_\ell(t))\).  For the autonomous
model used here,
\[
    \dot x(t)=A\bigl(\boldsymbol\rho(t)\bigr)x(t),
\]
the state equation is linear in \(x\), while the scheduling vector selects the
current matrix from the known family \(A(\boldsymbol\rho)\).  The admissible
scheduling values lie in a compact parameter set \(\mathcal P\), and a
rate-dependent problem also bounds
\(\dot{\boldsymbol\rho}(t)\in\mathcal V\).

A \emph{parameter-dependent linear matrix inequality} (PD-LMI)\index{PD-LMI}
asks a matrix inequality to hold for every
\(\boldsymbol\rho\in\mathcal P\).  When derivatives of decision functions
enter through the scheduling rate, this manual calls it a
\emph{differentiable parameter-dependent LMI} (DPD-LMI)\index{DPD-LMI}.  A
standard LPV Lyapunov model seeks a symmetric matrix
\(P(\boldsymbol\rho)\) such that
\begin{align}
    P(\boldsymbol\rho) & \succeq I,
    \label{eq:canonical-positive}               \\
    \dot P(\boldsymbol\rho,\dot{\boldsymbol\rho})
    +P(\boldsymbol\rho)A(\boldsymbol\rho)
    +A(\boldsymbol\rho)^{\mathsf T}P(\boldsymbol\rho)
    & \preceq-\varepsilon I.
    \label{eq:lpv-spine}
\end{align}
After known data are fixed, every term must remain affine in the decision
coefficients.  Removing \(\dot P\), or fixing
\(\dot{\boldsymbol\rho}=0\), recovers an ordinary PD-LMI.  The package inserts
no hidden strictness margin: the displayed \(\varepsilon I\) belongs to the
user's model.

\section{The Chain Rule in a DPD-LMI}

The dot on \(P\) is a time derivative along the scheduling trajectory, not a
new independent matrix.  The multivariable chain rule is
\[
    \dot P\bigl(\boldsymbol\rho(t)\bigr)
    =\frac{dP}{dt}
    =\sum_{s=1}^{\ell}
    \frac{\partial P}{\partial\rho_s}
    \bigl(\boldsymbol\rho(t)\bigr)\dot\rho_s(t).
\]
Write \(\boldsymbol\nu=\dot{\boldsymbol\rho}\) and give every scheduling
direction independent finite rate bounds,
\[
    \mathcal V
    =\prod_{s=1}^{\ell}[\underline\nu_s,\overline\nu_s].
\]
For a residual that is affine in \(\boldsymbol\nu\), the rate box is handled
exactly by traversing its \(2^\ell\) lower/upper vertices
\(\boldsymbol v^{(r)}\).  Thus, rather than retaining a rate continuum, the
rate-dependent inequality can be written in the block-diagonal form
\[
    \operatorname{diag}_{r=1}^{2^\ell}
    \mathcal F\bigl(\boldsymbol\rho,\boldsymbol v^{(r)}\bigr)
    \prec 0,
    \qquad \boldsymbol\rho\in\mathcal P.
\]
Each diagonal block is the residual at one rate vertex.  The remaining
continuum is only the scheduling parameter \(\boldsymbol\rho\), which the
package treats cellwise with its Bernstein certificates.

We therefore focus on the following general DPD-LMI.
\keyequation{General form of the differentiable parameter-dependent LMI}{%
\begin{equation}
  \mathcal F(\boldsymbol\rho,\dot{\boldsymbol\rho})
  = \sum_{i=1}^{N} R_i(\boldsymbol\rho)
  \sum_{j=1}^{\ell}
  \frac{\partial x_i}{\partial\rho_j}(\boldsymbol\rho)
  \dot\rho_j
  + F_0(\boldsymbol\rho)
  + \sum_{i=1}^{N} F_i(\boldsymbol\rho)x_i(\boldsymbol\rho)
  \prec 0, \qquad \forall (\boldsymbol\rho,\dot{\boldsymbol\rho}) \in\mathcal P\times\mathcal V.
\label{eq:general-dpdlmi}
\end{equation}
}
Here \(R_i\), \(F_0\), and \(F_i\) are known matrix functions, while the
\(x_i\) are the parameter-dependent decision functions.  The construction is
dimension-generic in \(\ell\), subject to the cost of the resulting tensor
data, and requires the displayed affine dependence on the rate.  Setting
\(R_i\equiv0\) for every \(i\) (collectively, \(R=0\)) removes the derivative
term and recovers an ordinary PD-LMI.

\section{Grid the Scheduling-Parameter Space}

Choose a one-dimensional physical grid
\[
    \mathcal G_s=
    \{\rho_s^{(1)},\ldots,\rho_s^{(k_s)}\}
\]
in every parameter direction \(s\).  The full tensor grid is
\(\mathcal G=\mathcal G_1\times\cdots\times\mathcal G_\ell\).  It contains
\(\prod_s k_s\) physical nodes and partitions \(\mathcal P\) into
\(\prod_s(k_s-1)\) physical hypercubes.  A hypercube is identified by the
one-based multi-index
\(\mathbf c=(c_1,\ldots,c_\ell)\) and has domain
\[
    \mathcal H_{\mathbf c}
    =\prod_{s=1}^{\ell}
    [\rho_s^{(c_s)},\rho_s^{(c_s+1)}].
\]
Inside that hypercube, the forward local coordinate is
\[
    \alpha_s=
    \frac{\rho_s-\rho_s^{(c_s)}}
    {\rho_s^{(c_s+1)}-\rho_s^{(c_s)}}\in[0,1].
\]
Physical-node indices and components of \(\mathbf c\) are one-based.  They
identify geometry, not coefficient positions.

\section{Physical Hypercubes and Local Bernstein Labels}

On one selected hypercube, a tensor-product polynomial of degree \(m\) is
\[
    C^{(\mathbf c)}(\boldsymbol\rho)=
    \sum_{\mathbf i\in\{0,\ldots,m\}^{\ell}}
    \left(\prod_{s=1}^{\ell}B_{i_s}^{m}(\alpha_s)\right)
    C^{(\mathbf c)}[\mathbf i],
\]
where
\[
    B_i^m(\alpha)=\binom mi(1-\alpha)^{m-i}\alpha^i.
\]
In one dimension, the first three degrees make the zero-based local labels
explicit.  For \(m=1\), \(B_0^1=1-\alpha\) and \(B_1^1=\alpha\), so
\[
    C^{(c)}(\rho)=(1-\alpha)C^{(c)}[0]
    +\alpha C^{(c)}[1].
\]
For degrees two and three, the individual basis functions are
\[
    \begin{aligned}
        m=2:\quad
         & B_0^2=(1-\alpha)^2,\qquad
        B_1^2=2(1-\alpha)\alpha,\qquad
        B_2^2=\alpha^2,                     \\
        m=3:\quad
         & B_0^3=(1-\alpha)^3,\qquad
        B_1^3=3(1-\alpha)^2\alpha,          \\[-1pt]
         & B_2^3=3(1-\alpha)\alpha^2,\qquad
        B_3^3=\alpha^3.
    \end{aligned}
\]
The corresponding matrix expansions add one local coefficient at a time:
\begin{align*}
    C^{(c)}(\rho)\big|_{m=2}
     & =(1-\alpha)^2C^{(c)}[0]
    +2(1-\alpha)\alpha C^{(c)}[1]
    +\alpha^2C^{(c)}[2],       \\
    C^{(c)}(\rho)\big|_{m=3}
     & =(1-\alpha)^3C^{(c)}[0]
    +3(1-\alpha)^2\alpha C^{(c)}[1]
    +3(1-\alpha)\alpha^2C^{(c)}[2]
    +\alpha^3C^{(c)}[3].
\end{align*}
The local Bernstein label \(\mathbf i\) is zero-based.  It chooses one matrix
coefficient inside the already selected physical hypercube; it is not another
physical point.  The nonnegative basis functions sum to one, so the polynomial
value lies in the convex hull of its coefficient matrices.  Exact elevation,
multiplication, and differentiation operate on these local rows without
changing the physical identity \(\mathbf c\).

The traversal order used by \code{cells}, \code{lbls}, \code{coeffs}, and
rate-vertex rows varies later dimensions fastest.  In two dimensions,
\code{lbls()} therefore lists
\([0,0],[0,1],\ldots,[0,m],[1,0],\ldots,[m,m]\).

\section{What One Hypercube Stores}
\label{sec:one-hypercube-storage}

The first two-dimensional example uses
\(\mathcal G_1=\{0,0.5,1\}\),
\(\mathcal G_2=\{0,1\}\), and degree two:

\begin{lstlisting}[style=dpmatlab]
grid2 = {[0 .5 1], [0 1]};
A2 = pdmat(grid2, @(rho1,rho2) ...
    [1+rho1+rho2, rho1*rho2; ...
        rho1*rho2,   2+rho1^2], Degree=2);
physicalCells = A2.cells()
localLabels = A2.lbls()
secondCellCoefficients = A2.coeffs([2 1])
sameLeaf = A2.LocalValues{2}{1}
\end{lstlisting}

The figure lists the nine matrices in \code{A2.lbls()} order, with \(i_1\)
varying more slowly than \(i_2\).

The grid has two physical hypercubes.  The highlighted one below is
\(\mathbf c=(2,1)\), whose domain is
\([0.5,1]\times[0,1]\).  Selecting it through
\code{A2.coeffs([2 1])} reaches the same nested storage leaf as
\code{A2.LocalValues{2}{1}}.

\begin{figure}[H]
\centering
\begin{tikzpicture}[x=3.2cm,y=3.2cm,>=Latex]
\fill[dpbluewash] (0.5,0) rectangle (1,1);
\draw[black!55,very thick] (0,0) rectangle (1,1);
\draw[black!55,very thick] (0.5,0)--(0.5,1);
\foreach \x/\lab in {0/{0},0.5/{0.5},1/{1}}{
        \draw (\x,0.02)--(\x,-0.02);
        \node[below=4pt,font=\scriptsize] at (\x,0) {$\lab$};}
\foreach \y/\lab in {0/{0},1/{1}}{
        \draw (0.01,\y)--(-0.01,\y);
        \node[left=4pt,font=\scriptsize] at (0,\y) {$\lab$};}
\node[font=\scriptsize] at (0.25,0.5) {$\mathbf c=(1,1)$};
\node[font=\scriptsize\bfseries] (selected) at (0.75,0.5)
{$\mathbf c=(2,1)$};
\node[below=10pt,font=\scriptsize] at (0.5,0) {$\rho_1$};
\node[rotate=90,above=11pt,font=\scriptsize] at (0,0.5) {$\rho_2$};

        \node[dp/panel,anchor=west,inner xsep=2.5mm,inner ysep=2.5mm,align=left,font=\scriptsize] (leaf) at (1.65,0.5) {%
            \textbf{Selected physical leaf}\\[-1pt]
            $\mathcal H_{(2,1)}=[0.5,1]\times[0,1]$\\
            \code{LocalValues{2}{1}}\\[2pt]
            \textbf{Nine matrices \(C^{(2,1)}[i_1,i_2]\)}\\[-2pt]
            $\begin{array}{c@{\hspace{4pt}}c@{\hspace{4pt}}c@{\hspace{4pt}}c}
                                                                                 & i_2=0 & i_2=1 & i_2=2 \\
                    i_1=0                                                        &
                    \begin{bmatrix}\frac32&0\\0&\frac94\end{bmatrix}             &
                    \begin{bmatrix}2&\frac14\\\frac14&\frac94\end{bmatrix}       &
                    \begin{bmatrix}\frac52&\frac12\\\frac12&\frac94\end{bmatrix}                         \\
                    i_1=1                                                        &
                    \begin{bmatrix}\frac74&0\\0&\frac52\end{bmatrix}             &
                    \begin{bmatrix}\frac94&\frac38\\\frac38&\frac52\end{bmatrix} &
                    \begin{bmatrix}\frac{11}4&\frac34\\\frac34&\frac52\end{bmatrix}                      \\
                    i_1=2                                                        &
                    \begin{bmatrix}2&0\\0&3\end{bmatrix}                         &
                    \begin{bmatrix}\frac52&\frac12\\\frac12&3\end{bmatrix}       &
                    \begin{bmatrix}3&1\\1&3\end{bmatrix}
                \end{array}$};
        \draw[dp/arrow] (selected.east) -- node[above,font=\tiny]
        {\code{A2.coeffs([2 1])}} (leaf.west);
        \end{tikzpicture}
    \caption{A physical hypercube selects one storage leaf.  Within that leaf,
        \code{A2.lbls()} supplies the nine zero-based local labels in the displayed
        order.  The matrices are Bernstein coefficients, not a \(3\)-by-\(3\) grid of
        point samples.}
    \label{fig:selected-hypercube-storage}
    \end{figure}

The selected leaf is a flat nine-entry coefficient cell: row \(q\) of
\code{A2.lbls()} labels entry \(q\).  Appendix~\ref{app:bernstein-math}
derives the conversion and ordering rules.

\section{Continuity Across Shared Faces}

Independent cell polynomials would not necessarily agree on a grid boundary.
The package instead shares the complete Bernstein coefficient face between
adjacent continuous cells.  On a face, the tensor polynomial depends only on
that face's coefficients, so sharing them gives \(C^0\) continuity.

For a one-dimensional degree-two example, take the two cells on the grid
\([0,0.5,1]\), with Bernstein controls
\[
  [0.8,\ 2.2,\ 1.4]\qquad\text{and}\qquad[1.4,\ -0.2,\ 1.8].
\]
Thus \(C^{(1)}[2]=C^{(2)}[0]=1.4\) is the shared value at
\(\rho=0.5\), while the interior controls remain independent.  The
one-sided physical derivatives there are \(-3.2\) from the left and
\(-6.4\) from the right.  The function is therefore continuous but not
\(C^1\).  Its derivative is bounded in magnitude by \(8\) on the two
cells, so it is globally Lipschitz with Lipschitz constant at most \(8\).
This is the intended contract: the package shares values on every shared
face\index{shared face}, but \code{rhodiff} produces cell-local derivative
data\index{derivative data} that may be discontinuous.

\begin{figure}[H]
\centering
\begin{tikzpicture}[x=1cm,y=0.62cm,font=\scriptsize]
  % Before: separate endpoint controls allow a jump at the shared face.
  \begin{scope}
    \draw[dp/axis] (0,0) -- (5.2,0) node[below] {\(\rho\)};
    \draw[dp/axis] (0,-1.2) -- (0,3.25) node[left] {\(C\)};
    \draw[dp/grid-line,densely dashed] (2.5,-1.1) -- (2.5,3.1);
    \draw[dp/curve-primary] plot[smooth] coordinates
      {(0,0.8) (1.25,2.2) (2.5,1.4)};
    \draw[dp/curve-secondary] plot[smooth] coordinates
      {(2.5,0.8) (3.75,-0.2) (5,1.8)};
    \fill[black] (2.5,1.4) circle (1.3pt);
    \fill[white,draw=black!70] (2.5,0.8) circle (1.3pt);
    \node[align=center] at (2.5,3.1) {Before: unshared controls\\can jump at the face};
    \node[below=3pt] at (2.5,0) {\(0.5\)};
  \end{scope}
  % After: the control face is shared, but tangent labels deliberately differ.
  \begin{scope}[xshift=7cm]
    \draw[dp/axis] (0,0) -- (5.2,0) node[below] {\(\rho\)};
    \draw[dp/axis] (0,-1.2) -- (0,3.25) node[left] {\(C\)};
    \draw[dp/grid-line,densely dashed] (2.5,-1.1) -- (2.5,3.1);
    \draw[dp/curve-primary] plot[smooth] coordinates
      {(0,0.8) (1.25,2.2) (2.5,1.4)};
    \draw[dp/curve-primary] plot[smooth] coordinates
      {(2.5,1.4) (3.75,-0.2) (5,1.8)};
    \fill[white,draw=black!90,thick] (2.5,1.4) circle (1.7pt);
    \node[align=center] at (2.5,3.1) {After: one shared value\\(continuous and Lipschitz)};
    \node[above left=1pt] at (2.5,1.4) {\(1.4\)};
    \node[below=3pt] at (2.5,0) {\(0.5\)};
    \node[align=right,anchor=east] at (2.35,0.35) {left slope\\\(-3.2\)};
    \node[align=left,anchor=west] at (2.65,0.35) {right slope\\\(-6.4\)};
    \node[below,align=center] at (2.5,-1.0) {\(\lvert C'(\rho)\rvert\le8\), but the derivative is not continuous};
  \end{scope}
\end{tikzpicture}
\caption{Sharing the face control changes a jump into a continuous piecewise
quadratic curve.  It does not force the two one-sided derivatives to agree.}
\label{fig:shared-face-lipschitz}
\end{figure}

\begin{center}
    \begin{tikzpicture}[y=1cm,
        face/.style={dp/decision,minimum width=22mm,minimum height=8mm,
                    font=\scriptsize,align=center},
        interior/.style={dp/contribution,minimum width=22mm,minimum height=8mm,
                    font=\scriptsize,align=center}]
    \node[face] (c10) {$C^{(1)}[0]$};
    \node[interior,right=7mm of c10] (c11) {$C^{(1)}[1]$};
    \node[face,minimum width=40mm,right=10mm of c11] (shared) {$C^{(1)}[2]=C^{(2)}[0]$};
    \node[interior,right=10mm of shared] (c21) {$C^{(2)}[1]$};
    \node[face,right=7mm of c21] (c22) {$C^{(2)}[2]$};
        \draw[dp/arrow] (c10)--(c11); \draw[dp/arrow] (c11)--(shared);
        \draw[dp/arrow] (shared)--(c21); \draw[dp/arrow] (c21)--(c22);
        \node[below=7pt of shared,font=\scriptsize] {shared value, separate slopes};
    \end{tikzpicture}
\end{center}

\input{chapters/workflow-constraints}
