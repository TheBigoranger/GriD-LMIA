\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb,mathtools}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{needspace}
\usepackage{titlesec}
\usepackage{graphicx}
\usepackage{float}
\usepackage{tikz}
\usepackage{listings}
\usepackage[numbered,framed]{matlab-prettifier}
\usepackage{hyperref}
\usepackage{cleveref}

\usetikzlibrary{arrows.meta,positioning,calc,fit,matrix,decorations.pathreplacing}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!55!black,
  urlcolor=blue!55!black,
  citecolor=blue!55!black,
  pdftitle={PD-LMI Package User Manual},
  pdfauthor={PD-LMI Package}
}

\newcommand{\sectionbreak}{\clearpage}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\definecolor{dpblue}{RGB}{35,86,125}
\definecolor{dpbluewash}{RGB}{239,245,250}
\definecolor{dpinputwash}{RGB}{247,249,252}
\definecolor{dpoutputwash}{RGB}{246,249,246}
\definecolor{dpknown}{RGB}{35,86,125}
\definecolor{dpdecision}{RGB}{103,78,167}
\definecolor{dpcontribution}{RGB}{181,103,24}
\definecolor{dpresult}{RGB}{38,120,83}
\definecolor{dpcertificate}{RGB}{180,82,38}
\definecolor{dpneutral}{RGB}{83,92,104}
% Shared semantic drawing vocabulary.  All conceptual figures use the same
% input/result/certificate roles, restrained fills, rounded corners, and arrow
% weight.  Dense relations are split into panels instead of crossing arrows.
\tikzset{
  every picture/.append style={>=Latex},
  dp/known/.style={draw=dpknown!85!black,fill=dpknown!8,rounded corners=2pt},
  dp/decision/.style={draw=dpdecision!85!black,fill=dpdecision!8,rounded corners=2pt},
  dp/contribution/.style={draw=dpcontribution!85!black,fill=dpcontribution!9,rounded corners=2pt},
  dp/result/.style={draw=dpresult!85!black,fill=dpresult!8,rounded corners=2pt},
  dp/certificate/.style={draw=dpcertificate!85!black,fill=dpcertificate!8,rounded corners=2pt},
  dp/neutral/.style={draw=dpneutral!75,fill=white,rounded corners=2pt},
  dp/arrow/.style={->,semithick,draw=dpneutral},
  dp/panel/.style={draw=black!18,rounded corners=3pt,inner sep=6pt}
}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.3em}
\hbadness=10000
\titleformat{\section}[display]
  {\normalfont\Huge\bfseries}
  {\thesection}
  {0.6em}
  {}
\titlespacing*{\subsection}{0pt}{1.25\baselineskip}{0.55\baselineskip}
\titlespacing*{\subsubsection}{0pt}{1.05\baselineskip}{0.4\baselineskip}

\lstdefinestyle{dpmatlab}{
  style=Matlab-editor,
  basicstyle=\mlttfamily\small,
  breaklines=true,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  frame=single,
  rulecolor=\color{dpblue!35},
  backgroundcolor=\color{dpinputwash},
  framesep=4pt,
  framerule=0.45pt,
  aboveskip=0.45\baselineskip,
  belowskip=0.85\baselineskip,
  title={\textcolor{dpblue}{\sffamily\bfseries\footnotesize MATLAB input}},
  numbers=none
}

\lstdefinestyle{dpsyntax}{
  basicstyle=\ttfamily\small,
  breaklines=true,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  frame=single,
  rulecolor=\color{dpblue!45},
  backgroundcolor=\color{dpbluewash},
  framesep=4pt,
  framerule=0.55pt,
  aboveskip=0.45\baselineskip,
  belowskip=0.85\baselineskip,
  title={\textcolor{dpblue}{\sffamily\bfseries\footnotesize Call forms}},
  numbers=none
}

\lstdefinestyle{dpoutput}{
  basicstyle=\ttfamily\scriptsize,
  breaklines=false,
  columns=fullflexible,
  keepspaces=true,
  frame=single,
  rulecolor=\color{green!35!black},
  backgroundcolor=\color{dpoutputwash},
  framesep=4pt,
  framerule=0.55pt,
  aboveskip=0.45\baselineskip,
  belowskip=0.95\baselineskip,
  title={\textcolor{green!35!black}{\sffamily\bfseries\footnotesize Output}},
  numbers=none
}

\lstdefinestyle{dpwideoutput}{
  basicstyle=\ttfamily\tiny,
  breaklines=false,
  columns=fullflexible,
  keepspaces=true,
  frame=single,
  rulecolor=\color{green!35!black},
  backgroundcolor=\color{dpoutputwash},
  framesep=4pt,
  framerule=0.55pt,
  aboveskip=0.45\baselineskip,
  belowskip=0.95\baselineskip,
  title={\textcolor{green!35!black}{\sffamily\bfseries\footnotesize Output}},
  numbers=none
}

% Keep short reference listings together; long transcripts may still flow
% naturally when they exceed the remaining page height.
\AtBeginEnvironment{lstlisting}{\needspace{6\baselineskip}}

\newcommand{\pkg}{PD-LMI package}
\newcommand{\matlab}{MATLAB}
\newcommand{\code}[1]{\texttt{\detokenize{#1}}}
\newcommand{\codeerr}[2]{\code{#1:}\allowbreak\code{#2}}
\newcommand{\refhead}[1]{%
  \par\addvspace{0.75\baselineskip}%
  \needspace{4\baselineskip}%
  \noindent\textcolor{black!75}{\sffamily\bfseries\normalsize #1}\par
  \nopagebreak\vspace{0.22\baselineskip}%
  \noindent\textcolor{black!55}{\rule{\linewidth}{0.45pt}}\par
  \vspace{0.48\baselineskip}\nopagebreak}
\newcommand{\syntaxhead}{\refhead{Syntax}}
\newcommand{\examplehead}{\refhead{Example}}
\newcommand{\arghead}{\refhead{Arguments and options}}
\newcommand{\ideastep}[1]{%
  \par\addvspace{0.65\baselineskip}%
  \needspace{3\baselineskip}%
  \noindent\textcolor{dpblue}{\sffamily\bfseries #1.}\enspace}

\title{\pkg\\User Manual}
\author{Version v0.3.3}
\date{July 2026}

\begin{document}

\maketitle

\begin{abstract}
This manual teaches the \pkg{} from the continuous matrix inequality to the
finite semidefinite program.  Its core representation is a continuous
piecewise-polynomial matrix function on a physical cell grid, expressed in a
cell-local tensor-product Bernstein basis.  A worked \(2\)-by-\(2\) LPV
Lyapunov problem first introduces quadratic decisions, physical cells, local
Bernstein coefficients, matrix algebra, parameter-rate vertices, certificate
selection, and the YALMIP boundary.  The
reference chapters then document \code{pdbase}, \code{pdmat}, \code{pdvar}, and
\code{pdlmi}.  Two self-contained appendices develop the Bernstein and
polynomial-positivity background for readers who know basic matrices and
calculus but have not previously used Bernstein bases or SOS certificates.
\end{abstract}

\tableofcontents

\section{From a PD-LMI to Solver Constraints}
\label{sec:canonical-pdlmi}

\subsection{Begin With The Continuous Inequality}

Let the scheduling vector be
\(\boldsymbol\rho=(\rho_1,\ldots,\rho_\ell)\in\mathcal P\).  A
\emph{parameter-dependent LMI} is abbreviated \emph{PD-LMI} throughout this
manual and is the umbrella problem class named by the package.  When a model
also uses the bounded rate
\(\dot{\boldsymbol\rho}\in\mathcal V\) and derivatives of its decision
functions, we call it a \emph{differentiable parameter-dependent LMI}, or
\emph{DPD-LMI}.  A canonical DPD-LMI is
\begin{equation}
  \sum_i R_i(\boldsymbol\rho)
    \left(\sum_{j=1}^{\ell}
      \frac{\partial x_i(\boldsymbol\rho)}{\partial\rho_j}\dot\rho_j\right)
  +F_0(\boldsymbol\rho)
  +\sum_i F_i(\boldsymbol\rho)x_i(\boldsymbol\rho)\prec0.
  \label{eq:canonical-dpdlmi}
\end{equation}
The known functions are the \(R_i\) and \(F_i\); the differentiable functions
\(x_i\) are decisions.  After known data are fixed, every term must remain
affine in the decision coefficients.  Removing the derivative term recovers
an ordinary PD-LMI.  Thus ``DPD-LMI'' adds a derivative qualifier; it does not
replace the package's PD-LMI name.

A concrete one-parameter example is the LPV Lyapunov condition
\begin{align}
  P(\rho)&\succeq I, \label{eq:canonical-positive}\\
  \dot P(\rho,\dot\rho)+P(\rho)A(\rho)
    +A(\rho)^{\mathsf T}P(\rho)&\preceq-\varepsilon I, \label{eq:lpv-spine}\\
  \dot P(\rho,\dot\rho)&=
    \frac{\partial P(\rho)}{\partial\rho}\dot\rho.
\end{align}
Taking \(\dot\rho=0\), or removing \(\dot P\), gives the familiar PD-LMI
Lyapunov inequality.  In the worked path below \(P\) is \(2\)-by-\(2\),
\(\rho\in[0,1]\), \(\dot\rho\in[-1,1]\), and
\(\varepsilon=10^{-6}\).  The package inserts no hidden strictness margin;
the displayed \(\varepsilon I\) is part of the user's model.

\begin{center}
\begin{tikzpicture}[node distance=13mm and 23mm,>=Latex,
  box/.style={draw=dpblue!75!black,rounded corners=2pt,fill=dpbluewash,
    align=center,font=\small,minimum width=40mm,minimum height=11mm,
    inner sep=5pt}]
  \node[box] (continuous) {PD-LMI / DPD-LMI\\continuous domain};
  \node[box,right=of continuous] (coeff) {Bernstein data\\cell by cell};
  \node[box,below=of coeff] (certificate) {finite certificate\\direct / P\'olya / Putinar / full box};
  \node[box,left=of certificate] (solver) {YALMIP model\\solver ready};
  \draw[->,thick,dpblue] (continuous) --
    node[above=3pt,font=\scriptsize]{represent} (coeff);
  \draw[->,thick,dpblue] (coeff) --
    node[right=4pt,font=\scriptsize]{certify} (certificate);
  \draw[->,thick,dpblue] (certificate) --
    node[above=3pt,font=\scriptsize]{export} (solver);
\end{tikzpicture}

\textit{The package changes representation and certificate; it does not
replace YALMIP's solver interface.}
\end{center}

Figure colors keep one semantic meaning throughout the manual:
\textcolor{dpknown}{blue is known data},
\textcolor{dpdecision}{violet is a decision},
\textcolor{dpcontribution}{amber is an intermediate contribution},
\textcolor{dpresult}{green is a computed result}, and
\textcolor{dpcertificate}{rust is a certificate choice}.  Neutral gray arrows
show reading order.  When a relation has many contributions, separate panels
replace crossing edge fans.

\subsection{Separate Physical Nodes, Cells, And Local Labels}

For one parameter choose physical grid nodes
\[
  \rho_0<\rho_1<\rho_2.
\]
The subscript on \(\rho_k\) identifies a physical node only.  The two physical
cells have separate identities \(c=1\) and \(c=2\):
\([\rho_0,\rho_1]\) and \([\rho_1,\rho_2]\).  On cell \(c\), define the
forward coordinate
\[
  \alpha=\frac{\rho-\rho_{c-1}}{\rho_c-\rho_{c-1}}\in[0,1].
\]
For local degree \(m=2\), the three coefficient labels are bracketed local
positions, not additional physical nodes:
\[
  M_c(\alpha)=\sum_{i=0}^{2}B_i^2(\alpha)C^{(c)}[i].
\]

\begin{center}
\begin{tikzpicture}[x=3.0cm,y=1cm,>=Latex,
  coeff/.style={draw=dpblue!75!black,rounded corners=2pt,fill=dpbluewash,
    font=\scriptsize,minimum width=10mm,minimum height=6mm}]
  \draw[very thick,black!55] (0,0) -- (2,0);
  \foreach \x/\lab in {0/{\rho_0},1/{\rho_1},2/{\rho_2}}{
    \filldraw[fill=white,draw=black!70] (\x,0) circle (1.6pt);
    \node[above=2pt,font=\small] at (\x,0) {$\lab$};}
  \draw[decorate,decoration={brace,mirror,amplitude=4pt}] (0,-0.18) -- (1,-0.18)
    node[midway,below=6pt,font=\scriptsize]{physical cell (c=1)};
  \draw[decorate,decoration={brace,mirror,amplitude=4pt}] (1,-0.18) -- (2,-0.18)
    node[midway,below=6pt,font=\scriptsize]{physical cell (c=2)};
  \node[coeff] at (0.13,-0.82) {$C^{(1)}[0]$};
  \node[coeff] at (0.50,-0.82) {$C^{(1)}[1]$};
  \node[coeff] at (0.87,-0.82) {$C^{(1)}[2]$};
  \node[coeff] at (1.13,-0.82) {$C^{(2)}[0]$};
  \node[coeff] at (1.50,-0.82) {$C^{(2)}[1]$};
  \node[coeff] at (1.87,-0.82) {$C^{(2)}[2]$};
  \node[align=center,font=\scriptsize] at (1,-1.38)
    {physical-node subscripts (k), cell identity (c), and local labels ([i]) have different jobs};
\end{tikzpicture}
\end{center}

In MATLAB, the physical grid and degree are likewise independent choices.
The example below uses degree two.  Each cell has labels ([0]), ([1]), and
([2]), while adjacent cells share only their common face value.  The middle
coefficient of each cell is an interior control, not a physical-grid sample.

\begin{lstlisting}[style=dpmatlab]
M = pdmat([0 0.5 1], {10, 15, 20, 25, 30}, Degree=2);
disp(M.cells())
disp(M.lbls())
fprintf("cells=%d, coefficients per cell=%d\n", M.ncell(), M.ncoeff());
c1 = M.coeffs(1);
c2 = M.coeffs(2);
disp([c1{:}])
disp([c2{:}])
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
     1
     2

     0
     1
     2

cells=2, coefficients per cell=3
    10    15    20
    20    25    30
\end{lstlisting}

The shared value \(20\) belongs to the physical interface \(\rho_1\).  It is
seen as \(C^{(1)}[2]\) from the left cell and \(C^{(2)}[0]\) from the right.
This is \(C^0\) face sharing: values meet, but the two cell-local derivatives
need not agree.  Consequently the representation is a continuous
piecewise-polynomial Bernstein representation on a gridding partition, not a
B-spline construction and not a claim of global differentiability.

\subsection{Store Known Data And Decisions On The Grid}

The known LPV matrix in the worked example is affine in \(\rho\).  The
physical grid has two cells, \([0,0.5]\) and \([0.5,1]\).  Supplying
\code{Degree=1} asks \code{pdmat} to retain exact function evaluation and
verified cell-local Bernstein coefficient evidence on both cells.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
grid = {[0 0.5 1]};
A = pdmat(grid, @(rho) [-1, 0.5; -1, -2] + ...
    rho * [-1.3, -20; 2, -10], Degree=1);
fprintf("A: %d-by-%d, cells=%d, degree=%d, coefficients/cell=%d\n", ...
    size(A,1), size(A,2), A.ncell(), A.Degree, A.ncoeff());
disp(A.evaluate(0))
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
A: 2-by-2, cells=2, degree=1, coefficients/cell=2
   -1.0000    0.5000
   -1.0000   -2.0000
\end{lstlisting}

The decision \(P(\rho)\) uses the same physical grid but local degree two.
Each cell stores three full symmetric coefficient matrices.  The right face of
cell 1 and the left face of cell 2 are the same YALMIP decision handle, so the
six local positions contain five global control matrices.  Only this face is
shared; the two interior controls remain independent.

\begin{lstlisting}[style=dpmatlab]
P = pdvar(2, grid, "symmetric", Degree=2);
fprintf("P: %d-by-%d, degree=%d, continuous=%d\n", ...
    size(P,1), size(P,2), P.Degree, P.IsContinuous);
fprintf("local coefficients/cell=%d, global controls=%d\n", ...
    numel(P.coeffs(1)), 2 * P.Degree + 1);
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
P: 2-by-2, degree=2, continuous=1
local coefficients/cell=3, global controls=5
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[x=2.05cm,y=1cm,
  face/.style={dp/decision,minimum width=18mm,minimum height=9mm,
    font=\scriptsize,align=center},
  interior/.style={dp/contribution,minimum width=18mm,minimum height=9mm,
    font=\scriptsize,align=center}]
  \node[face] (p0) at (0,0) {$P^{(1)}[0]$\\face};
  \node[interior] (p1) at (1,0) {$P^{(1)}[1]$\\interior};
  \node[face] (p2) at (2,0) {$P^{(1)}[2]$\\shared face};
  \node[interior] (p3) at (3,0) {$P^{(2)}[1]$\\interior};
  \node[face] (p4) at (4,0) {$P^{(2)}[2]$\\face};
  \draw[dp/arrow] (p0)--(p1); \draw[dp/arrow] (p1)--(p2);
  \draw[dp/arrow] (p2)--(p3); \draw[dp/arrow] (p3)--(p4);
  \node[above=5pt of p2,font=\scriptsize,align=center]
    {$P^{(1)}[2]=P^{(2)}[0]$};
  \node[below=7pt of p2,font=\scriptsize,align=center]
    {six local positions, five global matrix controls; value continuity only};
\end{tikzpicture}
\end{center}

\subsection{Form Matrix Algebra In Coefficient Space}

Fix a physical cell (c).  Its identity remains in the parenthesized
superscript while the bracketed integers are local Bernstein labels:
\(P=\sum_iB_i^mP^{(c)}[i]\) and
\(A=\sum_jB_j^nA^{(c)}[j]\).  Their ordered product is
\[
  PA=\sum_{k=0}^{m+n}B_k^{m+n}R^{(c)}[k],
  \qquad
  R^{(c)}[k]=\sum_{i+j=k}
    \frac{\binom mi\binom nj}{\binom{m+n}{k}}
    P^{(c)}[i]A^{(c)}[j].
\]
For the quadratic decision and affine known factor in this worked example,
write \(P_i=P^{(c)}[i]\) and \(A_j=A^{(c)}[j]\).  The four cubic
coefficients are
\[
  R^{(c)}[0]=P_0A_0,\qquad
  R^{(c)}[1]=\frac{P_0A_1+2P_1A_0}{3},
\]
\[
  R^{(c)}[2]=\frac{2P_1A_1+P_2A_0}{3},\qquad
  R^{(c)}[3]=P_2A_1.
\]
The pairs are first grouped by the anti-diagonal relation \(k=i+j\), then
combined with the binomial weights.  This is a weighted convolution, not a
samplewise product.  Matrix order is preserved in every contribution.

\begin{lstlisting}[style=dpmatlab]
PA = P * A;
ATP = A' * P;
fprintf("degree(P*A)=%d, degree(A'*P)=%d\n", PA.Degree, ATP.Degree);
fprintf("coefficient matrices in P*A=%d\n", numel(PA.coeffs(1)));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
degree(P*A)=3, degree(A'*P)=3
coefficient matrices in P*A=4
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[node distance=7mm,
  item/.style={minimum width=27mm,minimum height=6mm,font=\scriptsize,
    align=center},
  group/.style={minimum width=43mm,minimum height=7mm,font=\scriptsize,
    align=left},
  resultrow/.style={minimum width=43mm,minimum height=7mm,font=\scriptsize,
    align=left}]
  \matrix (inputs) [matrix of nodes,nodes={item,dp/neutral},row sep=2mm] {
    \(P_0,P_1,P_2\)\\
    \(A_0,A_1\)\\
  };
  \node[above=3pt of inputs,font=\scriptsize\bfseries,text=dpdecision]
    {1. Input rows};
  \node[group,dp/contribution,right=13mm of inputs] (g0)
    {\(k=0:\ (0,0)\)};
  \node[group,dp/contribution,below=2mm of g0] (g1)
    {\(k=1:\ (0,1),(1,0)\)};
  \node[group,dp/contribution,below=2mm of g1] (g2)
    {\(k=2:\ (1,1),(2,0)\)};
  \node[group,dp/contribution,below=2mm of g2] (g3)
    {\(k=3:\ (2,1)\)};
  \node[above=3pt of g0,font=\scriptsize\bfseries,text=dpcontribution]
    {2. Group \(i+j=k\)};
  \node[resultrow,dp/result,right=13mm of g0] (r0) {\(R[0]=P_0A_0\)};
  \node[resultrow,dp/result,below=2mm of r0] (r1)
    {\(R[1]=(P_0A_1+2P_1A_0)/3\)};
  \node[resultrow,dp/result,below=2mm of r1] (r2)
    {\(R[2]=(2P_1A_1+P_2A_0)/3\)};
  \node[resultrow,dp/result,below=2mm of r2] (r3) {\(R[3]=P_2A_1\)};
  \node[above=3pt of r0,font=\scriptsize\bfseries,text=dpresult]
    {3. Normalize};
  \draw[dp/arrow] (inputs.east)--node[above,font=\tiny]{enumerate}(g1.west);
  \draw[dp/arrow] (g1.east)--node[above,font=\tiny]{weight}(r1.west);
\end{tikzpicture}

\textit{The staged view replaces a crossing arrow fan: enumerate ordered
products, group by one anti-diagonal, then apply binomial normalization.}
\end{center}

\subsection{Differentiate And Enumerate Rate Vertices}

For a degree-\(m\) cell (c) of width \(h_c\), differentiation is a forward
difference of adjacent local coefficients:
\[
  \frac{\partial P}{\partial\rho}
  =\frac{m}{h_c}\sum_{i=0}^{m-1}
    \bigl(P^{(c)}[i+1]-P^{(c)}[i]\bigr)B_i^{m-1}(\alpha).
\]
Because \(\dot P=(\partial P/\partial\rho)\dot\rho\) is affine in the rate,
checking both vertices \(-1\) and \(+1\) covers the whole interval
\(\dot\rho\in[-1,1]\).

\begin{lstlisting}[style=dpmatlab]
D = rhodiff(P, [-1 1]);
fprintf("derivative degree=%d, continuous=%d\n", D.Degree, D.IsContinuous);
fprintf("rate rows=%d, coefficients per row=%d\n", ...
    size(D.coeffs(1),1), size(D.coeffs(1),2));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
derivative degree=1, continuous=0
rate rows=2, coefficients per row=2
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=8mm and 13mm,
  box/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    align=center,font=\scriptsize,minimum width=28mm,minimum height=9mm}]
  \node[box] (diff) {$\partial P/\partial\rho$\\two degree-one coefficients};
  \node[box,right=of diff,yshift=7mm] (lo) {$\dot\rho=-1$\\lower rate vertex};
  \node[box,right=of diff,yshift=-7mm] (hi) {$\dot\rho=+1$\\upper rate vertex};
  \draw[->,thick,dpblue] (diff.east) -- (lo.west);
  \draw[->,thick,dpblue] (diff.east) -- (hi.west);
  \node[below=16mm of diff,font=\scriptsize,align=center]
    {rate rows are not physical cells and are not extra Bernstein labels};
\end{tikzpicture}
\end{center}

\subsection{Compare The Residual And Choose A Certificate}

The following MATLAB expressions are a direct transcription of
\cref{eq:canonical-positive,eq:lpv-spine}.  On each cell, addition elevates the
degree-one derivative rows to the degree-three algebraic residual without
changing their represented values.

\begin{lstlisting}[style=dpmatlab]
residual = D + P * A + A' * P;
Cdecay = residual <= -1e-6 * eye(2);
Cpositive = P >= eye(2);
fprintf("residual degree=%d\n", residual.Degree);
fprintf("direct constraints: decay=%d, positivity=%d\n", ...
    numel(Cdecay.Constraints), numel(Cpositive.Constraints));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
residual degree=3
direct constraints: decay=16, positivity=6
\end{lstlisting}

There are two physical cells, two rate vertices, and four degree-three local
labels, hence \(2\times2\times4=16\) decay constraints.  Positivity has no
rate rows and uses three degree-two labels on each cell, hence
\(2\times3=6\) direct constraints.  The shared face is deliberately visited
from each physical cell because direct certification is cell-local.

Direct coefficient constraints are the default.  Three opt-in alternatives keep
the same stored residual but change the finite sufficient certificate:

\begin{lstlisting}[style=dpmatlab]
Cpolya = Cdecay.applyPolya(1);
Cputinar = Cdecay.applyPutinar();
Cbox = Cdecay.applyFullBoxPreorder();
fprintf("direct=%d, Polya(1)=%d, Putinar=%d, full-box=%d\n", ...
    numel(Cdecay.Constraints), numel(Cpolya.Constraints), ...
    numel(Cputinar.Constraints), numel(Cbox.Constraints));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
direct=16, Polya(1)=20, Putinar=28, full-box=24
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=7mm and 9mm,
  mode/.style={draw=dpblue!75!black,rounded corners=2pt,fill=dpbluewash,
    align=center,font=\scriptsize,minimum width=35mm,minimum height=14mm}]
  \node[mode] (res) {stored residual\\physical cells and rate rows};
  \node[mode,right=of res,yshift=20mm] (direct) {direct\\sign of every coefficient};
  \node[mode,right=of res,yshift=7mm] (polya) {P\'olya\\elevate, then test coefficients};
  \node[mode,right=of res,yshift=-7mm] (putinar) {Putinar box module\\singleton generators + identities};
  \node[mode,right=of res,yshift=-20mm] (box) {full box\\all generator products + identities};
  \draw[->,thick,dpblue] (res.east) -- (direct.west);
  \draw[->,thick,dpblue] (res.east) -- (polya.west);
  \draw[->,thick,dpblue] (res.east) -- (putinar.west);
  \draw[->,thick,dpblue] (res.east) -- (box.west);
\end{tikzpicture}

\textit{A failed sufficient certificate does not prove that the continuous
inequality is infeasible.  Appendix~\ref{app:certificates} derives the four
implemented choices and places them beside SOS background.}
\end{center}

\subsection{Export The Finite Model To YALMIP}

\code{toYalmip} returns exactly the stored finite constraints.  It does not
select a solver, add an objective, or insert a margin.

\begin{lstlisting}[style=dpmatlab]
Fdecay = toYalmip(Cdecay);
Fpositive = Cpositive.toYalmip();
fprintf("exported constraints: decay=%d, positivity=%d\n", ...
    length(Fdecay), length(Fpositive));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
exported constraints: decay=16, positivity=6
\end{lstlisting}

Use ordinary YALMIP calls after that boundary:

\begin{lstlisting}[style=dpmatlab]
solver = 'lmilab';
if exist('mosekopt', 'file') ~= 0
    solver = 'mosek';
end
opts = sdpsettings('solver', solver, 'verbose', 0);
sol = optimize([Fdecay, Fpositive], [], opts);
fprintf("YALMIP problem code: %d\n", sol.problem);
\end{lstlisting}

The final code is produced by the installed solver and should be interpreted
with \code{sol.info}; it is not a package-owned status.  The complete tested
solver examples in \cref{sec:parameter-dependent-lyapunov-smoke,sec:solver-example}
include assertions appropriate to their solver paths.

\subsection{Generalize From An Interval To A Hypercube}

Different parameter directions need not have the same number of physical
nodes.  For example, four nodes in the first direction and three in the second
create a \(3\)-by-\(2\) array of six physical cells:

\begin{lstlisting}[style=dpmatlab]
grid2 = {[-1 0 0.5 1], [10 15 20]};
G = pdbase(grid2, [1 1], 1);
fprintf("physical cells=%d, coefficients per cell=%d\n", ...
    G.ncell(), G.ncoeff());
disp(G.cells())
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
physical cells=6, coefficients per cell=4
     1     1
     1     2
     2     1
     2     2
     3     1
     3     2
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[x=1.55cm,y=1.25cm,>=Latex,
  cell/.style={font=\scriptsize,text=dpblue!70!black}]
  \foreach \x/\lab in {0/{\rho_{1,0}},1/{\rho_{1,1}},2/{\rho_{1,2}},3/{\rho_{1,3}}}{
    \draw[black!45] (\x,0) -- (\x,2);
    \node[below=3pt,font=\scriptsize] at (\x,0) {$\lab$};}
  \foreach \y/\lab in {0/{\rho_{2,0}},1/{\rho_{2,1}},2/{\rho_{2,2}}}{
    \draw[black!45] (0,\y) -- (3,\y);
    \node[left=3pt,font=\scriptsize] at (0,\y) {$\lab$};}
  \node[cell] at (0.5,0.5) {$\boldsymbol c=(1,1)$};
  \node[cell] at (0.5,1.5) {$\boldsymbol c=(1,2)$};
  \node[cell] at (1.5,0.5) {$\boldsymbol c=(2,1)$};
  \node[cell] at (1.5,1.5) {$\boldsymbol c=(2,2)$};
  \node[cell] at (2.5,0.5) {$\boldsymbol c=(3,1)$};
  \node[cell] at (2.5,1.5) {$\boldsymbol c=(3,2)$};
  \draw[->,thick,dpblue] (0,-0.55) -- (3,-0.55) node[right,font=\scriptsize]{$\rho_1$};
  \draw[->,thick,dpblue] (-0.55,0) -- (-0.55,2) node[above,font=\scriptsize]{$\rho_2$};
  \node[draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=left] at (5.0,1)
    {inside one degree-one cell:\\local labels\\
     \(\boldsymbol i=[0,0],[0,1],\)\\
     \(\phantom{\boldsymbol i={}}[1,0],[1,1]\)\\coefficients
     \(C^{(\boldsymbol c)}[\boldsymbol i]\)};
\end{tikzpicture}
\end{center}

In \(\ell\) dimensions, a physical node has an \(\ell\)-component subscript
\(\boldsymbol k=(k_1,\ldots,k_\ell)\) and is written
\(\boldsymbol\rho_{\boldsymbol k}\).  Direction \(s\) may have its own node
count \(N_s\).  A physical cell has the separate identity
\(\boldsymbol c=(c_1,\ldots,c_\ell)\), so the number of cells is
\(\prod_s(N_s-1)\).  With the forward coordinates
\[
  \alpha_s=\frac{\rho_s-\rho_{s,c_s-1}}
                  {\rho_{s,c_s}-\rho_{s,c_s-1}},
\]
the local degree-(m) tensor representation is
\[
  M_{\boldsymbol c}(\boldsymbol\alpha)=
  \sum_{\boldsymbol i\in\{0,\ldots,m\}^{\ell}}
  \left(\prod_{s=1}^{\ell}B_{i_s}^{m}(\alpha_s)\right)
  C^{(\boldsymbol c)}[\boldsymbol i].
\]
The bracketed \(\boldsymbol i\) is an \(\ell\)-component local label, never a
physical-node subscript.  Earlier dimensions vary more slowly in \code{cells},
\code{lbls}, \code{coeffs}, and rate-vertex traversal.  Cell-local spline and
gridding constructions for PD-LMIs motivate this architecture
\cite{masubuchi1998,xu2024}; Appendix~\ref{app:bernstein-math} develops the
underlying basis from degree-one interpolation to the general tensor case.

\section{Installation, Verification, And Lookup}
\label{sec:setup}

\subsection{Installation}
\label{sec:installation}

Start \matlab{} in the repository root, or set \code{projectRoot} to the
absolute path of the checked-out repository.  Add the package recursively, then
remove the documentation directory from the executable path.

\begin{lstlisting}[style=dpmatlab]
projectRoot = "path/to/Differential Parameter-Dependent Linear Matrix Inequality";
addpath(genpath(projectRoot));
rmpath(genpath(fullfile(projectRoot, "doc")));
\end{lstlisting}

For local work from the repository root, this package also resolves from:

\begin{lstlisting}[style=dpmatlab]
addpath(pwd)
\end{lstlisting}

\subsection{Verification}
\label{sec:verification}

Run the current MATLAB test entry point from the repository root:

\begin{lstlisting}[style=dpmatlab]
results = tests.run_all();
\end{lstlisting}

The test entry point covers helper utilities, \code{pdbase}, \code{pdmat},
\code{pdvar}, and ordinary \code{pdlmi} behavior.  The YALMIP-backed
\code{pdvar} and \code{pdlmi} tests require YALMIP.  Solver smoke tests
additionally require an SDP solver.

\subsection{Smoke Test}
\label{sec:quick-start}

First confirm that MATLAB resolves the four renamed constructors from this
repository and not from another toolbox:

\begin{lstlisting}[style=dpmatlab]
which pdbase -all
which pdmat -all
which pdvar -all
which pdlmi -all
\end{lstlisting}

Then exercise known-data inspection and the solver-facing assembly boundary:

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {1, 3}, Degree=1);
T = bernsteinTable(A, "oneLine");
disp(T)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
    CellSubscript      Expression
    _____________    _______________

        {[1]}        "(1-alpha)*1 + alpha*3"
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, {[0 1]}, "symmetric");
C = P >= 0;
F = toYalmip(C);
Cputinar = C.applyPutinar();
Fputinar = toYalmip(Cputinar);
Cbox = C.applyFullBoxPreorder();
Fbox = toYalmip(Cbox);
\end{lstlisting}

\code{F} is the direct coefficient certificate; \code{Fputinar} and
\code{Fbox} are the default Putinar and full-box certificates for the same
stored residual.  Any one can be passed to
ordinary YALMIP calls, for example:

\begin{lstlisting}[style=dpsyntax]
sol = optimize(F, objective, opts);
\end{lstlisting}

\subsection{Global Function Lookup}
\label{sec:global-lookup}

\begin{tabularx}{\textwidth}{@{}p{0.28\textwidth}Y@{}}
\toprule
Category & Entries \\
\midrule
Backend &
\hyperref[sec:pdbase]{\code{pdbase}},
\hyperref[sec:pdbase-constructor]{constructor},
\hyperref[sec:pdbase-inspection]{\code{cells}},
\hyperref[sec:pdbase-inspection]{\code{lbls}},
\hyperref[sec:pdbase-inspection]{\code{coeffs}},
\hyperref[sec:pdbase-counts]{\code{ncell}},
\hyperref[sec:pdbase-counts]{\code{ncoeff}},
\hyperref[sec:pdbase-counts]{\code{npar}},
\hyperref[sec:pdbase-counts]{\code{size}} \\
Known data &
\hyperref[sec:pdmat]{\code{pdmat}},
\hyperref[sec:pdmat-construction]{construction},
\hyperref[sec:pdmat-evaluate]{\code{evaluate}},
\hyperref[sec:pdmat-table]{\code{bernsteinTable}},
\hyperref[sec:pdmat-display]{\code{disp}/\code{display}},
\hyperref[sec:pdmat-plot]{\code{plot}},
\hyperref[sec:pdmat-algebra]{operators},
\hyperref[sec:pdmat-matrix-ops]{matrix methods} \\
Decision variables &
\hyperref[sec:pdvar]{\code{pdvar}},
\hyperref[sec:pdvar-construction]{construction},
\hyperref[sec:pdvar-rhodiff]{\code{rhodiff}},
\hyperref[sec:pdvar-algebra]{affine/mixed algebra},
\hyperref[sec:pdvar-matrix-ops]{matrix methods},
\hyperref[sec:pdvar-comparison]{comparisons} \\
LMIs &
\hyperref[sec:pdlmi]{\code{pdlmi}},
\hyperref[sec:pdlmi-construction]{construction},
  \hyperref[sec:pdlmi-direct]{direct coefficient assembly},
  \hyperref[sec:pdlmi-polya]{\code{applyPolya}},
  \hyperref[sec:pdlmi-putinar]{\code{applyPutinar}},
  \hyperref[sec:pdlmi-full-box]{\code{applyFullBoxPreorder}},
\hyperref[sec:pdlmi-toyalmip]{\code{toYalmip}},
\hyperref[sec:examples]{solver examples} \\
\bottomrule
\end{tabularx}

\section{\texorpdfstring{\code{pdbase}}{pdbase} Backend Reference}
\label{sec:pdbase}

\code{pdbase} is the developer-facing backend parent for \code{pdmat} and
\code{pdvar}.  Ordinary users should model known data with \code{pdmat} and
decision expressions with \code{pdvar}.  This section documents \code{pdbase}
because it explains the shared storage, traversal, and inspection contract used
by both public modeling classes.

\subsection{Lookup}
\label{sec:pdbase-lookup}

\begin{tabularx}{\textwidth}{@{}p{0.28\textwidth}Y@{}}
\toprule
Task & Function or field \\
\midrule
Create backend object &
\hyperref[sec:pdbase-constructor]{\code{pdbase}} \\
Inspect cells/labels &
\hyperref[sec:pdbase-inspection]{\code{cells}},
\hyperref[sec:pdbase-inspection]{\code{lbls}},
\hyperref[sec:pdbase-inspection]{\code{coeffs}} \\
Count sizes &
\hyperref[sec:pdbase-counts]{\code{ncell}},
\hyperref[sec:pdbase-counts]{\code{ncoeff}},
\hyperref[sec:pdbase-counts]{\code{npar}},
\hyperref[sec:pdbase-counts]{\code{size}} \\
Elevate stored coefficients &
\hyperref[sec:pdbase-elevvals]{\code{elevVals}} \\
Backend algebra mechanisms &
\hyperref[sec:pdbase-elevlocalvalues]{\code{elevLocalValues}},
\hyperref[sec:pdbase-bernelev]{\code{bernElev}},
\hyperref[sec:pdbase-bernprod]{\code{bernProd}},
\hyperref[sec:pdbase-mergegrid]{\code{mergeGrid}} \\
Read storage fields &
\hyperref[sec:pdbase-fields]{\code{GridInfo}},
\hyperref[sec:pdbase-fields]{\code{LocalValues}},
\hyperref[sec:pdbase-fields]{metadata fields} \\
\bottomrule
\end{tabularx}

\subsection{\texorpdfstring{\code{pdbase}}{pdbase} Constructor}
\label{sec:pdbase-constructor}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
obj = pdbase(gridVectors, matrixSize, degree)
obj = pdbase(gridVectors, matrixSize, degree, localValues)
obj = pdbase(..., IsContinuous=tf)
obj = pdbase(..., ContainsDecision=tf)
obj = pdbase(..., HasRateDependence=tf)
obj = pdbase(..., RateBounds=rb)
obj = pdbase(..., SourceSummary=txt)
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{gridVectors} is a cell vector with one strictly increasing node
        vector per parameter, such as \code{{[0 1 2], [10 20]}}.
  \item \code{matrixSize} is a positive two-element matrix size, such as
        \code{[2 2]}.
  \item \code{degree} is a nonnegative integer Bernstein degree, such as
        \code{1}.
  \item \code{localValues} is the nested physical-cell storage.  When omitted,
        \code{pdbase} creates a zero coefficient-backed object.
  \item \code{IsContinuous}, \code{ContainsDecision}, and
        \code{HasRateDependence} are logical metadata scalars.  They default
        to false; subclasses set them to describe their own payload contract.
  \item \code{RateBounds} is empty or an \(\ell\times2\) matrix of finite lower
        and upper rate bounds, such as \code{RateBounds=[-1 1; -2 2]}.
        Nonempty bounds also mark the object as rate-dependent.
  \item \code{SourceSummary} is inspectable origin text and defaults to
        \code{"coefficient-backed"}.
\end{itemize}

\examplehead
This backend example constructs a two-parameter parent object.  The object has
two physical cells because the first parameter has two intervals and the second
parameter has one interval.

\begin{lstlisting}[style=dpmatlab]
obj = pdbase({[0 1 2], [10 20]}, [2 2], 1);
class(obj)
obj.MatrixSize
obj.Degree
obj.ncell()
obj.ncoeff()
obj.npar()
size(obj)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
    'pdbase'

ans =
     2     2

ans =
     1

ans =
     2

ans =
     4

ans =
     2

ans =
     2     2
\end{lstlisting}

The printed \code{ncoeff} value is \((1+1)^2=4\), one local Bernstein
coefficient for each tensor-product label in the two parameter directions.

\refhead{Validation and errors}
Malformed grid containers and node vectors raise
\codeerr{pdbase}{InvalidGrid} and
\codeerr{pdbase}{Invalid}\allowbreak\code{GridVector}.
Invalid matrix sizes, degrees, or rate bounds raise
\codeerr{pdbase}{Invalid}\allowbreak\code{MatrixSize},
\codeerr{pdbase}{InvalidDegree}, and
\codeerr{pdbase}{Invalid}\allowbreak\code{RateBounds}.  The nested tree must contain exactly one
leaf per physical cell and \((m+1)^\ell\) coefficient columns per leaf;
violations raise
\codeerr{pdbase}{Invalid}\allowbreak\code{LocalValues} or
\codeerr{pdbase}{Invalid}\allowbreak\code{CoefficientCell}.  Payload matrices must have the
declared size and be finite, real, and either numeric or supported real YALMIP
expressions.  Malformed payloads raise
\codeerr{pdbase}{Invalid}\allowbreak\code{CoefficientPayload}.  Rate-affine payload structures
and \code{HasRateDependence=true} require compatible nonempty
\code{RateBounds}.

\subsection{Storage Fields}
\label{sec:pdbase-fields}

\begin{tabularx}{\textwidth}{@{}p{0.24\textwidth}Y@{}}
\toprule
Field & Meaning \\
\midrule
\code{GridInfo} & Grid vectors, node counts, and grid bounds. \\
\code{MatrixSize} & Size of each matrix coefficient or matrix expression. \\
\code{Degree} & Local Bernstein degree stored in every physical cell. \\
\code{LocalValues} & Nested cell tree indexed by physical-cell subscript; each leaf stores coefficient cells. \\
\code{IsContinuous} & Metadata flag; subclasses are responsible for boundary sharing if continuity is true. \\
\code{ContainsDecision} & True when coefficient payloads include YALMIP decision variables. \\
\code{HasRateDependence} & True when the object carries rate metadata or rate-vertex rows. \\
\code{RateBounds} & Parameter-rate lower and upper bounds used by \code{rhodiff} and \code{pdlmi}. \\
\code{SourceSummary} & Short origin label such as \code{coefficient-backed}, \code{decision}, or \code{derivative}. \\
\bottomrule
\end{tabularx}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[>=Latex,
      store/.style={draw=dpblue!80!black,rounded corners=2pt,fill=dpbluewash,
        align=center,font=\small,inner sep=4pt},
      leaf/.style={draw=black!55,rounded corners=2pt,fill=white,
        align=center,font=\scriptsize,inner sep=4pt}]
    \node[store] (root) at (0,0) {\texttt{LocalValues}};
    \node[store] (first) at (2.55,1.15) {first physical\\cell index \texttt{\{1\}}};
    \node[store] (second) at (2.55,-1.15) {first physical\\cell index \texttt{\{2\}}};
    \node[leaf] (leafone) at (6.0,1.15) {\texttt{LocalValues\{1\}\{1\}}\\[2pt]
      \(\{C^{(1,1)}[0,0],C^{(1,1)}[0,1],
             C^{(1,1)}[1,0],C^{(1,1)}[1,1]\}\)};
    \node[leaf] (leaftwo) at (6.0,-1.15) {\texttt{LocalValues\{2\}\{1\}}\\[2pt]
      \(\{C^{(2,1)}[0,0],C^{(2,1)}[0,1],
             C^{(2,1)}[1,0],C^{(2,1)}[1,1]\}\)};
    \draw[->,dpblue!75!black,thick] (root.east) -- (first.west);
    \draw[->,dpblue!75!black,thick] (root.east) -- (second.west);
    \draw[->,dpblue!75!black,thick] (first.east) -- (leafone.west);
    \draw[->,dpblue!75!black,thick] (second.east) -- (leaftwo.west);
    \node[align=center,font=\scriptsize] at (3.1,2.0)
      {second physical index is \texttt{\{1\}} in both leaves};
  \end{tikzpicture}
  \caption{Nested cell-local storage for a two-parameter, degree-one object
  on \code{{[0 1 2], [10 20]}}.  There are two physical cells, \([1,1]\)
  and \([2,1]\).  Each leaf is a flat local coefficient cell in \code{lbls}
  order: \([0,0]\), \([0,1]\), \([1,0]\), \([1,1]\).}
  \label{fig:localvalues-tree}
\end{figure}

The nesting follows physical-cell subscripts, not the Bernstein labels.  Start
with \code{LocalValues\{i1\}}, then take one cell index per remaining
parameter; this selects physical cell \([i_1,i_2,\ldots,i_\ell]\).  Only
after that selection does the leaf's flat cell array enumerate the
\((m+1)^\ell\) local Bernstein coefficients.
Thus \code{coeffs(obj, [2 1])} returns the lower leaf in
\cref{fig:localvalues-tree}, while \code{lbls(obj)} explains the order inside
that leaf.

\subsection{Inspection: \texorpdfstring{\code{cells}}{cells}, \texorpdfstring{\code{lbls}}{lbls}, And \texorpdfstring{\code{coeffs}}{coeffs}}
\label{sec:pdbase-inspection}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
subs = cells(obj)
subs = obj.cells()

labels = lbls(obj)
labels = obj.lbls()

c = coeffs(obj, cellSubs)
c = obj.coeffs(cellSubs)
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{obj} is a \code{pdbase} object or a \code{pdmat}/\code{pdvar}
        subclass instance using the shared cell-local storage convention.
  \item \code{cellSubs} selects one physical cell for \code{coeffs}.  Supply a
        numeric row vector such as \code{[2 1]} or a cell array of scalar
        subscripts such as \code{{2,1}}.
  \item \code{cells} returns physical hypercube subscripts in the shared
        traversal order; \code{lbls} returns local Bernstein labels in the
        shared coefficient order.
  \item \code{c} is the flat coefficient cell array for the selected physical
        cell, ordered consistently with \code{lbls(obj)}.  Rate-dependent
        leaves return one row per active rate vertex in the stored row order.
\end{itemize}

\refhead{Validation and errors}
\code{coeffs} requires one positive integer physical-cell index per parameter,
within the corresponding cell count.  Invalid numeric or cell-array selectors
raise \codeerr{pdbase}{InvalidCellSubs}.

\examplehead
The label order is shared by \code{pdmat}, \code{pdvar}, and \code{pdlmi}.  In
this two-parameter degree-one object, each physical cell stores four
coefficient entries with labels \([0,0]\), \([0,1]\), \([1,0]\), and
\([1,1]\).

\begin{lstlisting}[style=dpmatlab]
obj = pdbase({[0 1 2], [10 20]}, [1 1], 1);
lbls(obj)
cells(obj)
c = coeffs(obj, [2 1]);
numel(c)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
     0     0
     0     1
     1     0
     1     1

ans =
     1     1
     2     1

ans =
     4
\end{lstlisting}

\subsection{Counts And Shape}
\label{sec:pdbase-counts}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
n = ncell(obj)
n = obj.ncell()

n = ncoeff(obj)
n = obj.ncoeff()

n = npar(obj)
n = obj.npar()

sz = size(obj)
d = size(obj, dim)
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{obj} is a \code{pdbase} object or subclass instance.
  \item \code{dim} is a positive integer matrix dimension for \code{size}; it
        follows MATLAB's ordinary \code{size(obj,dim)} convention.
  \item \code{ncell} returns \(\prod_r(N_r-1)\), the number of physical
        hypercube cells.
  \item \code{ncoeff} returns \((m+1)^\ell\), the number of local Bernstein
        coefficients stored per physical cell.
  \item \code{npar} returns the parameter dimension \(\ell\).  \code{size}
        returns the matrix payload size, never the physical grid size.
\end{itemize}

\examplehead
For a two-parameter degree-one backend object, the first grid has two physical
intervals and the second has one.  The matrix payload is \(2\)-by-\(3\),
independently of the grid shape.

\begin{lstlisting}[style=dpmatlab]
obj = pdbase({[0 1 2], [10 20]}, [2 3], 1);
fprintf('ncell = %d\n', obj.ncell());
fprintf('ncoeff = %d\n', obj.ncoeff());
fprintf('npar = %d\n', obj.npar());
disp(size(obj))
disp(size(obj, 1))
disp(size(obj, 2))
disp(size(obj, 3))
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ncell = 2
ncoeff = 4
npar = 2
     2     3

     2

     3

     1
\end{lstlisting}

\subsubsection{\texorpdfstring{\code{elevVals}}{elevVals}}
\label{sec:pdbase-elevvals}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
vals = obj.elevVals(degreeIncrement)
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{degreeIncrement} is a finite nonnegative integer scalar.  It is
        added to \code{obj.Degree} in every parameter direction; zero returns
        coefficient evidence at the current degree.
\end{itemize}

\refhead{Output}
\code{vals} is a nested \code{LocalValues}-shaped cell tree.  Each physical
cell contains coefficients of degree \code{obj.Degree + degreeIncrement}, with
the represented polynomial, physical-cell tree, and any rate-row ordering
preserved.  The method does not modify \code{obj} or its stored coefficient
evidence.

\refhead{Validation and errors}
An invalid increment raises \code{pdbase:InvalidDegreeIncrement}.  A
function-only \code{pdmat} has no stored Bernstein coefficient evidence, so
this method raises \code{pdbase:MissingCoefficientEvidence}.

\textit{Relationship to backend and public algebra.}  Unlike protected
\code{bernElev}, which elevates one flat cell coefficient family for internal
alignment, \code{elevVals} is a public whole-object query returning elevated
\code{LocalValues}.  Public coefficient algebra uses \code{bernElev}
internally when compatible operands need degree alignment; it does not mutate
either operand.

\subsubsection{\texorpdfstring{\code{elevLocalValues}}{elevLocalValues}}
\label{sec:pdbase-elevlocalvalues}

\textit{Internal/protected mechanism.}  This method elevates a temporary
nested coefficient tree after public algebra has remapped operands to a common
physical grid.  It is documented as backend context, not as a supported user
call form.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
vals = elevLocalValues(obj, vals, fromDeg, toDeg)
vals = elevLocalValues(obj, vals, fromDeg, toDeg, grid)
\end{lstlisting}

Each leaf must be a cell array with one column per degree-\code{fromDeg}
Bernstein label.  The method elevates each rate-vertex row independently to
\code{toDeg}; it never mixes rows.  The optional \code{grid} supplies the
temporary physical-cell layout, and otherwise defaults to
\code{obj.GridInfo.Vectors}.  Equal source and target degrees return the input
tree unchanged.  A malformed leaf raises
\code{pdbase:InvalidCoefficientCell}.  Public users encounter this behavior
through grid merging, assignment, concatenation, and arithmetic rather than by
calling the protected method directly.

\subsubsection{\texorpdfstring{\code{bernElev}}{bernElev}}
\label{sec:pdbase-bernelev}

\textit{Internal/protected mechanism.}  This signature documents the backend
algorithm used by public algebra; it is not a supported user call form.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
out = bernElev(obj, coeffs, fromDeg, toDeg)
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{coeffs} is a flat coefficient-cell family in the same label
        order as \code{lbls(obj)}.
  \item \code{fromDeg} and \code{toDeg} are nonnegative integer degrees, with
        \code{toDeg >= fromDeg}.
  \item \code{out} contains \((\code{toDeg}+1)^\ell\) elevated coefficients
        in the same label order.
\end{itemize}

\examplehead
\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {1, 2}, Degree=1);
B = pdmat({[0 1]}, {1, 2, 3}, Degree=2);
C = A + B;
C.Degree
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
     2
\end{lstlisting}

The example invokes the protected helper through public addition; users do not
call \code{bernElev} directly.  Invalid degree values raise
\codeerr{pdbase}{InvalidDegree}; a target below the source degree raises
\codeerr{pdbase}{InvalidDegreeElevation}; a coefficient count inconsistent
with the source degree and parameter dimension raises
\codeerr{pdbase}{InvalidCoefficientCell}.

\subsubsection{\texorpdfstring{\code{bernProd}}{bernProd}}
\label{sec:pdbase-bernprod}

\textit{Internal/protected mechanism.}  This signature documents the backend
algorithm used by public matrix multiplication; it is not a supported user
call form.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
out = bernProd(obj, lhs, lhsDeg, rhs, rhsDeg)
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{lhs} and \code{rhs} are flat coefficient-cell families whose
        matrix payload inner dimensions are compatible for multiplication.
  \item \code{lhsDeg} and \code{rhsDeg} are their nonnegative local Bernstein
        degrees.
  \item \code{out} is the binomial-normalized coefficient convolution at
        degree \code{lhsDeg + rhsDeg}; payload products follow MATLAB matrix
        multiplication rules.
\end{itemize}

For one parameter, the protected helper implements
\[
  C[k]=\sum_{i+j=k}
  \frac{\binom mi\binom nj}{\binom{m+n}{k}}\,A[i]B[j].
\]
It enumerates ordered payload products, groups them by the anti-diagonal
\(i+j=k\), and only then applies the binomial normalization.  For tensor
labels the same rule is applied componentwise.  See the public
\code{pdmat} multiplication entry in \cref{sec:pdmat-operator-entries}, the
\code{pdvar} known-data boundary in \cref{sec:pdvar-algebra}, and the full
derivation in Appendix~\ref{app:bernstein-math}.

\examplehead
\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {1, 2}, Degree=1);
P = A * A;
c = P.coeffs(1);
fprintf("degree=%d; coefficients=", P.Degree)
disp([c{:}])
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
degree=2; coefficients=     1     2     4
\end{lstlisting}

The example invokes the protected helper through public matrix multiplication;
users do not call \code{bernProd} directly.  Even for scalar data, the middle
coefficient comes from two ordered contributions rather than from one sample.

\subsubsection{\texorpdfstring{\code{mergeGrid}}{mergeGrid}}
\label{sec:pdbase-mergegrid}

\textit{Internal/protected mechanism.}  This signature documents the backend
grid-refinement algorithm used by public algebra; it is not a supported user
call form.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
grid = obj.mergeGrid(errId, other1, other2, ...)
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{errId} is the error identifier used when parameter counts or
        physical bounds are incompatible.
  \item \code{other1, other2, ...} are coefficient-backed operands, including
        function-backed operands constructed with an explicit \code{Degree}.
        They must have equal parameter counts and matching first and last nodes
        on every parameter axis.
  \item \code{grid} has the sorted union of the interior nodes.  Public
        algebra re-expresses operands on these cells before degree elevation,
        coefficient-wise addition, or product convolution.
\end{itemize}
This protected helper is invoked by public algebra methods rather than called
directly by users.

\examplehead
\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {1, 2}, Degree=1);
B = pdmat({[0 0.5 1]}, {10, 20, 30}, Degree=1);
C = A + B;
C.GridInfo.Vectors{1}
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
         0    0.5000    1.0000
\end{lstlisting}

The example invokes the protected helper through public addition; users do not
call \code{mergeGrid} directly.

\subsection{See Also}
\code{pdmat}, \code{pdvar}, \code{cells}, \code{coeffs}, \code{lbls},
\code{ncell}, \code{ncoeff}, \code{npar}, \code{size}, \code{elevVals},
\code{elevLocalValues}, \code{bernElev}, \code{bernProd}, \code{mergeGrid}.

\section{\texorpdfstring{\code{pdmat}}{pdmat} Reference}
\label{sec:pdmat}

\code{pdmat} stores known finite real matrix data on a tensor grid.  Objects
can be coefficient-backed, function-only, or function-backed with explicit
Bernstein coefficient evidence.  Coefficient-backed objects participate in
algebra.  Function-only objects evaluate exactly through the stored handle, but
do not expose coefficient evidence for \code{bernsteinTable} or coefficient algebra.

\subsection{Lookup}
\label{sec:pdmat-lookup}

\begin{tabularx}{\textwidth}{@{}p{0.28\textwidth}Y@{}}
\toprule
Task & Entry \\
\midrule
Construct data &
\hyperref[sec:pdmat-construction]{\code{pdmat}} \\
Evaluate &
\hyperref[sec:pdmat-evaluate]{\code{evaluate}} \\
Inspect/display &
\hyperref[sec:pdmat-table]{\code{bernsteinTable}},
\hyperref[sec:pdmat-display]{\code{disp}},
\hyperref[sec:pdmat-display]{\code{display}} \\
Plot &
\hyperref[sec:pdmat-plot]{\code{plot}} \\
Arithmetic &
\hyperref[sec:pdmat-algebra]{\code{+}},
\hyperref[sec:pdmat-algebra]{\code{-}},
\hyperref[sec:pdmat-algebra]{unary \code{+}/\code{-}},
\hyperref[sec:pdmat-algebra]{\code{*}} \\
Matrix/index methods &
\hyperref[sec:pdmat-matrix-ops]{shape and structural methods},
\hyperref[sec:pdmat-indexing]{indexing and assignment} \\
\bottomrule
\end{tabularx}

\subsection{Construction}
\label{sec:pdmat-construction}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
A = pdmat(gridVector, source)
A = pdmat(gridVector, source, Degree=m)
A = pdmat(gridVectors, source)
A = pdmat(gridVectors, source, Degree=m)
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{gridVector} is a numeric vector used as a one-parameter grid
        shorthand, such as \code{[0 1 2]}.
  \item \code{gridVectors} is a cell vector with one numeric grid vector per
        parameter, such as \code{{[0 1], [10 20]}}.
  \item \code{source} may be a function handle, a global cell grid of numeric
        Bernstein coefficients, or explicit nested \code{LocalValues}; for
        example, \code{{1, 2, 3}} is scalar degree-two data on one cell.
  \item \code{Degree=m} sets the local Bernstein degree.  For function handles,
        omitting \code{Degree} creates a function-only object; passing a value
        such as \code{Degree=2} validates and stores Bernstein coefficient
         evidence while retaining the exact function handle for \code{evaluate}.
\end{itemize}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=5mm and 10mm,
  src/.style={draw=black!55,fill=white,rounded corners=2pt,font=\scriptsize,
    align=center,minimum width=34mm,minimum height=9mm},
  result/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=39mm,minimum height=12mm}]
  \node[src] (f) {function handle\\exact evaluator};
  \node[src,below=of f] (g) {global coefficient grid\\shared physical nodes};
  \node[src,below=of g] (l) {nested \code{LocalValues}\\cell-local coefficients};
  \node[result,right=of g] (a) {\code{pdmat}\\grid + matrix size + degree\\
    optional coefficient evidence};
  \draw[->,thick,dpblue] (f.east) -- (a.west);
  \draw[->,thick,dpblue] (g.east) -- (a.west);
  \draw[->,thick,dpblue] (l.east) -- (a.west);
\end{tikzpicture}
\end{center}

\examplehead
The scalar-grid shorthand is the shortest coefficient-backed form.  The three
numeric cells below are the global Bernstein coefficients for two physical
cells with degree one.

\begin{lstlisting}[style=dpmatlab]
A = pdmat([0 1 2], {10, 20, 30}, Degree=1)
size(A)
A.Degree
A.SourceSummary
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
A =
  pdmat [1 1] over 1-D grid, degree 1, source coefficient-backed

ans =
     1     1

ans =
     1

ans =
    "coefficient-backed"
\end{lstlisting}

For tensor grids, pass one grid vector per parameter.  The source cell array
matches the global coefficient grid implied by the two parameter directions.

\begin{lstlisting}[style=dpmatlab]
B = pdmat({[0 1], [10 20]}, {1 2; 3 4}, Degree=1)
B.GridInfo.Vectors{2}
B.lbls()
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
B =
  pdmat [1 1] over 2-D grid, degree 1, source coefficient-backed

ans =
    10    20

ans =
     0     0
     0     1
     1     0
     1     1
\end{lstlisting}

Use explicit nested \code{LocalValues} when each physical cell must be written
directly.  Here each cell stores two \(1\times2\) row-vector coefficients.

\begin{lstlisting}[style=dpmatlab]
localVals = {{[1 0], [0 1]}, {[2 0], [0 2]}};
C = pdmat({[0 1 2]}, localVals, Degree=1)
size(C)
C.coeffs(2)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
C =
  pdmat [1 2] over 1-D grid, degree 1, source coefficient-backed

ans =
     1     2

ans =
  1x2 cell array
    {[2 0]}    {[0 2]}
\end{lstlisting}

When \code{source} is a function handle and \code{Degree} is omitted, the
object keeps the exact evaluator but does not claim coefficient evidence.

\begin{lstlisting}[style=dpmatlab]
F = pdmat({[0 1]}, @(rho) [rho rho^2])
F.SourceSummary
F.evaluate(0.5)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
F =
  pdmat [1 2] over 1-D grid, degree 1, source function

ans =
    "function"

ans =
    0.5000    0.2500
\end{lstlisting}

Add \code{Degree} for a polynomial function when later coefficient inspection
or algebra should use Bernstein evidence.  The exact function handle is still
used for point evaluation.

\begin{lstlisting}[style=dpmatlab]
G = pdmat({[0 1]}, @(rho) rho.^2, Degree=2)
G.SourceSummary
G.coeffs(1)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
G =
  pdmat [1 1] over 1-D grid, degree 2, source function-bernstein

ans =
    "function-bernstein"

ans =
  1x3 cell array
    {[0]}    {[0]}    {[1]}
\end{lstlisting}

These forms all create finite known-data objects, but only the coefficient-
backed and function-Bernstein forms can participate in coefficient algebra.

\refhead{Public property}
\code{FunctionHandle} retains the supplied function handle for function-only
and function-Bernstein sources; it is empty for cell-backed sources.  Its set
access is private.

\refhead{Validation and errors}
Constructor options must be name--value pairs.  Malformed syntax raises
\codeerr{pdmat}{InvalidOptions}; backend-only options raise
\codeerr{pdmat}{Unsupported}\allowbreak\code{Option}; unknown names raise
\codeerr{pdmat}{Unknown}\allowbreak\code{Option}.  Grid errors use
\codeerr{pdmat}{InvalidGrid} or
\codeerr{pdmat}{Invalid}\allowbreak\code{GridVector}.

An unsupported source kind raises \codeerr{pdmat}{InvalidSource}.  Function
arity or initial-call failures raise
\codeerr{pdmat}{Invalid}\allowbreak\code{FunctionHandle}; empty, complex, or
otherwise invalid initial outputs raise
\codeerr{pdmat}{Invalid}\allowbreak\code{FunctionOutput}.  Global coefficient
grids with inconsistent finite-real payload sizes raise
\codeerr{pdmat}{InvalidData}.  Incompatible inferred or explicit degrees raise
\codeerr{pdmat}{InvalidDegree}; malformed nested cell storage raises
\codeerr{pdmat}{Invalid}\allowbreak\code{LocalValues}.  Malformed explicit
coefficient-leaf counts or shapes raise
\codeerr{pdmat}{Invalid}\allowbreak\code{CoefficientCell}; invalid, nonfinite,
nonreal, or wrong-size coefficient payloads raise
\codeerr{pdmat}{Invalid}\allowbreak\code{CoefficientPayload}.  With an explicit
\code{Degree}, a function that is not representable at that degree raises
\codeerr{pdmat}{NonBernstein}\allowbreak\code{Polynomial}; a failed or
size-inconsistent validation probe raises
\codeerr{pdmat}{Invalid}\allowbreak\code{FunctionValue}.  Nested local data
with mismatched shared faces is retained as discontinuous data and emits
warning \codeerr{pdmat}{Discontinuous}\allowbreak\code{LocalValues}.

\subsection{\texorpdfstring{\code{evaluate}}{evaluate}}
\label{sec:pdmat-evaluate}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
val = evaluate(A, point)
val = A.evaluate(point)
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{A} is a \code{pdmat} object.
  \item \code{point} is a finite real scalar for a one-parameter object, such
        as \code{0.25}, or a finite real row vector with one entry per
        parameter, such as \code{[0.25 12]}.  Every entry must lie within the
        corresponding grid bounds.
  \item \code{val} is a numeric matrix with \code{size(A)}.  Coefficient-backed
        objects use local Bernstein interpolation; function-backed objects call
        their stored function handle.
\end{itemize}

\refhead{Validation and errors}
A point with the wrong type, length, or finite-real shape raises
\codeerr{pdmat}{InvalidPoint}; a point outside any grid bound raises
\codeerr{pdmat}{PointOutOfBounds}.  If a stored function fails or returns a
nonfinite, complex, empty, or size-inconsistent matrix at the requested point,
\code{evaluate} raises
\codeerr{pdmat}{Invalid}\allowbreak\code{FunctionValue}.

For a degree-one scalar coefficient-backed object on \([\rho_0,\rho_1]\),
\[
  A(\rho) = (1-\alpha)A^{(c)}[0]+\alpha A^{(c)}[1],\qquad
  \alpha = \frac{\rho-\rho_0}{\rho_1-\rho_0}.
\]
The same local-coordinate rule is applied entrywise for matrix-valued data.

\begin{center}
\begin{tikzpicture}[x=5.2cm,y=1cm,>=Latex]
  \draw[very thick,black!55] (0,0) -- (1,0);
  \filldraw[fill=white] (0,0) circle (1.7pt)
    node[above=3pt,font=\scriptsize] {\(A^{(c)}[0]=2\)};
  \filldraw[fill=white] (1,0) circle (1.7pt)
    node[above=3pt,font=\scriptsize] {\(A^{(c)}[1]=4\)};
  \filldraw[fill=dpblue] (0.25,0) circle (1.8pt)
    node[below=4pt,font=\scriptsize] {\(\alpha=0.25\)};
  \draw[->,dpblue,thick] (0.25,-0.32) -- (0.25,-0.05);
  \node[font=\scriptsize,align=center] at (0.5,-0.72)
    {\(A=0.75A^{(c)}[0]+0.25A^{(c)}[1]=2.5\)\\
     local interpolation, not nearest-node sampling};
\end{tikzpicture}
\end{center}

\examplehead
Use the function-call form when the object is naturally one input among several
MATLAB expressions.

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {2, 4}, Degree=1);
val = evaluate(A, 0.25)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
val =
    2.5000
\end{lstlisting}

At \(\rho=0.25\), the public local coordinate is \(\alpha=0.25\), so the
interpolated value is \(0.75\cdot2+0.25\cdot4=2.5\).

The dot-call form is equivalent and can be convenient in command-window
exploration.

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {2, 4}, Degree=1);
val = A.evaluate(0.75)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
val =
    3.5000
\end{lstlisting}

Function-backed objects route through the retained function handle, so
non-polynomial expressions can still be evaluated exactly at a point.

\begin{lstlisting}[style=dpmatlab]
F = pdmat({[0 pi]}, @(rho) sin(rho));
val = F.evaluate(pi/2)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
val =
     1
\end{lstlisting}

\subsection{Visualization And Inspection}
\label{sec:pdmat-visualization}

\subsubsection{\texorpdfstring{\code{bernsteinTable}}{bernsteinTable}}
\label{sec:pdmat-table}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
T = bernsteinTable(A)
T = bernsteinTable(A, cellSubs)
T = bernsteinTable(A, "oneLine")
T = bernsteinTable(A, cellSubs, "oneLine")
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{cellSubs} selects one physical cell.
  \item \code{"oneLine"} returns one row per selected physical cell with a
        compact Bernstein expression.
  \item Without \code{"oneLine"}, the full table uses seven metadata columns;
        the output below shows their exact MATLAB names.
\end{itemize}

For one parameter, the \code{Basis} column reports
\[
  \binom{m}{j}(1-\alpha)^{m-j}\alpha^j.
\]
For multiple parameters, the basis is the tensor product of that expression in
each local coordinate, using \code{alpha1}, \code{alpha2}, and so on.
\code{LocalIndex} is the local Bernstein label.
\code{CoeffSubscript} maps the local label back to the global coefficient
subscript on the physical grid.  \code{IsPhysicalNode} is true when that
coefficient lies on a physical grid node.

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=7mm and 9mm,
  box/.style={draw=black!55,fill=white,rounded corners=2pt,font=\scriptsize,
    align=center,minimum width=31mm,minimum height=10mm},
  result/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=36mm,minimum height=10mm}]
  \node[box] (cell) {physical cell selector\\\code{cellSubs}};
  \node[box,below=of cell] (labels) {local labels\\\code{lbls(A)}};
  \node[result,right=of cell,yshift=-7mm] (table) {\code{bernsteinTable}\\
    cell + label + basis + value};
  \draw[->,thick,dpblue] (cell.east) -- (table.west);
  \draw[->,thick,dpblue] (labels.east) -- (table.west);
\end{tikzpicture}

\textit{The table reports stored coefficient evidence.  It does not sample a
function and does not create solver constraints.}
\end{center}

\examplehead
\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {[0 1], [1 2]}, Degree=1);
T = bernsteinTable(A, 1, "oneLine");
disp(T)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
    CellSubscript          Expression
    _____________    _______________________

        {[1]}        "(1-alpha)*[0 1] + alpha*[1 2]"
\end{lstlisting}

Use the full table when the metadata columns are the point of the example:

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {1, 2, 3}, Degree=2);
T = bernsteinTable(A);
disp(T)
\end{lstlisting}

\begin{lstlisting}[style=dpwideoutput]
    TermIndex    CellSubscript    CoeffSubscript    LocalIndex      Basis      IsPhysicalNode    Value
    _________    _____________    ______________    __________    _________    ______________    _____

        1            {[1]}            {[1]}           {[0]}       "(1-alpha)^2"       true          {[1]}
        2            {[1]}            {[2]}           {[1]}       "2(1-alpha)alpha"   false         {[2]}
        3            {[1]}            {[3]}           {[2]}       "alpha^2"           true          {[3]}
\end{lstlisting}

\refhead{Validation and errors}
A function-only object has no coefficient evidence and raises
\codeerr{pdmat}{FunctionOnly}\allowbreak\code{BernsteinTable}.  Unknown text
options, repeated selectors, or invalid physical-cell selectors raise
\codeerr{pdmat}{Invalid}\allowbreak\code{BernsteinTableInput}.

\subsubsection{\texorpdfstring{\code{disp}}{disp} And \texorpdfstring{\code{display}}{display}}
\label{sec:pdmat-display}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
disp(A)
display(A)
A
\end{lstlisting}

\arghead
\code{disp(A)} prints a compact one-line summary.  \code{display(A)} is the
command-window display used when an expression is not terminated by a semicolon;
it prints grid, degree, coefficient, and source metadata.

\examplehead
\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {1, 2}, Degree=1);
disp(A)
display(A)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
pdmat [1 1] over 1-D grid, degree 1, source coefficient-backed
A =
  pdmat with 1-by-1 matrix values
  Parameters: 1
    rho_1: [0, 1], 2 nodes
  Degree: 1
  Physical cells: 1
  Coefficients per cell: 2
  Continuous: true
  Source: coefficient-backed
  Function handle: false
\end{lstlisting}

\subsubsection{\texorpdfstring{\code{plot}}{plot}}
\label{sec:pdmat-plot}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
h = plot(A)
h = plot(A, dims)
h = plot(A, SamplesPerCell=n)
h = plot(A, dims, SamplesPerCell=n)
h = plot(A, ..., Name, Value)
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{dims} is one or two parameter indices.  For one-parameter
        objects, the default is \code{1}; otherwise the default is
        \code{[1 2]}, as in \code{plot(A, [1 2])}.
  \item \code{SamplesPerCell} is the package-owned sampling option.  The default
        is \code{15}; use \code{SamplesPerCell=4} for a coarse diagnostic plot.
  \item Remaining name-value pairs pass through to MATLAB graphics.  One-
        dimensional plots pass them to \code{plot}; two-dimensional plots pass
        them to \code{surf}.  Examples include \code{LineWidth},
        \code{FaceAlpha}, and \code{EdgeColor}.
  \item The output \code{h} is the vector of graphics handles, one entry per
        matrix element.
\end{itemize}

\refhead{Validation and errors}
An invalid parameter index, a repeated dimension, or a request for more than
two plotting dimensions raises
\codeerr{pdmat}{Invalid}\allowbreak\code{PlotDimensions}.  Malformed plotting
options or a nonpositive integer \code{SamplesPerCell} raise
\codeerr{pdmat}{Invalid}\allowbreak\code{PlotOptions}.  Errors from MATLAB
graphics options remain MATLAB graphics errors.

\examplehead
The one-dimensional call returns one graphics handle per matrix entry.  The
example inspects the generated sample count and line styling without relying on
an interactive figure window.

\begin{lstlisting}[style=dpmatlab]
fig = figure('Visible','off');
A = pdmat({[0 1]}, @(rho) [rho, rho^2]);
h = plot(A, SamplesPerCell=4, LineWidth=2);
numel(h)
numel(h(1).XData)
h(1).LineWidth
h(1).YData([1 end])
close(fig)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
     2

ans =
     5

ans =
     2

ans =
     0     1
\end{lstlisting}

With four samples per cell on one physical interval, MATLAB stores five plotted
node values including both endpoints.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{matlab-figures/pdmat-plot-1d.png}
  \caption{One-dimensional \code{pdmat/plot} output generated from MATLAB.}
  \label{fig:pdmat-plot-1d}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{matlab-figures/pdmat-plot-2d.png}
  \caption{Two-dimensional \code{pdmat/plot} surface output generated from MATLAB.}
  \label{fig:pdmat-plot-2d}
\end{figure}

\subsection{Algebra And Matrix Operations}
\label{sec:pdmat-algebra}

A \code{pdmat} operation acts on complete matrix coefficients while the
physical grid remains attached to the result.  The blocks below deliberately
show one operation family at a time: purpose, supported syntax, input, immediate
output, and---when the result is spatial---a before/after picture.

\subsubsection{Signs, Addition, And Subtraction}

Use signs, sums, and differences to build known matrix expressions.  Compatible
physical bounds are refined to the union grid, and lower-degree operands are
elevated exactly before matching coefficients are combined.  The corresponding
MATLAB overload names are \code{plus}, \code{minus}, \code{uplus}, and
\code{uminus}.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
S = A + B
S = A + M
S = M + A
D = A - B
D = A - M
D = M - A
P = +A
N = -A
\end{lstlisting}

Here \code{M} is a finite real scalar or size-compatible matrix.  A scalar
broadcasts to the matrix payload size.  The first example makes the degree
alignment visible.

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {1, 2}, Degree=1);
B = pdmat({[0 1]}, {10, 20, 30}, Degree=2);
S = A + B;
cS = S.coeffs(1);
fprintf("sum degree=%d\n", S.Degree);
disp([cS{:}])
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
sum degree=2
   11.0000   21.5000   32.0000
\end{lstlisting}

The middle coefficient of the elevated \(A\) is
\((1+2)/2=1.5\), so the middle sum is \(1.5+20=21.5\).
Subtraction and unary signs act immediately on the same local coefficients.

\begin{lstlisting}[style=dpmatlab]
D = 5 - A;
N = -A;
cD = D.coeffs(1);
fprintf("5-A coefficients: "); disp([cD{:}])
fprintf("-A at rho=1: %.0f\n", N.evaluate(1));
fprintf("class(+A) = %s\n", class(+A));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
5-A coefficients:      4     3
-A at rho=1: -2
class(+A) = pdmat
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=7mm and 10mm,
  box/.style={draw=black!55,rounded corners=2pt,fill=white,font=\scriptsize,
    minimum width=31mm,minimum height=8mm},
  result/.style={draw=dpblue!75!black,rounded corners=2pt,fill=dpbluewash,
    font=\scriptsize,align=center,minimum width=35mm,minimum height=8mm}]
  \node[box] (a) {degree 1: \([1,2]\)};
  \node[box,below=of a] (b) {degree 2: \([10,20,30]\)};
  \node[result,right=of a,yshift=-7mm] (s) {elevate, then add:\\
    \([11,21.5,32]\)};
  \draw[->,thick,dpblue] (a.east) -- (s.west);
  \draw[->,thick,dpblue] (b.east) -- (s.west);
\end{tikzpicture}
\end{center}

When grids have the same outer bounds but different interior nodes, the union
grid is used before addition or subtraction.  For example,
\code{pdmat({[0 1]},...)+pdmat({[0 .5 1]},...)} has grid
\code{[0 .5 1]}; each operand is represented exactly on both resulting cells.

\subsubsection{Ordered Matrix Multiplication}
\label{sec:pdmat-operator-entries}

Use \code{mtimes} for a matrix product.  The inner payload dimensions must
match.  Two parameter-dependent factors produce the degree sum; a numeric
factor is constant and does not raise the degree.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
K = A * B
K = A * M
K = M * A
\end{lstlisting}

For a fixed physical cell (c), let
\(A=\sum_iB_i^mA^{(c)}[i]\) and
\(D=\sum_jB_j^nD^{(c)}[j]\).  Then (K=AD) has coefficients
\[
  K^{(c)}[k]=\sum_{i+j=k}
  \frac{\binom mi\binom nj}{\binom{m+n}{k}}\,
  A^{(c)}[i]D^{(c)}[j].
\]
The left/right matrix order is never exchanged.

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {1, 2}, Degree=1);
B = pdmat({[0 1]}, {10, 20, 30}, Degree=2);
K = A * B;
cK = K.coeffs(1);
fprintf("product degree=%d\n", K.Degree);
disp([cK{:}])
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
product degree=3
   10.0000   20.0000   36.6667   60.0000
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[node distance=11mm,
  panel/.style={minimum width=42mm,minimum height=32mm,font=\scriptsize,
    align=left,inner sep=5pt}]
  \node[panel,dp/neutral] (input) {\textbf{1. Inputs}\\[2pt]
    \(A:[A_0,A_1]\)\\
    \(D:[D_0,D_1,D_2]\)};
  \node[panel,dp/contribution,right=of input] (groups) {
    \textbf{2. Anti-diagonals}\\[2pt]
    \(k=0:(0,0)\)\\
    \(k=1:(0,1),(1,0)\)\\
    \(k=2:(0,2),(1,1)\)\\
    \(k=3:(1,2)\)};
  \node[panel,dp/result,right=of groups] (output) {
    \textbf{3. Weighted sums}\\[2pt]
    \(K[0]=10\)\\
    \(K[1]=20\)\\
    \(K[2]=110/3\)\\
    \(K[3]=60\)};
  \draw[dp/arrow] (input)--node[above,font=\tiny]{group \(i+j\)}(groups);
  \draw[dp/arrow] (groups)--node[above,font=\tiny]{normalize}(output);
\end{tikzpicture}

\textit{Coefficient labels add under the weighted convolution; payload
matrices multiply in the written order.  The three panels make the convolution
readable without crossing contribution arrows.}
\end{center}

For tensor data, label addition and binomial weights are applied independently
in each parameter direction.  Two degree-one factors in two parameters produce
degree two and \(3^2=9\) coefficients per physical cell.

\refhead{Validation and errors}
Incompatible operands for \code{+}, \code{-}, and \code{*} raise
\codeerr{pdmat}{InvalidAddition},
\codeerr{pdmat}{InvalidSubtraction}, or
\codeerr{pdmat}{InvalidMultiplication}.  Parameter counts or outer bounds that
cannot be merged raise \codeerr{pdmat}{MixedGrid}.  Coefficient algebra on a
  function-only object raises \code{pdmat:}\allowbreak\code{FunctionOnly}%
  \allowbreak\code{Algebra}; construct the
function with an explicit verified \code{Degree} when coefficient evidence is
required.

\subsection{Matrix Methods}
\label{sec:pdmat-matrix-ops}

These methods transform every local matrix coefficient in the same way.  They
do not transpose, reshape, or concatenate the physical grid.  Except for the
documented evaluator/display/shape paths, structural methods require
coefficient evidence.

\subsubsection{Shape, Equality, And Display Protocols}
\label{sec:pdmat-matrix-shape}
\label{sec:pdmat-matrix-protocol}

Shape queries describe the matrix payload, not the grid.  Equality compares
coefficient-backed functions after compatible grid refinement and degree
elevation.  The protocol methods support ordinary MATLAB indexing syntax and
dot access; they are not a second storage API.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
sz = size(A)
d = size(A, dim)
n = height(A)
n = width(A)
n = length(A)
n = numel(A)
n = ndims(A)
tf = isequal(A, B, ...)
disp(A)
display(A)
idx = end(A, k, n)
n = numArgumentsFromSubscript(A, S, ctx)
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, ...
    {[1 2 3; 4 5 6], [10 20 30; 40 50 60]}, Degree=1);
fprintf("size=%d-by-%d, height=%d, width=%d\n", ...
    size(A,1), size(A,2), height(A), width(A));
fprintf("length=%d, numel=%d, ndims=%d, equal(+A)=%d\n", ...
    length(A), numel(A), ndims(A), isequal(A,+A));
disp(A)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
size=2-by-3, height=2, width=3
length=3, numel=6, ndims=2, equal(+A)=1
pdmat [2 3] over 1-D grid, degree 1, source coefficient-backed
\end{lstlisting}

A function-only object can use shape queries and compares its stored metadata
and function handle.  A coefficient-backed equality that cannot form a common
comparison grid raises \codeerr{pdmat}{InvalidEquality}.  The detailed
multi-line \code{display} transcript is shown in
\cref{sec:pdmat-display}.

\subsubsection{Transpose And Conjugate Transpose}
\label{sec:pdmat-matrix-transform}

Both transpose forms act on every coefficient.  Because \code{pdmat} payloads
are finite and real, conjugation does not change their values.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
T = transpose(A)
H = ctranspose(A)
T = A.'
H = A'
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, ...
    {[1 2 3; 4 5 6], [10 20 30; 40 50 60]}, Degree=1);
T = A.';
H = A';
fprintf("transpose size=%d-by-%d, equal forms=%d\n", ...
    size(T,1), size(T,2), isequal(T,H));
disp(T.evaluate(0))
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
transpose size=3-by-2, equal forms=1
     1     4
     2     5
     3     6
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=12mm,
  a/.style={matrix of math nodes,nodes={draw=black!45,minimum size=6mm,
    font=\scriptsize},column sep=-\pgflinewidth,row sep=-\pgflinewidth},
  lab/.style={font=\scriptsize,align=center}]
  \matrix[a] (m) {1&2&3\\4&5&6\\};
  \matrix[a,right=of m] (t) {1&4\\2&5\\3&6\\};
  \draw[->,thick,dpblue] (m.east) -- node[above,lab]{transpose each\\coefficient} (t.west);
  \node[below=3pt of m,lab]{\(2\times3\)};
  \node[below=3pt of t,lab]{\(3\times2\)};
\end{tikzpicture}
\end{center}

\subsubsection{Vectorization, Reshape, And Squeeze}

Use these methods to change the matrix view while preserving column-major
element order, the parameter grid, the degree, and every coefficient value.
\code{reshape} accepts exactly two positive dimensions with at most one
inferred \code{[]} dimension.  \code{squeeze} retains the package's
two-dimensional matrix convention.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
V = vec(A)
R = reshape(A, [m n])
R = reshape(A, m, n)
S = squeeze(A)
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, ...
    {[1 2 3; 4 5 6], [10 20 30; 40 50 60]}, Degree=1);
V = vec(A);
R = reshape(A, [3 2]);
S = squeeze(A);
fprintf("vec=%d-by-%d, reshape=%d-by-%d, squeeze=%d-by-%d\n", ...
    size(V,1), size(V,2), size(R,1), size(R,2), size(S,1), size(S,2));
v0 = V.evaluate(0);
disp(v0.')
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
vec=6-by-1, reshape=3-by-2, squeeze=2-by-3
     1     4     2     5     3     6
\end{lstlisting}

Malformed or element-count-changing reshape requests raise
\codeerr{pdmat}{InvalidReshape}.

\subsubsection{Diagonals And Trace}

\code{diag} extracts a selected diagonal from a matrix, or builds a square
diagonal matrix from a vector.  A finite integer offset \code{k} may not select
an empty diagonal.  \code{trace} returns a \(1\)-by-\(1\) \code{pdmat}.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
d = diag(A)
d = diag(A, k)
D = diag(v)
D = diag(v, k)
t = trace(A)
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {[1 2; 3 4], [10 20; 30 40]}, Degree=1);
d = diag(A);
D = diag(d);
t = trace(A);
disp(d.evaluate(0))
disp(D.evaluate(0))
fprintf("trace at rho=0: %.0f\n", t.evaluate(0));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
     1
     4

     1     0
     0     4
trace at rho=0: 5
\end{lstlisting}

Invalid offsets or empty selections raise \codeerr{pdmat}{InvalidDiag}.

\subsubsection{Triangular Parts And Reductions}
\label{sec:pdmat-matrix-reductions}

Triangular extraction preserves or zeros entries relative to a selected
diagonal.  Reductions act along matrix dimensions; \code{"all"} is supported by
\code{sum} and \code{mean}.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
L = tril(A)
L = tril(A, k)
U = triu(A)
U = triu(A, k)
S = sum(A)
S = sum(A, dim)
S = sum(A, "all")
M = mean(A)
M = mean(A, dim)
M = mean(A, "all")
C = cumsum(A)
C = cumsum(A, dim)
\end{lstlisting}

First inspect the two triangular results.

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {[1 2; 3 4], [10 20; 30 40]}, Degree=1);
L = tril(A);
U = triu(A);
disp(L.evaluate(0))
disp(U.evaluate(0))
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
     1     0
     3     4

     1     2
     0     4
\end{lstlisting}

Then apply reductions to the same coefficient matrix.

\begin{lstlisting}[style=dpmatlab]
rowSum = sum(A, 2);
allMean = mean(A, "all");
running = cumsum(A, 2);
disp(rowSum.evaluate(0))
fprintf("mean(all) at rho=0: %.1f\n", allMean.evaluate(0));
disp(running.evaluate(0))
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
     3
     7
mean(all) at rho=0: 2.5
     1     3
     3     7
\end{lstlisting}

Invalid triangular offsets raise
\codeerr{pdmat}{InvalidTriangularPart}.  Invalid reduction calls raise
\codeerr{pdmat}{InvalidSum}, \codeerr{pdmat}{InvalidMean}, or
\codeerr{pdmat}{InvalidCumsum}.

\subsubsection{Reordering And Rotation}
\label{sec:pdmat-matrix-reorder}

These methods reorder rows or columns inside every coefficient; they never
reverse the scheduling grid.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
B = flip(A)
B = flip(A, dim)
B = flipud(A)
B = fliplr(A)
B = rot90(A)
B = rot90(A, k)
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, {[1 2; 3 4], [10 20; 30 40]}, Degree=1);
F = flipud(A);
R = rot90(A);
disp(F.evaluate(0))
disp(R.evaluate(0))
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
     3     4
     1     2

     2     4
     1     3
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=9mm,
  b/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=22mm,minimum height=10mm}]
  \node[b] (a) {\(1\ \ 2\)\\\(3\ \ 4\)};
  \node[b,right=of a] (f) {\(3\ \ 4\)\\\(1\ \ 2\)};
  \node[b,right=of f] (r) {\(2\ \ 4\)\\\(1\ \ 3\)};
  \draw[->,thick,dpblue] (a) -- node[above,font=\scriptsize]{\code{flipud}} (f);
  \draw[->,thick,dpblue] (a) to[bend left=22]
    node[above,font=\scriptsize]{\code{rot90}} (r);
\end{tikzpicture}
\end{center}

Invalid dimensions or quarter-turn counts raise
\codeerr{pdmat}{InvalidFlip} or \codeerr{pdmat}{InvalidRot90}.

\subsubsection{Concatenation And Block Assembly}
\label{sec:pdmat-matrix-assembly}

Concatenation joins payload matrices after compatible grid refinement and
degree elevation.  Numeric blocks are promoted to constant known data.
\code{cat} supports only dimensions 1 and 2.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
H = horzcat(A, B, ...)
V = vertcat(A, B, ...)
H = [A, B]
V = [A; B]
C = cat(dim, A, B, ...)
D = blkdiag(A, B, ...)
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
a = pdmat({[0 1]}, {1, 2}, Degree=1);
b = pdmat({[0 1]}, {10, 20}, Degree=1);
H = [a, b];
V = [a; b];
D = blkdiag(a, 5);
disp(H.evaluate(0))
disp(V.evaluate(0))
disp(D.evaluate(0))
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
     1    10

     1
    10

     1     0
     0     5
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=8mm and 13mm,
  b/.style={draw=black!55,fill=white,minimum size=7mm,font=\scriptsize},
  o/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,minimum width=25mm,minimum height=10mm}]
  \node[b] (a) {\(a\)}; \node[b,below=of a] (b) {\(b\)};
  \node[o,right=of a] (h) {\([a\ b]\)};
  \node[o,right=of b] (d) {\(\left[\begin{smallmatrix}a&0\\0&5\end{smallmatrix}\right]\)};
  \draw[->,thick,dpblue] (a) -- (h); \draw[->,thick,dpblue] (b) -- (h);
  \draw[->,thick,dpblue] (a) -- (d);
\end{tikzpicture}
\end{center}

Incompatible blocks or syntax raise
\codeerr{pdmat}{InvalidConcatenation}; unsupported dimensions raise
\codeerr{pdmat}{UnsupportedCatDimension}; invalid block-diagonal inputs raise
\codeerr{pdmat}{InvalidBlkdiag}.  Function-only operands raise
\codeerr{pdmat}{FunctionOnlyAlgebra}.

\subsubsection{Repetition}

\code{repmat} tiles the matrix payload.  It accepts a positive scalar
\code{m}, interpreted as \code{[m m]}, two positive counts, or a two-element
count vector.  Grid and Bernstein degree remain unchanged.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
R = repmat(A, m)
R = repmat(A, m, n)
R = repmat(A, [m n])
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
v = pdmat({[0 1]}, {[1 2], [3 4]}, Degree=1);
R = repmat(v, [2 2]);
fprintf("repeated size=%d-by-%d, degree=%d\n", ...
    size(R,1), size(R,2), R.Degree);
disp(R.evaluate(0))
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
repeated size=2-by-4, degree=1
     1     2     1     2
     1     2     1     2
\end{lstlisting}

Invalid repetition shapes raise \codeerr{pdmat}{InvalidRepmat}.

\subsection{Indexing And Assignment}
\label{sec:pdmat-indexing}

Parenthesis indexing selects a matrix block from every coefficient.  Assignment
writes the selected block into every coefficient after compatible promotion and
degree alignment.  It does not select a physical cell; use \code{coeffs} for
cell inspection.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
B = A(rows, cols)
B = A(1:end, end)
value = A.PropertyName
A(rows, cols) = numericValue
A(rows, cols) = B
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
A = pdmat({[0 1]}, ...
    {[1 2 3; 4 5 6], [10 20 30; 40 50 60]}, Degree=1);
lastCol = A(:, end);
A(:, 2) = 5;
fprintf("selected size=%d-by-%d, assigned size=%d-by-%d\n", ...
    size(lastCol,1), size(lastCol,2), size(A,1), size(A,2));
disp(lastCol.evaluate(0))
disp(A.evaluate(0))
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
selected size=2-by-1, assigned size=2-by-3
     3
     6

     1     5     3
     4     5     6
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=10mm,
  b/.style={draw=black!55,fill=white,rounded corners=2pt,font=\scriptsize,
    align=center,minimum width=31mm,minimum height=13mm},
  o/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=31mm,minimum height=13mm}]
  \node[b] (a) {\(1\ \boxed{2}\ 3\)\\\(4\ \boxed{5}\ 6\)};
  \node[o,right=of a] (u) {\(1\ \boxed{5}\ 3\)\\\(4\ \boxed{5}\ 6\)};
  \draw[->,thick,dpblue] (a) -- node[above,font=\scriptsize]
    {\code{A(:,2)=5}} (u);
\end{tikzpicture}
\end{center}

\code{subsref}, \code{subsasgn}, \code{end}, and
\code{numArgumentsFromSubscript} implement this two-index MATLAB protocol.
Invalid selectors raise \codeerr{pdmat}{InvalidSubscript}.  Linear indexing,
deletion, or non-parenthesis assignment raises
\codeerr{pdmat}{UnsupportedAssignment}; an incompatible right-hand side raises
\codeerr{pdmat}{InvalidAssignment}.  Slicing and assignment require coefficient
evidence and otherwise raise \codeerr{pdmat}{FunctionOnlyAlgebra}.
\subsection{Limits}
\label{sec:pdmat-limits}

\begin{itemize}[leftmargin=*]
  \item Function-only objects created without \code{Degree} do not have
        Bernstein coefficient evidence for \code{bernsteinTable} or coefficient algebra.
  \item Numeric operands must be finite real nonempty matrices with compatible
        sizes, or finite real scalars that can broadcast to the required size.
  \item Grid refinement is supported for matching parameter dimensions and
        matching grid bounds.
\end{itemize}

\subsubsection{See Also}
\code{pdmat}, \code{evaluate}, \code{bernsteinTable}, \code{plot},
\code{pdbase}.

\section{\texorpdfstring{\code{pdvar}}{pdvar} Reference}
\label{sec:pdvar}

\code{pdvar} creates continuous cell-local Bernstein decision expressions backed
by YALMIP \code{sdpvar} coefficients.  It participates in supported affine and
known-data algebra.  It does not choose solvers or call \code{optimize};
solver-facing work happens after comparison overloads create \code{pdlmi}
objects and \code{toYalmip} converts them to YALMIP constraints.

\subsection{Lookup}
\label{sec:pdvar-lookup}

\begin{tabularx}{\textwidth}{@{}p{0.28\textwidth}Y@{}}
\toprule
Task & Entry \\
\midrule
Construct decisions &
\hyperref[sec:pdvar-construction]{\code{pdvar}} \\
Inspect coefficients &
\hyperref[sec:pdvar-table]{\code{bernsteinTable}} \\
Convert assigned coefficients &
\hyperref[sec:pdvar-value]{\code{value}} \\
Differentiate rates &
\hyperref[sec:pdvar-rhodiff]{\code{rhodiff}} \\
Affine/mixed algebra &
\hyperref[sec:pdvar-algebra]{\code{+}},
\hyperref[sec:pdvar-algebra]{\code{-}},
\hyperref[sec:pdvar-algebra]{\code{*}} with known data \\
Matrix/index methods &
\hyperref[sec:pdvar-matrix-ops]{structural methods},
\hyperref[sec:pdvar-indexing]{indexing and assignment} \\
Build LMIs &
\hyperref[sec:pdvar-comparison]{\code{<=}},
\hyperref[sec:pdvar-comparison]{\code{>=}} \\
\bottomrule
\end{tabularx}

\subsection{Construction}
\label{sec:pdvar-construction}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
P = pdvar(n, gridVectors)
P = pdvar(n, n, gridVectors)
P = pdvar(n, m, gridVectors)
P = pdvar(..., "full")
P = pdvar(..., "symmetric")
P = pdvar(..., Degree=0)
P = pdvar(..., Degree=1)
P = pdvar(..., Degree=d)
P = pdvar(..., RateBounds=rb)
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{pdvar(n, gridVectors)} creates an \(n\times n\) square variable
        using scalar-size shorthand; for example, \code{pdvar(2, {[0 1]})}.
        Square variables default to symmetric YALMIP coefficients.
  \item \code{pdvar(n, n, gridVectors)} is the explicit square-size form; for
        example, \code{pdvar(2, 2, {[0 1]})}.  It has the same default
        symmetric structure as the shorthand.
  \item \code{pdvar(n, m, gridVectors)} creates an \(n\times m\) variable.
        Rectangular variables default to full YALMIP coefficients; for example,
        \code{pdvar(2, 3, {[0 1]})}.
  \item \code{"full"} and \code{"symmetric"} explicitly choose the YALMIP
        coefficient structure.  \code{"symmetric"} requires a square size.
  \item \code{Degree=1} is the default continuous Bernstein decision variable.
        Every finite nonnegative integer \code{Degree=d} is supported.
        \code{Degree=0} creates one parameter-independent decision matrix
        shared across the grid; \code{d >= 1} creates continuous piecewise
        Bernstein data whose neighboring cells share complete control faces.
  \item \code{RateBounds=rb} stores parameter-rate metadata for later
        \code{rhodiff(P)} calls.  Use \code{RateBounds=[-1 1]} for one
        parameter; use a two-row matrix for two parameters, as shown in the
         rate-metadata example below.
\end{itemize}

\begin{center}
\begin{tikzpicture}[x=1.85cm,y=1cm,>=Latex,
  c/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=18mm,minimum height=8mm}]
  \node[c] (p0) at (0,0) {\(P^{(1)}[0]\)};
  \node[c] (p1) at (1,0) {\(P^{(1)}[1]\)};
  \node[c,minimum width=22mm] (p2) at (2,0)
    {\(P^{(1)}[2]\)\\\(=P^{(2)}[0]\)};
  \node[c] (p3) at (3,0) {\(P^{(2)}[1]\)};
  \node[c] (p4) at (4,0) {\(P^{(2)}[2]\)};
  \draw[thick,dpblue] (p0) -- (p1) -- (p2) -- (p3) -- (p4);
  \draw[decorate,decoration={brace,mirror,amplitude=4pt}]
    (-0.25,-0.45) -- (2.25,-0.45)
    node[midway,below=6pt,font=\scriptsize]{cell 1 view, degree 2};
  \draw[decorate,decoration={brace,mirror,amplitude=4pt}]
    (1.75,-0.45) -- (4.25,-0.45)
    node[midway,below=6pt,font=\scriptsize]{cell 2 view, degree 2};
  \node[above=5pt of p2,font=\scriptsize,align=center]
    {shared interface handle\\gives structural continuity};
\end{tikzpicture}

\textit{For positive degree, neighboring physical cells reuse complete
boundary-face YALMIP handles; no extra continuity LMI is generated.}
\end{center}

\examplehead
The square shorthand creates a \(2\times2\) continuous decision object.  The
default degree is one and the object carries no rate metadata.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
Ps = pdvar(2, {[0 1]});
class(Ps)
size(Ps)
Ps.Degree
Ps.HasRateDependence
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
    'pdvar'

ans =
     2     2

ans =
     1

ans =
  logical
   0
\end{lstlisting}

Use two dimension arguments for a rectangular full variable.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
Pf = pdvar(2, 3, {[0 1]});
class(Pf)
size(Pf)
Pf.Degree
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
    'pdvar'

ans =
     2     3

ans =
     1
\end{lstlisting}

The structure flag makes the coefficient structure explicit.  This is useful
when a square variable should use full, not symmetric, coefficients.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
Pfull = pdvar(2, 2, {[0 1]}, "full");
class(Pfull)
Pfull.MatrixSize
Pfull.Degree
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
    'pdvar'

ans =
     2     2

ans =
     1
\end{lstlisting}

Use \code{Degree=0} for a parameter-independent decision variable stored with
grid metadata.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P0 = pdvar(1, [0 1 2], Degree=0);
class(P0)
P0.Degree
P0.ncell()
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
    'pdvar'

ans =
     0

ans =
     2
\end{lstlisting}

Rate bounds are metadata for later \code{rhodiff(P)} calls.  \code{pdlmi}
constrains rate vertices only after an expression such as \code{rhodiff} has
stored rate-vertex rows.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
Pr = pdvar(1, {[0 1], [10 20]}, RateBounds=[-1 2; -3 4]);
class(Pr)
Pr.HasRateDependence
Pr.RateBounds
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
    'pdvar'

ans =
  logical
   1

ans =
    -1     2
    -3     4
\end{lstlisting}

\refhead{Validation and errors}
Missing dimensions, a missing grid, or a grid in the wrong argument position
raises \codeerr{pdvar}{InvalidInput}.  Nonpositive or noninteger matrix sizes
raise \codeerr{pdvar}{Invalid}\allowbreak\code{MatrixSize}; malformed degrees
raise \codeerr{pdvar}{InvalidDegree}.  Malformed option sequences raise
\codeerr{pdvar}{InvalidOptions}; an unknown option raises
\codeerr{pdvar}{UnknownOption}; attempts to set subclass-owned metadata raise
\codeerr{pdvar}{UnsupportedOption}.  A symmetric request for a nonsquare
matrix raises \codeerr{pdvar}{InvalidStructure}.  Grid validation uses
\codeerr{pdvar}{InvalidGrid} and
\codeerr{pdvar}{Invalid}\allowbreak\code{GridVector}.

\code{RateBounds} is validated by the inherited storage contract and must be a
finite \(\ell\)-by-2 matrix with lower bounds no greater than upper bounds.
Constructor violations therefore raise
\codeerr{pdbase}{Invalid}\allowbreak\code{RateBounds}.

\subsection{\texorpdfstring{\code{rhodiff}}{rhodiff}}
\label{sec:pdvar-rhodiff}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
D = rhodiff(P)
D = rhodiff(P, rb)
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{P} is a \code{pdvar} without existing rate-vertex rows.
  \item \code{rb} is a finite \(\ell\)-by-\(2\) numeric matrix of lower and
        upper parameter-rate bounds, with each lower bound no greater than its
        upper bound.  It must equal \code{P.RateBounds} when the object already
        carries nonempty bounds.
  \item Without \code{rb}, \code{rhodiff(P)} requires nonempty
        \code{P.RateBounds}.
  \item \code{D} is a discontinuous \code{pdvar} with one coefficient row per
        rate vertex.  In one parameter a degree-\(m\) derivative has degree
        \(m-1\), while a degree-zero derivative remains zero; multivariate
        partials are elevated into the implementation's common tensor degree.
\end{itemize}

For a one-parameter degree-one coefficient pair
\(P^{(c)}[0],P^{(c)}[1]\) on physical cell (c) of width \(h_c\), the
rate-dependent derivative row is
\[
  \dot P(\rho,\dot\rho)
  =\dot\rho\,\frac{P^{(c)}[1]-P^{(c)}[0]}{h_c}.
\]
The implementation stores one row for each allowed rate vertex.

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=7mm and 10mm,
  box/.style={draw=black!55,fill=white,rounded corners=2pt,font=\scriptsize,
    align=center,minimum width=33mm,minimum height=10mm},
  result/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=34mm,minimum height=10mm}]
  \node[box] (p) {\(P^{(c)}[0],P^{(c)}[1]\)\\physical cell (c)};
  \node[result,right=of p] (d)
    {\((P^{(c)}[1]-P^{(c)}[0])/h_c\)\\local derivative};
  \node[result,right=of d,yshift=8mm] (lo) {\(\dot\rho_{\min}\) row};
  \node[result,right=of d,yshift=-8mm] (hi) {\(\dot\rho_{\max}\) row};
  \draw[->,thick,dpblue] (p) -- node[above,font=\scriptsize]{forward difference} (d);
  \draw[->,thick,dpblue] (d) -- (lo);
  \draw[->,thick,dpblue] (d) -- (hi);
\end{tikzpicture}

\textit{Differentiation changes local coefficients; rate substitution creates
rows.  Neither operation adds physical cells.}
\end{center}

\refhead{Validation and errors}
Unsupported call arity or differentiating an expression that already contains
rate-vertex rows raises \codeerr{pdvar}{InvalidDiff}.  Explicit malformed rate
bounds raise \codeerr{pdvar}{Invalid}\allowbreak\code{RateBounds}.  Calling
\code{rhodiff(P)} without stored bounds raises
\codeerr{pdvar}{Missing}\allowbreak\code{RateBounds}; explicit bounds that
disagree with \code{P.RateBounds} raise
\codeerr{pdvar}{RateBounds}\allowbreak\code{Mismatch}.

\examplehead
When rate bounds live on the object, \code{rhodiff(P)} uses them directly.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, {[0 1 2]}, "symmetric", RateBounds=[-1 1]);
D = rhodiff(P);
class(D)
D.Degree
D.IsContinuous
size(D.coeffs(1))
D.RateBounds
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
    'pdvar'

ans =
     0

ans =
  logical
   0

ans =
     2     1

ans =
    -1     1
\end{lstlisting}

For a two-parameter object, the rate-vertex rows enumerate all lower/upper
combinations from the two rows of \code{RateBounds}.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
Q = pdvar(1, {[0 1], [10 20]}, RateBounds=[-1 1; -2 2]);
E = rhodiff(Q);
class(E)
E.Degree
size(E.coeffs([1 1]))
E.RateBounds
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
    'pdvar'

ans =
     1

ans =
     4     4

ans =
    -1     1
    -2     2
\end{lstlisting}

\subsection{\texorpdfstring{\code{bernsteinTable}}{bernsteinTable}}
\label{sec:pdvar-table}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
T = bernsteinTable(P)
T = bernsteinTable(P, cellSubs)
T = bernsteinTable(P, "oneLine")
T = bernsteinTable(P, cellSubs, "oneLine")
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{P} is a \code{pdvar} object.
  \item \code{cellSubs} selects one physical cell; omit it to list every cell.
  \item \code{"oneLine"} is the only supported text option.  It returns one
        compact Bernstein expression per selected cell.
  \item \code{T} is a table.  Ordinary rows contain the local label, basis,
         physical-node flag, and symbolic or numeric value.  A rate-dependent
         derivative also includes \code{RateVertexIndex} and \code{RateVertex}.
\end{itemize}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=6mm and 9mm,
  box/.style={draw=black!55,fill=white,rounded corners=2pt,font=\scriptsize,
    align=center,minimum width=31mm,minimum height=9mm},
  result/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=39mm,minimum height=11mm}]
  \node[box] (ordinary) {ordinary \code{pdvar}\\cell + local label};
  \node[box,below=of ordinary] (rate) {\code{rhodiff(P)}\\cell + rate vertex + label};
  \node[result,right=of ordinary,yshift=-7mm] (table) {stable display text\\
    never raw \code{sdpvar} cells};
  \draw[->,thick,dpblue] (ordinary.east) -- (table.west);
  \draw[->,thick,dpblue] (rate.east) -- (table.west);
\end{tikzpicture}
\end{center}

\examplehead
\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(1, {[0 1]});
T = bernsteinTable(P, "oneLine");
disp(T)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
    CellSubscript       Expression
    _____________    ________________

        {[1]}        "(1-alpha)*internal(1) + alpha*internal(2)"
\end{lstlisting}

\refhead{Validation and errors}
\code{bernsteinTable} is an inspection method, not a solver call.  Text options
other than \code{"oneLine"}, repeated selectors, invalid physical-cell
selectors, or inconsistent rate-vertex rows raise
\codeerr{pdvar}{Invalid}\allowbreak\code{BernsteinTableInput}.

\subsection{Affine Algebra And Known-Data Products}
\label{sec:pdvar-algebra}

A \code{pdvar} result must remain affine in the underlying YALMIP decisions.
The following blocks separate affine sums from products so the supported
boundary is visible beside each example.

\subsubsection{Signs, Addition, And Subtraction}

Use this family to combine compatible decision expressions, coefficient-backed
known data, finite real numeric data, and affine \code{sdpvar} matrices.
Operands with the same physical bounds are aligned on a common grid and degree.
The corresponding overload names are \code{plus}, \code{minus}, \code{uplus},
and \code{uminus}.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
R = P + Q
R = P + A
R = A + P
R = P + M
R = M + P
R = P + X
R = X + P
R = P - Q
R = P - A
R = A - P
R = P - M
R = M - P
R = +P
R = -P
\end{lstlisting}

Here \code{A} is coefficient-backed \code{pdmat}, \code{M} is numeric, and
\code{X} is affine \code{sdpvar}.  A numeric scalar broadcasts to the payload
size; an affine \code{sdpvar} is promoted as parameter-independent data.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, {[0 1]}, "full");
X = sdpvar(2, 2, 'full');
S = P + X;
D = eye(2) - P;
N = -P;
fprintf("sum: %s, %d-by-%d, degree=%d, decision=%d\n", ...
    class(S), size(S,1), size(S,2), S.Degree, S.ContainsDecision);
fprintf("difference: %s, unary minus: %s\n", class(D), class(N));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
sum: pdvar, 2-by-2, degree=1, decision=1
difference: pdvar, unary minus: pdvar
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=7mm and 10mm,
  box/.style={draw=black!55,fill=white,rounded corners=2pt,font=\scriptsize,
    align=center,minimum width=31mm,minimum height=9mm},
  result/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=34mm,minimum height=9mm}]
  \node[box] (p) {\(P(\rho)\)\\degree-one decisions};
  \node[box,below=of p] (x) {\(X\)\\constant affine \code{sdpvar}};
  \node[result,right=of p,yshift=-7mm] (s) {\(P(\rho)+X\)\\same grid and degree};
  \draw[->,thick,dpblue] (p.east) -- (s.west);
  \draw[->,thick,dpblue] (x.east) -- (s.west);
\end{tikzpicture}
\end{center}

A nonlinear symbolic operand such as \code{x*x} is rejected.  Rate-vertex rows
from \code{rhodiff} may be added to compatible ordinary expressions; the
ordinary rows are broadcast over the active rate vertices.

\subsubsection{Multiplication By Known Or Numeric Data}
\label{sec:pdvar-operator-entries}

Multiplication is supported only when decision dependence occurs on at most one
side.  A coefficient-backed \code{pdmat} or finite real numeric matrix may
multiply a \code{pdvar} from either side.  A scalar \code{pdvar} may scale a
known matrix.  Two decision-bearing factors, including \code{P*Q} and
\code{P*x} for a decision \code{sdpvar x}, are nonlinear and rejected.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
R = A * P
R = P * A
R = M * P
R = P * M
\end{lstlisting}

The first example shows numeric left and right matrix products without exposing
incidental YALMIP variable identifiers.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, 1, {[0 1]}, "full");
L = [1 2] * P;
R = P * [4 5];
S = 3 * P;
fprintf("left=%d-by-%d, right=%d-by-%d, scaled=%d-by-%d\n", ...
    size(L,1), size(L,2), size(R,1), size(R,2), size(S,1), size(S,2));
fprintf("degrees: %d, %d, %d\n", L.Degree, R.Degree, S.Degree);
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
left=1-by-1, right=2-by-2, scaled=2-by-1
degrees: 1, 1, 1
\end{lstlisting}

A known parameter-dependent factor adds its Bernstein degree through ordered
coefficient convolution.  On one cell, a quadratic decision and affine known
factor produce
\[
  (PA)[k]=\sum_{i+j=k}
  \frac{\binom2i\binom1j}{\binom3k}\,P[i]A[j],
  \qquad k=0,1,2,3.
\]
The same backend rule is documented at \code{bernProd} in
\cref{sec:pdbase-bernprod} and derived step by step in
Appendix~\ref{app:bernstein-math}.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, {[0 1]}, "symmetric", Degree=2);
A = pdmat({[0 1]}, {eye(2), 2*eye(2)}, Degree=1);
L = A * P;
R = P * A;
fprintf("known-left: %s degree %d; known-right: %s degree %d\n", ...
    class(L), L.Degree, class(R), R.Degree);
fprintf("coefficients per cell=%d\n", numel(L.coeffs(1)));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
known-left: pdvar degree 3; known-right: pdvar degree 3
coefficients per cell=4
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=8mm and 11mm,
  known/.style={dp/known,font=\scriptsize,
    align=center,minimum width=30mm,minimum height=10mm},
  decision/.style={dp/decision,font=\scriptsize,
    align=center,minimum width=30mm,minimum height=10mm},
  result/.style={dp/result,
    font=\scriptsize,align=center,minimum width=35mm,minimum height=10mm}]
  \node[known] (a) {\(A(\rho)\) known\\degree 1};
  \node[decision,below=of a] (p) {\(P(\rho)\) decision\\degree 2};
  \node[result,right=of a,yshift=-7mm] (ap) {\(A(\rho)P(\rho)\)\\affine, degree 3};
  \draw[->,thick,dpblue] (a.east) -- (ap.west);
  \draw[->,thick,dpblue] (p.east) -- (ap.west);
\end{tikzpicture}
\end{center}

A derivative object may be multiplied by numeric data or a matching-grid known
object while preserving every rate row.  Products with rate dependence on both
sides, products of a derivative with an ordinary decision expression, and
products involving metadata-only rate dependence are rejected.

Two decision-bearing factors are also rejected: their product would be
quadratic in YALMIP decision coefficients.  The supported boundary is numeric
or known \code{pdmat} data multiplying one affine \code{pdvar} expression on
either side, with the written matrix order preserved.

\refhead{Validation and errors}
Incompatible affine operands raise \codeerr{pdvar}{InvalidAddition} or
\codeerr{pdvar}{InvalidSubtraction}.  Bad dimensions, two decision-bearing
factors, nonlinear symbolic data, or unsupported rate combinations raise
\codeerr{pdvar}{InvalidMultiplication}.  Incompatible parameter bounds raise
\codeerr{pdvar}{MixedGrid}; a function-only \code{pdmat} operand raises
\codeerr{pdvar}{FunctionOnlyAlgebra}.

\subsection{Matrix Methods}
\label{sec:pdvar-matrix-ops}

Every method below transforms each local affine YALMIP payload while preserving
the physical grid, degree, and compatible rate rows.  The examples report
stable classes, dimensions, and metadata rather than YALMIP's internal variable
numbers.

\subsubsection{Shape, Equality, And MATLAB Protocols}
\label{sec:pdvar-matrix-shape}
\label{sec:pdvar-matrix-protocol}

Shape queries describe the matrix payload.  \code{isequal} is an exact identity
check: grid, degree, matrix size, metadata, and local symbolic payloads must
already match; it does not merge or elevate operands.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
sz = size(P)
d = size(P, dim)
n = height(P)
n = width(P)
n = length(P)
n = numel(P)
n = ndims(P)
tf = isequal(P, Q, ...)
idx = end(P, k, n)
n = numArgumentsFromSubscript(P, S, ctx)
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, 3, {[0 1]}, "full");
fprintf("size=%d-by-%d, height=%d, width=%d\n", ...
    size(P,1), size(P,2), height(P), width(P));
fprintf("length=%d, numel=%d, ndims=%d, equal(+P)=%d\n", ...
    length(P), numel(P), ndims(P), isequal(P,+P));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
size=2-by-3, height=2, width=3
length=3, numel=6, ndims=2, equal(+P)=1
\end{lstlisting}

\code{end}, \code{subsref}, \code{subsasgn}, and
\code{numArgumentsFromSubscript} support the two-index and dot-access forms
shown in \cref{sec:pdvar-indexing}; they do not expose physical-cell storage.

\subsubsection{Transpose And Conjugate Transpose}
\label{sec:pdvar-matrix-transform}

Both forms transpose every local affine payload without breaking the YALMIP
coefficient graph.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
T = transpose(P)
H = ctranspose(P)
T = P.'
H = P'
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, 3, {[0 1]}, "full");
T = P.';
H = P';
fprintf("transpose=%d-by-%d, ctranspose=%d-by-%d\n", ...
    size(T,1), size(T,2), size(H,1), size(H,2));
fprintf("classes: %s, %s\n", class(T), class(H));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
transpose=3-by-2, ctranspose=3-by-2
classes: pdvar, pdvar
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=12mm,
  a/.style={matrix of math nodes,nodes={draw=black!45,minimum size=6mm,
    font=\scriptsize},column sep=-\pgflinewidth,row sep=-\pgflinewidth}]
  \matrix[a] (m) {p_{11}&p_{12}&p_{13}\\p_{21}&p_{22}&p_{23}\\};
  \matrix[a,right=of m] (t) {p_{11}&p_{21}\\p_{12}&p_{22}\\p_{13}&p_{23}\\};
  \draw[->,thick,dpblue] (m.east) -- node[above,font=\scriptsize]
    {each coefficient} (t.west);
\end{tikzpicture}
\end{center}

\subsubsection{Vectorization, Reshape, And Squeeze}

\code{vec} uses MATLAB column-major order.  \code{reshape} accepts two positive
dimensions with at most one inferred \code{[]} dimension and preserves the
element count.  \code{squeeze} retains the two-dimensional package convention.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
V = vec(P)
R = reshape(P, [m n])
R = reshape(P, m, n)
S = squeeze(P)
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, 3, {[0 1]}, "full");
V = vec(P);
R = reshape(P, [3 2]);
S = squeeze(P);
fprintf("vec=%d-by-%d, reshape=%d-by-%d, squeeze=%d-by-%d\n", ...
    size(V,1), size(V,2), size(R,1), size(R,2), size(S,1), size(S,2));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
vec=6-by-1, reshape=3-by-2, squeeze=2-by-3
\end{lstlisting}

Malformed or size-changing reshape requests raise
\codeerr{pdvar}{InvalidReshape}.

\subsubsection{Diagonals And Trace}

\code{diag} extracts a diagonal or constructs a diagonal matrix from a vector;
the selected offset may not be empty.  \code{trace} returns a scalar decision
expression.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
d = diag(P)
d = diag(P, k)
D = diag(v)
D = diag(v, k)
t = trace(P)
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(3, {[0 1]}, "full");
d = diag(P);
D = diag(d);
t = trace(P);
fprintf("diag=%d-by-%d, rebuilt=%d-by-%d, trace=%d-by-%d\n", ...
    size(d,1), size(d,2), size(D,1), size(D,2), size(t,1), size(t,2));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
diag=3-by-1, rebuilt=3-by-3, trace=1-by-1
\end{lstlisting}

Invalid offsets or empty selections raise \codeerr{pdvar}{InvalidDiag}.

\subsubsection{Triangular Parts And Reductions}
\label{sec:pdvar-matrix-reductions}

Triangular methods zero the complementary part of every coefficient.
Reductions act on matrix dimensions; \code{"all"} is available for
\code{sum} and \code{mean}.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
L = tril(P)
L = tril(P, k)
U = triu(P)
U = triu(P, k)
S = sum(P)
S = sum(P, dim)
S = sum(P, "all")
M = mean(P)
M = mean(P, dim)
M = mean(P, "all")
C = cumsum(P)
C = cumsum(P, dim)
\end{lstlisting}

First form the two triangular views.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(3, {[0 1]}, "full");
L = tril(P);
U = triu(P);
fprintf("lower=%d-by-%d, upper=%d-by-%d, degrees=%d/%d\n", ...
    size(L,1), size(L,2), size(U,1), size(U,2), L.Degree, U.Degree);
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
lower=3-by-3, upper=3-by-3, degrees=1/1
\end{lstlisting}

Then reduce a rectangular expression.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, 3, {[0 1]}, "full");
rowSum = sum(P, 2);
allMean = mean(P, "all");
running = cumsum(P, 2);
fprintf("row sum=%d-by-%d, all mean=%d-by-%d, running=%d-by-%d\n", ...
    size(rowSum,1), size(rowSum,2), size(allMean,1), size(allMean,2), ...
    size(running,1), size(running,2));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
row sum=2-by-1, all mean=1-by-1, running=2-by-3
\end{lstlisting}

Invalid triangular offsets raise
\codeerr{pdvar}{InvalidTriangularPart}.  Invalid reduction calls raise
\codeerr{pdvar}{InvalidSum}, \codeerr{pdvar}{InvalidMean}, or
\codeerr{pdvar}{InvalidCumsum}.

\subsubsection{Reordering And Rotation}
\label{sec:pdvar-matrix-reorder}

These methods rearrange matrix entries inside every symbolic coefficient; they
do not reverse the parameter grid.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
Q = flip(P)
Q = flip(P, dim)
Q = flipud(P)
Q = fliplr(P)
Q = rot90(P)
Q = rot90(P, k)
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, 3, {[0 1]}, "full");
F = flipud(P);
R = rot90(P);
fprintf("flipud=%d-by-%d, rot90=%d-by-%d\n", ...
    size(F,1), size(F,2), size(R,1), size(R,2));
fprintf("classes: %s, %s\n", class(F), class(R));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
flipud=2-by-3, rot90=3-by-2
classes: pdvar, pdvar
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=8mm,
  b/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=30mm,minimum height=12mm}]
  \node[b] (p) {\(2\times3\) decision matrix};
  \node[b,right=of p] (f) {\code{flipud}: \(2\times3\)};
  \node[b,right=of f] (r) {\code{rot90}: \(3\times2\)};
  \draw[->,thick,dpblue] (p) -- (f);
  \draw[->,thick,dpblue] (f) -- (r);
\end{tikzpicture}
\end{center}

Invalid calls raise \codeerr{pdvar}{InvalidFlip} or
\codeerr{pdvar}{InvalidRot90}.

\subsubsection{Concatenation And Block Assembly}
\label{sec:pdvar-matrix-assembly}

Assembly accepts compatible \code{pdvar}, coefficient-backed \code{pdmat},
affine \code{sdpvar}, and finite real numeric blocks.  It aligns grids and
degrees while preserving an affine expression.  \code{cat} supports only
matrix dimensions 1 and 2.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
H = horzcat(P, Q, ...)
V = vertcat(P, Q, ...)
H = [P, Q]
V = [P; Q]
C = cat(dim, P, Q, ...)
D = blkdiag(P, Q, ...)
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, {[0 1]}, "full");
H = [P, P];
V = [P; P];
D = blkdiag(P, eye(2));
fprintf("horizontal=%d-by-%d, vertical=%d-by-%d, block=%d-by-%d\n", ...
    size(H,1), size(H,2), size(V,1), size(V,2), size(D,1), size(D,2));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
horizontal=2-by-4, vertical=4-by-2, block=4-by-4
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=10mm,
  b/.style={draw=black!55,fill=white,rounded corners=2pt,font=\scriptsize,
    minimum width=18mm,minimum height=10mm},
  o/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,minimum width=35mm,minimum height=13mm,align=center}]
  \node[b] (p) {\(P\)};
  \node[o,right=of p] (h) {\([P\ P]\)\\\(2\times4\)};
  \node[o,below=of h] (d) {\(\operatorname{blkdiag}(P,I)\)\\\(4\times4\)};
  \draw[->,thick,dpblue] (p) -- (h);
  \draw[->,thick,dpblue] (p) -- (d);
\end{tikzpicture}
\end{center}

Incompatible blocks or rate metadata raise
\codeerr{pdvar}{InvalidConcatenation}; unsupported dimensions raise
\linebreak[4]\code{pdvar:}\allowbreak\code{UnsupportedCat}\allowbreak\code{Dimension};
invalid block-diagonal inputs raise
\codeerr{pdvar}{InvalidBlkdiag}.  Incompatible bounds raise
\codeerr{pdvar}{MixedGrid}; function-only known blocks raise
\linebreak[4]\code{pdvar:}\allowbreak\code{FunctionOnly}\allowbreak\code{Algebra}.

\subsubsection{Repetition}

\code{repmat} tiles the matrix payload while retaining the parameter metadata.
It accepts a positive scalar, two positive counts, or a two-element count
vector.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
Q = repmat(P, m)
Q = repmat(P, m, n)
Q = repmat(P, [m n])
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(1, 2, {[0 1]}, "full");
Q = repmat(P, [2 2]);
fprintf("repeated=%d-by-%d, degree=%d, decision=%d\n", ...
    size(Q,1), size(Q,2), Q.Degree, Q.ContainsDecision);
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
repeated=2-by-4, degree=1, decision=1
\end{lstlisting}

Invalid repetition shapes raise \codeerr{pdvar}{InvalidRepmat}.

\subsection{Indexing And Assignment}
\label{sec:pdvar-indexing}

Parenthesis indexing selects a matrix block from every symbolic coefficient.
Assignment writes a compatible numeric, \code{pdvar}, coefficient-backed
\code{pdmat}, or affine \code{sdpvar} block into that position.  It does not
select one physical cell.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
Q = P(rows, cols)
Q = P(1:end, end)
value = P.PropertyName
P(rows, cols) = numericValue
P(rows, cols) = Q
P(rows, cols) = A
P(rows, cols) = X
\end{lstlisting}

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, 3, {[0 1]}, "full");
lastCol = P(:, end);
P(:, 1) = 0;
fprintf("selected=%d-by-%d, assigned=%d-by-%d\n", ...
    size(lastCol,1), size(lastCol,2), size(P,1), size(P,2));
fprintf("class=%s, degree=%d, decision=%d\n", ...
    class(P), P.Degree, P.ContainsDecision);
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
selected=2-by-1, assigned=2-by-3
class=pdvar, degree=1, decision=1
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=10mm,
  b/.style={draw=black!55,fill=white,rounded corners=2pt,font=\scriptsize,
    align=center,minimum width=34mm,minimum height=13mm},
  o/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=34mm,minimum height=13mm}]
  \node[b] (p) {\(\boxed{p_{11}}\ p_{12}\ p_{13}\)\\
    \(\boxed{p_{21}}\ p_{22}\ p_{23}\)};
  \node[o,right=of p] (q) {\(\boxed{0}\ p_{12}\ p_{13}\)\\
    \(\boxed{0}\ p_{22}\ p_{23}\)};
  \draw[->,thick,dpblue] (p) -- node[above,font=\scriptsize]
    {\code{P(:,1)=0}} (q);
\end{tikzpicture}
\end{center}

Invalid selectors raise \codeerr{pdvar}{InvalidSubscript}.  Linear indexing,
deletion, or non-parenthesis assignment raises
\codeerr{pdvar}{UnsupportedAssignment}; incompatible or nonlinear right-hand
sides raise \codeerr{pdvar}{InvalidAssignment}.
\subsection{\texorpdfstring{\code{value}}{value}}
\label{sec:pdvar-value}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
A = value(P)
rows = value(rhodiff(P))
\end{lstlisting}

\code{value} converts assigned \code{pdvar} coefficients into known,
coefficient-backed \code{pdmat} data.  An ordinary expression returns one
\code{pdmat}, preserving its grid, matrix size, degree, local coefficient
order, and continuity metadata.  A rate-vertex expression created by
\code{rhodiff} returns a \(1\)-by-\(2^\ell\) cell array of \code{pdmat}
objects in \code{helper.combRows(RateBounds)} lower/upper vertex order.  The
returned \code{pdmat} objects do not carry rate metadata.  Every symbolic
coefficient must have an assigned finite real YALMIP value; otherwise the
method raises \codeerr{pdvar}{UnassignedValue}.

\examplehead
\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(1, [0 1], Degree=2);
c = P.coeffs(1);
assign([c{:}], [1 2 4]);
A = value(P);
A.coeffs(1)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
    {[1]}    {[2]}    {[4]}
\end{lstlisting}

\subsection{Comparisons To \texorpdfstring{\code{pdlmi}}{pdlmi}}
\label{sec:pdvar-comparison}

The \code{le} and \code{ge} overloads implement \code{<=} and \code{>=}.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
C = P <= 0
C = P >= 0
C = lhs <= rhs
C = lhs >= rhs
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{P} and \code{lhs} are square, coefficient-wise symmetric or
        Hermitian \code{pdvar} expressions after residual assembly.
  \item \code{rhs} is commonly \code{0}; compatible numeric, \code{pdmat},
        affine \code{sdpvar}, and \code{pdvar} expressions are also accepted.
  \item \code{C} is a \code{pdlmi} wrapper.  The comparison creates direct
        coefficient constraints but does not solve an optimization problem.
\end{itemize}

\examplehead
Comparison creates a \code{pdlmi} wrapper with one stored constraint for each
physical cell and local Bernstein coefficient.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, {[0 0.5 1]}, "symmetric");
Cneg = P <= 0;
class(Cneg)
numel(Cneg.Constraints)
Cpos = P >= 0;
class(Cpos)
numel(Cpos.Constraints)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
    'pdlmi'

ans =
     4

ans =
    'pdlmi'

ans =
     4
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=6mm,
  box/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=36mm,minimum height=11mm}]
  \node[box] (lhs) {\code{lhs}\\affine square \code{pdvar}};
  \node[box,right=of lhs] (rhs) {\code{rhs}\\compatible affine data};
  \node[box,right=of rhs] (res) {\code{lhs-rhs}\\stored residual};
  \node[box,right=of res] (c) {\code{pdlmi}\\direct by default};
  \draw[->,thick,dpblue] (lhs) -- (res);
  \draw[->,thick,dpblue] (rhs) -- (res);
  \draw[->,thick,dpblue] (res) -- node[above,font=\scriptsize]
    {\code{<=} or \code{>=}} (c);
\end{tikzpicture}
\end{center}

\subsection{Limits}
\label{sec:pdvar-limits}

\begin{itemize}[leftmargin=*]
  \item Constructor-created \code{pdvar} objects support every finite
        nonnegative integer degree.  Higher degree increases the number of
        Bernstein control matrices per cell.
  \item Products may contain decision variables on at most one side; nonlinear
        decision products such as \code{P * Q} are outside this slice.
  \item Function-only \code{pdmat} operands cannot enter \code{pdvar} algebra
        unless they were constructed with explicit Bernstein evidence.
  \item \code{rhodiff} of an existing rate-vertex derivative expression is not
        part of the current public workflow.
\end{itemize}

\subsubsection{See Also}
\code{pdvar}, \code{rhodiff}, \code{bernsteinTable}, \code{pdlmi},
\code{pdmat}.

\section{\texorpdfstring{\code{pdlmi}}{pdlmi} Reference}
\label{sec:pdlmi}

\code{pdlmi} stores YALMIP constraints assembled from square symmetric
\code{pdvar} expressions.  Direct coefficient-wise assembly is the default;
cell-local P\'olya elevation, a Putinar box quadratic module, and the full box
Bernstein--Gram preordering are explicit alternatives.  It is the package
boundary between Bernstein objects and ordinary YALMIP solver calls.

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=7mm and 9mm,
  box/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=34mm,minimum height=13mm}]
  \node[box] (cmp) {\code{lhs <= rhs}\\store residual and relation};
  \node[box,right=of cmp,yshift=21mm] (direct) {direct\\coefficient signs};
  \node[box,right=of cmp,yshift=7mm] (polya) {P\'olya\\lossless elevation};
  \node[box,right=of cmp,yshift=-7mm] (putinar) {Putinar\\singleton box generators};
  \node[box,right=of cmp,yshift=-21mm] (box) {full box\\all generator products};
  \node[box,right=16mm of polya,yshift=-7mm] (yal) {\code{toYalmip}\\finite constraints};
  \draw[->,thick,dpblue] (cmp.east) -- (direct.west);
  \draw[->,thick,dpblue] (cmp.east) -- (polya.west);
  \draw[->,thick,dpblue] (cmp.east) -- (putinar.west);
  \draw[->,thick,dpblue] (cmp.east) -- (box.west);
  \draw[->,thick,dpblue] (direct.east) -- (yal.west);
  \draw[->,thick,dpblue] (polya.east) -- (yal.west);
  \draw[->,thick,dpblue] (putinar.east) -- (yal.west);
  \draw[->,thick,dpblue] (box.east) -- (yal.west);
\end{tikzpicture}

\textit{The four certificate paths are alternatives.  Selecting P\'olya,
Putinar, or full-box rebuilding replaces, rather than compounds, the previous
selection.}
\end{center}

\subsection{Lookup}
\label{sec:pdlmi-lookup}

\begin{tabularx}{\textwidth}{@{}p{0.28\textwidth}Y@{}}
\toprule
Task & Entry \\
\midrule
Construct wrapper &
\hyperref[sec:pdlmi-construction]{\code{pdlmi}},
\hyperref[sec:pdvar-comparison]{\code{<=}/\code{>=}} \\
Count constraints &
\hyperref[sec:pdlmi-direct]{direct coefficient constraints} \\
Select P\'olya elevation &
\hyperref[sec:pdlmi-polya]{\code{UsePolya}, \code{PolyaDegree}, and \code{applyPolya}} \\
Select Putinar box module &
\hyperref[sec:pdlmi-putinar]{\code{UsePutinar}, \code{PutinarOrder}, and
\code{applyPutinar}} \\
Select full box preordering &
\hyperref[sec:pdlmi-full-box]{\code{UseFullBoxPreorder}, \code{FullBoxOrder},
and \code{applyFullBoxPreorder}} \\
Export to YALMIP &
\hyperref[sec:pdlmi-toyalmip]{\code{toYalmip}} \\
Solve &
\hyperref[sec:pdlmi-solver-use]{solver-facing use},
\hyperref[sec:examples]{Examples} \\
\bottomrule
\end{tabularx}

\subsection{Construction And Comparisons}
\label{sec:pdlmi-construction}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
C = pdlmi(expr, relation)
C = pdlmi(expr, relation, "UsePolya")
C = pdlmi(expr, relation, UsePolya=true, PolyaDegree=d)
C = pdlmi(expr, relation, "UsePolya", "PolyaDegree", d)
C = pdlmi(expr, relation, "UsePutinar")
C = pdlmi(expr, relation, UsePutinar=true, PutinarOrder=r)
C = pdlmi(expr, relation, PutinarOrder=r)
C = pdlmi(expr, relation, "UseFullBoxPreorder")
C = pdlmi(expr, relation, UseFullBoxPreorder=true)
C = pdlmi(expr, relation, FullBoxOrder=r)
C = pdlmi(expr, relation, UseFullBoxPreorder=true, FullBoxOrder=r)
C = pdlmi(expr, relation, UseFullBoxPreorder=false, FullBoxOrder=r)
C = lhs <= rhs
C = lhs >= rhs
\end{lstlisting}

\arghead
\begin{itemize}[leftmargin=*]
  \item \code{expr} is a square \code{pdvar} expression whose coefficient
        matrices are symmetric or Hermitian; \code{relation} is exactly
        \code{"<="} or \code{">="}.
  \item \code{UsePolya} is a logical scalar option with default
        \code{false}. Its bare form, \code{"UsePolya"}, and
        \code{UsePolya=true} without a degree select increment one.
  \item \code{PolyaDegree=d} is a finite nonnegative integer scalar. It
        elevates the residual by \code{d} in every parameter direction before
        assembling constraints. Supplying it without \code{UsePolya} enables
        P\'olya and issues \codeerr{pdlmi}{ImplicitUsePolya};
        \code{UsePolya=false} with positive \code{d} raises
        \codeerr{pdlmi}{ConflictingPolyaOptions}.
  \item \code{UsePutinar} is a logical scalar option with default
        \code{false}.  Its bare form and the explicit value \code{true} select
        the Putinar box module at the smallest admissible order.
  \item \code{PutinarOrder=r} is a finite nonnegative integer scalar and is an
        absolute tensor Bernstein Gram order, not a degree increment.  Its
        default and minimum are \(\lceil m/2\rceil\) for residual degree \(m\).
        Supplying the order without \code{UsePutinar} enables Putinar assembly.
        Pairing \code{UsePutinar=false} with any explicit order, including
        zero, raises \codeerr{pdlmi}{ConflictingPutinarOptions}.
  \item \code{UseFullBoxPreorder} is a logical scalar option with default
        \code{false}.  Its bare form and the explicit value \code{true} select
        full-box assembly at the smallest admissible order.
  \item \code{FullBoxOrder=r} is a finite nonnegative integer scalar and is an
        absolute order, not a degree increment.  Supplying it without
        \code{UseFullBoxPreorder} enables full-box assembly.  When full-box
        assembly is enabled, the minimum is \(\lfloor m/2\rfloor\) for a
        one-parameter residual of degree \(m\), and \(\lceil m/2\rceil\) for
        two or more parameters.  Explicitly pairing
        \code{UseFullBoxPreorder=false} with \code{FullBoxOrder=r} suppresses
        that automatic selection: \code{C} remains direct and records \code{r}
        only as inspectable metadata; the order is not applied to assembly.
  \item P\'olya, Putinar, and full-box selection are mutually exclusive.
        Enabling more than one
        raises \codeerr{pdlmi}{Conflicting}\allowbreak\code{Relaxations}.
  \item \code{C} is direct coefficient-wise assembly unless one opt-in mode is
        selected. Comparisons such as \code{lhs >= rhs} are the ordinary
        user-facing entry point and accept compatible numeric, \code{pdmat},
        affine \code{sdpvar}, and \code{pdvar} right-hand sides.
\end{itemize}

The inspectable properties \code{Constraints}, \code{Residual}, and
\code{Relation} expose the assembled finite constraints and the original
comparison data used for rebuilding.  \code{UsePolya}, \code{PolyaDegree},
\code{UsePutinar}, \code{PutinarOrder},
\code{UseFullBox}\allowbreak\code{Preorder}, and \code{FullBoxOrder} report the
selected mode and order metadata.  Their set access is private.
Comparison-created direct objects have all three selectors false and all three
numeric properties zero.  The full-box explicit-false combination described
above is the exception: it retains \code{FullBoxOrder=r} while
\code{UseFullBoxPreorder} remains false and the stored constraints remain
direct.  Putinar deliberately has no corresponding exception.

\examplehead
The direct constructor is equivalent to the comparison path when the relation
and options are explicit.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, {[0 1]}, "symmetric");
C = pdlmi(P, ">=", UsePolya=false, PolyaDegree=0);
class(C)
numel(C.Constraints)
C.UsePolya
C.PolyaDegree
C.UsePutinar
C.PutinarOrder
C.UseFullBoxPreorder
C.FullBoxOrder
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
    'pdlmi'

ans =
     2

ans =
  logical
   0

ans =
     0

ans =
  logical
   0

ans =
     0

ans =
  logical
   0

ans =
     0
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=9mm,
  box/.style={draw=black!55,fill=white,rounded corners=2pt,font=\scriptsize,
    align=center,minimum width=33mm,minimum height=11mm},
  result/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=37mm,minimum height=11mm}]
  \node[box] (lhs) {\code{lhs >= rhs}\\matrix comparison};
  \node[result,right=of lhs] (res) {\code{Residual = lhs-rhs}\\
    \code{Relation = ">="}};
  \node[result,right=of res] (con) {\code{Constraints}\\one per selected certificate};
  \draw[->,thick,dpblue] (lhs) -- (res);
  \draw[->,thick,dpblue] (res) -- (con);
\end{tikzpicture}
\end{center}

\refhead{Validation and errors}
The constructor rejects a non-\code{pdvar} residual with
\codeerr{pdlmi}{InvalidExpression}, a relation other than \code{"<="} or
\code{">="} with \codeerr{pdlmi}{InvalidRelation}, and malformed, unknown, or
duplicate options with \codeerr{pdlmi}{InvalidOptions},
\codeerr{pdlmi}{UnknownOption}, or \codeerr{pdlmi}{DuplicateOption}.
Nonlogical selectors raise
\codeerr{pdlmi}{InvalidUse}\allowbreak\code{Polya},
\codeerr{pdlmi}{InvalidUse}\allowbreak\code{Putinar}, or
\codeerr{pdlmi}{InvalidUse}\allowbreak\code{FullBoxPreorder}.  Malformed P\'olya increments,
Putinar orders, and full-box orders raise
\codeerr{pdlmi}{InvalidPolya}\allowbreak\code{Degree},
\codeerr{pdlmi}{InvalidPutinar}\allowbreak\code{Order}, and
\codeerr{pdlmi}{InvalidFullBox}\allowbreak\code{Order}, respectively.  A valid integer below the
active Putinar or full-box minimum raises
\codeerr{pdlmi}{PutinarOrder}\allowbreak\code{TooLow} or
\codeerr{pdlmi}{FullBoxOrder}\allowbreak\code{TooLow}.  Explicit false plus an explicit Putinar
order raises\linebreak[4]
\codeerr{pdlmi}{ConflictingPutinar}\allowbreak\code{Options};\newline
positive P\'olya
degree plus explicit false raises
\codeerr{pdlmi}{ConflictingPolya}\allowbreak\code{Options}.
The full-box explicit-false case remains direct, so its admissibility minimum
is not applied to retained order metadata.  Every assembly path retains the
square and symmetry checks, which raise \codeerr{pdlmi}{InvalidMatrixSize} and
\codeerr{pdlmi}{NonSymmetricExpression}.

\subsection{Direct Coefficient Assembly}
\label{sec:pdlmi-direct}

For each physical cell, local Bernstein coefficient, and rate-vertex row if
present, \code{pdlmi} creates one YALMIP semidefinite constraint.  For an
ordinary expression with \(N_c\) physical cells and \(N_b\) Bernstein
coefficients per cell, the number of stored constraints is \(N_cN_b\).  For a
\code{rhodiff} expression with \(N_v\) rate vertices, the count becomes
\(N_cN_bN_v\).

The idea is the Bernstein convex-hull property.  After orienting the relation
as a positive-semidefinite target
\[
  S_{\boldsymbol c,v}(\boldsymbol\alpha)=
  \sum_{\boldsymbol i}B_{\boldsymbol i}^m(\boldsymbol\alpha)
  C^{(\boldsymbol c,v)}[\boldsymbol i],
\]
the direct certificate requires
\(C^{(\boldsymbol c,v)}[\boldsymbol i]\succeq0\) for every physical cell
\(\boldsymbol c\), active rate vertex \(v\), and local label
\(\boldsymbol i\).  This is sufficient, not necessary.

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=5mm and 7mm,
  c/.style={draw=black!55,fill=white,rounded corners=2pt,font=\scriptsize,
    minimum width=14mm,minimum height=7mm},
  ok/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,minimum width=19mm,minimum height=7mm}]
  \node[c] (c0) {\(C^{(c)}[0]\)};
  \node[c,right=of c0] (c1) {\(C^{(c)}[1]\)};
  \node[c,right=of c1] (c2) {\(C^{(c)}[2]\)};
  \node[ok,below=of c0] (p0) {\(\succeq0\)};
  \node[ok,below=of c1] (p1) {\(\succeq0\)};
  \node[ok,below=of c2] (p2) {\(\succeq0\)};
  \draw[->,dpblue] (c0) -- (p0); \draw[->,dpblue] (c1) -- (p1);
  \draw[->,dpblue] (c2) -- (p2);
  \node[below=4pt of p1,font=\scriptsize]
    {repeat independently for each cell and rate row};
\end{tikzpicture}
\end{center}

\subsection{Opt-In P\'olya Assembly And \texorpdfstring{\code{applyPolya}}{applyPolya}}
\label{sec:pdlmi-polya}

With \code{UsePolya=true}, \code{pdlmi} first elevates the stored residual by
\code{PolyaDegree} in every parameter direction, then constrains every
elevated local coefficient and every active rate row. Thus, if the original
degree is \(m\), the count is
\(N_c (m+d+1)^\ell\) for ordinary expressions and
\(N_c (m+d+1)^\ell N_v\) for rate-vertex expressions. An increment of zero is
valid and has the same coefficient count as direct assembly.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
Cpolya = C.applyPolya()
Cpolya = C.applyPolya(d)
\end{lstlisting}

\code{applyPolya} is a value-class method: it returns a new \code{pdlmi},
leaves \code{C} unchanged, and rebuilds from \code{C.Residual}. Selecting a
new increment therefore replaces rather than compounds a previous selection.
The no-argument form uses one; an invalid increment raises
\code{pdlmi:InvalidPolyaDegree}.

\examplehead
\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(1, {[0 1]}, Degree=1);
direct = P >= 0;
polya = direct.applyPolya(2);
direct.PolyaDegree
polya.UsePolya
polya.PolyaDegree
numel(polya.Constraints)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
     0

ans =
  logical
   1

ans =
     2

ans =
     4
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=8mm,
  box/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=37mm,minimum height=11mm}]
  \node[box] (m) {degree \(m\) residual\\fixed polynomial};
  \node[box,right=of m] (e) {elevate by \(d\)\\degree \(m+d\)};
  \node[box,right=of e] (s) {test every elevated\\coefficient sign};
  \draw[->,thick,dpblue] (m) -- node[above,font=\scriptsize]
    {\(\prod_s((1-\alpha_s)+\alpha_s)^d=1\)} (e);
  \draw[->,thick,dpblue] (e) -- (s);
\end{tikzpicture}

\textit{P\'olya degree is a uniform representation increment, not a new
decision degree, a grid refinement, or a Gram order.}
\end{center}

The mathematical background and the limits of fixed-increment coefficient
positivity are developed in \cref{app:certificates}.

\subsection{Putinar Box Module And
\texorpdfstring{\code{applyPutinar}}{applyPutinar}}
\label{sec:pdlmi-putinar}

The Putinar mode is a fixed-order, box-specific Bernstein--Gram quadratic
module.  After normalizing the relation so that the target
\(F(\boldsymbol\alpha)\) is positive semidefinite, it represents
\[
  F(\boldsymbol\alpha)=S_0(\boldsymbol\alpha)
    +\sum_{s=1}^{\ell}\alpha_s(1-\alpha_s)
      S_s(\boldsymbol\alpha).
\]
Only the singleton box generators appear: products such as
\(\alpha_1(1-\alpha_1)\alpha_2(1-\alpha_2)\) belong to the full-box
preordering, not this module.

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
Cputinar = C.applyPutinar()
Cputinar = C.applyPutinar(r)
\end{lstlisting}

Here \code{r} is an absolute tensor Bernstein Gram order.  The no-argument
form selects \(r=\lceil m/2\rceil\), where \(m\) is the scalar degree of
\code{C.Residual}; an explicit order must satisfy the same lower bound.  The
unweighted block \(S_0\) uses tensor basis degree \(r\boldsymbol1\), while
\(S_s\) uses degree \(r\boldsymbol1-\boldsymbol e_s\).  A nominal block with a
negative component is omitted, which occurs for every multiplier at \(r=0\).
The target is degree-elevated without changing its represented polynomial, and
the Gram sum is matched exactly at tensor degree \(2r\).

\examplehead
\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(1, {[0 1]}, Degree=2);
direct = P >= 0;
putinar = direct.applyPutinar();
fprintf("direct: putinar=%d, order=%d, constraints=%d\n", ...
    direct.UsePutinar, direct.PutinarOrder, numel(direct.Constraints));
fprintf("selected: putinar=%d, order=%d, constraints=%d\n", ...
    putinar.UsePutinar, putinar.PutinarOrder, ...
    numel(putinar.Constraints));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
direct: putinar=0, order=0, constraints=3
selected: putinar=1, order=1, constraints=5
\end{lstlisting}

For this scalar degree-two cell, order one creates an unweighted Gram block of
dimension two and one generator-weighted block of dimension one, followed by
three exact coefficient identities.  Each physical cell and each active
rate-vertex row receives independent copies of these PSD blocks and identities.

\begin{center}
\begin{tikzpicture}[node distance=7mm and 9mm,
  n/.style={dp/neutral,font=\scriptsize,align=center,minimum width=28mm,
    minimum height=11mm},
  c/.style={dp/certificate,font=\scriptsize,align=center,minimum width=31mm,
    minimum height=11mm},
  r/.style={dp/result,font=\scriptsize,align=center,minimum width=29mm,
    minimum height=11mm}]
  \node[n] (target) {oriented residual\\elevate to degree \(2r\)};
  \node[c,right=of target] (grams) {PSD Gram blocks\\\(S_0\) and singleton \(S_s\)};
  \node[r,right=of grams] (match) {exact Bernstein\\coefficient identities};
  \draw[dp/arrow] (target) -- (grams);
  \draw[dp/arrow] (grams) -- (match);
\end{tikzpicture}

\textit{The method changes the finite certificate, not the stored residual,
decision degree, or physical grid.}
\end{center}

\code{applyPutinar} is a value-class method: it returns a new \code{pdlmi},
leaves \code{C} unchanged, and rebuilds from the original \code{Residual} and
\code{Relation}.  Reapplication replaces any earlier P\'olya, Putinar, or
full-box selection.  It adds no implicit margin and makes no solver call.

\refhead{Validation and errors}
\begingroup
\setlength{\emergencystretch}{1em}
Malformed orders raise
\codeerr{pdlmi}{Invalid}\allowbreak\code{Putinar}\allowbreak\code{Order}; an integer below
\(\lceil m/2\rceil\) raises
\codeerr{pdlmi}{Putinar}\allowbreak\code{Order}\allowbreak\code{TooLow}.  The
constructor rejects malformed selectors with
\codeerr{pdlmi}{InvalidUse}\allowbreak\code{Putinar}, explicit false plus any supplied order with
\codeerr{pdlmi}{ConflictingPutinar}\allowbreak\code{Options}, and simultaneous relaxation
families with \codeerr{pdlmi}{Conflicting}\allowbreak\code{Relaxations}.  The expression,
relation, option, square-matrix, and symmetry checks in
\cref{sec:pdlmi-construction} apply unchanged.
\par
\endgroup

\subsection{Full Box Bernstein--Gram Preordering And
\texorpdfstring{\code{applyFullBoxPreorder}}{applyFullBoxPreorder}}
\label{sec:pdlmi-full-box}

The full-box mode is a cell-local positive-semidefinite representation in the
forward local coordinates \(\alpha_s\in[0,1]\).  It is a box-specific Bernstein--Gram
preordering, not a general Putinar certificate, general-domain SOS interface,
or full-space SOS representation.  It adds no implicit positivity margin.

\needspace{10\baselineskip}
\syntaxhead
\begin{lstlisting}[style=dpsyntax]
Cbox = C.applyFullBoxPreorder()
Cbox = C.applyFullBoxPreorder(r)
\end{lstlisting}

The no-argument form selects the smallest admissible absolute order.  The
explicit form selects \code{r}; it does not add \code{r} to the residual
degree.  Both forms are value-class selectors: they return a new \code{pdlmi},
leave \code{C} unchanged, and rebuild from \code{C.Residual}.  Reapplication
therefore replaces an earlier full-box order or a P\'olya or Putinar selection
rather than compounding it.

The idea is to represent the oriented positive target as a sum of PSD Gram
forms multiplied by functions that are nonnegative on the local box:
\[
  S(\boldsymbol\alpha)=
  \sum_{J\subseteq\{1,\ldots,\ell\}}
  \left(\prod_{s\in J}\alpha_s(1-\alpha_s)\right)
  \mathcal Z_J(\boldsymbol\alpha)^{\mathsf T}
  Q_J\mathcal Z_J(\boldsymbol\alpha),
  \qquad Q_J\succeq0.
\]
In one parameter the implementation uses the even/odd Markov--Luk\'acs forms,
with minimum absolute order \(\lfloor m/2\rfloor\) for residual degree \(m\).
For two or more parameters it uses every subset product of the \(\ell\) box
generators and minimum order \(\lceil m/2\rceil\).  The selected order fixes the
dense Bernstein Gram bases; it is not added to \(m\).

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(1, {[0 1]}, Degree=2);
direct = P >= 0;
box = direct.applyFullBoxPreorder();
fprintf("direct constraints=%d\n", numel(direct.Constraints));
fprintf("full-box=%d, order=%d, constraints=%d\n", ...
    box.UseFullBoxPreorder, box.FullBoxOrder, numel(box.Constraints));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
direct constraints=3
full-box=1, order=1, constraints=5
\end{lstlisting}

At this order the scalar degree-two cell has two PSD Gram constraints followed
by three exact Bernstein coefficient identities.

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=7mm and 9mm,
  box/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=34mm,minimum height=12mm}]
  \node[box] (target) {cell-(c) target coefficients\\
    \(C^{(c)}[0],C^{(c)}[1],C^{(c)}[2]\)};
  \node[box,right=15mm of target,yshift=9mm] (q0) {unweighted Gram block\\\(Q_0\succeq0\)};
  \node[box,right=15mm of target,yshift=-9mm] (q1) {box-weighted block\\
    \(\alpha(1-\alpha)Q_1\succeq0\)};
  \node[box,right=14mm of q0,yshift=-9mm] (match) {exact coefficient\\identities};
  \draw[->,thick,dpblue] ([yshift=2.5mm]target.east) -- ++(5mm,0) |- (q0.west);
  \draw[->,thick,dpblue] ([yshift=-2.5mm]target.east) -- ++(5mm,0) |- (q1.west);
  \draw[->,thick,dpblue] (q0.east) -- ++(5mm,0) |- ([yshift=3mm]match.west);
  \draw[->,thick,dpblue] (q1.east) -- ++(5mm,0) |- ([yshift=-3mm]match.west);
\end{tikzpicture}
\end{center}

Appendix~\ref{app:certificates} derives the one-dimensional parity forms,
explains the multivariate subset masks, and distinguishes this implemented
  box preordering from the singleton-generator Putinar mode, full-space SOS,
  and general polynomial parsers.
Assembly is independent for every physical cell and every active rate-vertex
row; Gram variables are not shared across either boundary.  For \code{>=}, the
method represents the residual.  For \code{<=}, it negates the target before
forming the positive-semidefinite representation.  Within each cell and row,
the stored constraints are ordered as all PSD Gram blocks followed by exact
equalities matching every Bernstein coefficient.  Consequently, the number of
stored constraints is not the direct coefficient count.

\refhead{Validation and errors}
Malformed orders raise \codeerr{pdlmi}{InvalidFullBoxOrder}; an admissible
integer below the minimum raises \codeerr{pdlmi}{FullBoxOrderTooLow}.  The
constructor rejects malformed selectors with
\codeerr{pdlmi}{InvalidUse}\allowbreak\code{FullBox}\allowbreak\code{Preorder},
and simultaneous P\'olya, Putinar, or full-box selection raises\newline
\codeerr{pdlmi}{Conflicting}\allowbreak\code{Relaxations}.  The usual
expression, relation, option, square-matrix, and symmetry checks described in
\cref{sec:pdlmi-construction} apply unchanged.

\subsection{\texorpdfstring{\code{toYalmip}}{toYalmip}}
\label{sec:pdlmi-toyalmip}

\syntaxhead
\begin{lstlisting}[style=dpsyntax]
F = toYalmip(C)
F = C.toYalmip()
\end{lstlisting}

\arghead
\code{toYalmip} concatenates the stored constraint cells into a YALMIP
constraint array for direct, P\'olya, Putinar, and full-box objects.  It has no
package-specific options and does not add a solver wrapper, objective, margin,
or diagnostic layer.  The output is suitable for \code{optimize},
concatenation with other YALMIP constraints, or ordinary YALMIP inspection.

\examplehead
The function-call and dot-call forms return equivalent YALMIP constraint
arrays.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(2, {[0 1]}, "symmetric");
C = P >= 0;
class(C)
numel(C.Constraints)
F1 = toYalmip(C);
F2 = C.toYalmip();
isa(F1, "lmi") || isa(F1, "constraint")
length(F1)
isa(F2, "lmi") || isa(F2, "constraint")
length(F2)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
    'pdlmi'

ans =
     2

ans =
  logical
   1

ans =
     2

ans =
  logical
   1

ans =
     2
\end{lstlisting}

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=8mm,
  box/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=34mm,minimum height=11mm}]
  \node[box] (cells) {\code{C.Constraints}\\cell/rate/label order};
  \node[box,right=18mm of cells] (flat) {YALMIP constraint array\\\code{F=toYalmip(C)}};
  \node[box,right=of flat] (solve) {\code{optimize(F,...)}\\solver-owned status};
  \draw[->,thick,dpblue] (cells) -- node[above,font=\scriptsize]{concatenate} (flat);
  \draw[->,thick,dpblue] (flat) -- (solve);
\end{tikzpicture}
\end{center}

After this boundary, the package is no longer assembling Bernstein data; YALMIP
owns solver choice, diagnostics, and optimization status.

\subsection{See Also}
\code{pdvar}, \code{rhodiff}, \code{applyPolya}, \code{applyPutinar},
\code{applyFullBoxPreorder}, \code{toYalmip}, \code{optimize}.

\needspace{13\baselineskip}
\subsection{Solver-Facing Use}
\label{sec:pdlmi-solver-use}

\code{pdlmi} does not own solver settings or wrap \code{optimize}.  After
\code{toYalmip}, use ordinary YALMIP:

\begin{lstlisting}[style=dpsyntax]
opts = sdpsettings("solver", solverName, "verbose", 0);
sol = optimize([C1.toYalmip, C2.toYalmip], objective, opts);
\end{lstlisting}

The solver selected by YALMIP must be available on the MATLAB path.  The current
test suite prefers MOSEK when available and otherwise falls back to LMILAB for
smoke coverage.

\section{Cross-Class Helper Reference}
\label{sec:helpers}

The \code{+helper} namespace contains exactly nine implementation utilities
whose ownership genuinely crosses class boundaries.  They are developer-facing
building blocks, not an alternative modeling API: ordinary users should enter
through \code{pdbase}, \code{pdmat}, \code{pdvar}, and \code{pdlmi}.  Their
signatures and current consumers are listed below so maintainers can distinguish
shared contracts from class-local implementation details.

\begin{enumerate}[leftmargin=*,itemsep=0.65em]
  \item \code{helper.bernTbl(...)}\\
        Builds ordinary, rate-vertex, or one-line Bernstein inspection tables
        with caller-supplied value and expression formatters.
        \textit{Consumers:} \code{pdmat}, \code{pdvar}.
  \item \code{helper.cellGet(vals, subs)}\\
        Follows one tensor-subscript row through nested \code{LocalValues} and
        returns the selected leaf.  \textit{Consumers:} all four classes.
  \item \code{helper.chk(val, id, msg, ...)}\\
        Applies shared predicate tags and bounds, returning the input or
        raising the caller's error ID.  \textit{Consumers:} all four classes.
  \item \code{helper.combRows(vecs)}\\
        Returns Cartesian-product rows with earlier dimensions varying more
        slowly.  \textit{Consumers:} all four classes.
  \item \code{helper.isZero(val, mode, ...)}\\
        Classifies numeric, additive, nested-payload, or coefficient-backed
        object zero evidence.  \textit{Consumers:} \code{pdmat}, \code{pdvar}.
  \item \code{helper.mapVals(vals, fcn, grid)}\\
        Maps one function over every coefficient in a nested cell tree.
        \textit{Consumers:} \code{pdmat}, \code{pdvar}.
  \item \code{helper.matSubs(subs, sz, id)}\\
        Normalizes numeric, logical, or colon matrix subscripts into row and
        column indices.  \textit{Consumers:} \code{pdmat}, \code{pdvar}.
  \item \code{helper.mkGrid(grid, owner)}\\
        Validates tensor-grid vectors and builds \code{GridInfo} with vectors,
        points, bounds, and node counts.  \textit{Consumers:} \code{pdbase},
        \code{pdmat}, \code{pdvar}.
  \item \code{helper.mkNest(counts, fcn[, prefix])}\\
        Constructs recursive tensor storage and calls the leaf factory with
        each complete subscript row.  \textit{Consumers:} \code{pdbase},
        \code{pdmat}, \code{pdvar}.
\end{enumerate}

\subsection{Shared Validation And Errors}

\code{helper.chk} recognizes the predicate tags \code{numeric}, \code{real},
\code{cell}, \code{struct}, \code{nonempty}, \code{scalar}, \code{vector},
\code{matrix}, \code{finite}, \code{integer}, \code{positive},
\code{nonnegative}, \code{increasing}, and \code{rowbounds}; its named tests are
\code{Size}, \code{Numel}, \code{MinNumel}, \code{Min}, and \code{Max}.
Failed user-data checks raise the supplied class error ID.  An unknown tag or a
missing validator-option value raises
\codeerr{helper}{Invalid}\allowbreak\code{ValidatorCall}.

\code{helper.isZero} accepts modes \code{"num"}, \code{"add"},
\code{"vals"}, and \code{"obj"}.  Unknown modes raise
\codeerr{helper}{Invalid}\allowbreak\code{ZeroMode}.  A wrong number of
mode-specific inputs raises
\codeerr{helper}{Invalid}\allowbreak\code{ZeroCall}.
\code{helper.matSubs} and
\code{helper.bernTbl} deliberately receive a caller-owned error ID, so public
indexing and table errors remain in the owning class namespace.

\subsection{Class-Private Helpers}

Single-class mechanics remain beside their owner rather than in
\code{+helper}.  The private area of \code{pdbase} owns coefficient-tree
validation.  The private area of \code{pdmat} owns source fitting, continuity,
grid conversion, matrix scanning, and algebraic wrapping.  The private area of
\code{pdvar} owns affine and rate-row packing, product routing, value-tree
zipping, and symbolic wrapping.  The private area of \code{pdlmi} owns direct
coefficient, Putinar, and full-box assembly, Gram conversion, and order
validation.
These functions have no supported direct call form.  Their placement is an
ownership boundary: a helper should move into a class when only that class can
meaningfully use it, and should enter \code{+helper} only after a real
cross-class contract exists.

\section{Examples}
\label{sec:examples}

\subsection{Deterministic Putinar And Full-Box Selection}
\label{sec:full-box-example}

This example selects default Putinar and full-box certificates and an explicit
full-box order without solving an optimization problem.  The reported values
depend only on the public assembly contract, not on incidental YALMIP
decision-variable identifiers.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(1, {[0 1]}, Degree=2);
direct = P >= 0;
putinarDefault = direct.applyPutinar();
boxDefault = direct.applyFullBoxPreorder();
boxHigher = boxDefault.applyFullBoxPreorder(2);
polya = direct.applyPolya(1);
boxFromPolya = polya.applyFullBoxPreorder();
putinarFromBox = boxHigher.applyPutinar();
Fbox = toYalmip(boxDefault);

fprintf("direct: fullBox=%d, order=%d, constraints=%d\n", ...
    direct.UseFullBoxPreorder, direct.FullBoxOrder, ...
    numel(direct.Constraints));
fprintf("default: fullBox=%d, order=%d, constraints=%d\n", ...
    boxDefault.UseFullBoxPreorder, boxDefault.FullBoxOrder, ...
    numel(boxDefault.Constraints));
fprintf("Putinar: selected=%d, order=%d, constraints=%d\n", ...
    putinarDefault.UsePutinar, putinarDefault.PutinarOrder, ...
    numel(putinarDefault.Constraints));
fprintf("higher: order=%d, constraints=%d\n", ...
    boxHigher.FullBoxOrder, numel(boxHigher.Constraints));
fprintf("replacement: polya=%d, fullBox=%d, order=%d\n", ...
    boxFromPolya.UsePolya, boxFromPolya.UseFullBoxPreorder, ...
    boxFromPolya.FullBoxOrder);
fprintf("replace box: putinar=%d, fullBox=%d, order=%d\n", ...
    putinarFromBox.UsePutinar, putinarFromBox.UseFullBoxPreorder, ...
    putinarFromBox.PutinarOrder);
fprintf("exported constraints=%d\n", length(Fbox));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
direct: fullBox=0, order=0, constraints=3
default: fullBox=1, order=1, constraints=5
Putinar: selected=1, order=1, constraints=5
higher: order=2, constraints=7
replacement: polya=0, fullBox=1, order=1
replace box: putinar=1, fullBox=0, order=1
exported constraints=5
\end{lstlisting}

For the degree-two scalar residual, order one uses two PSD Gram blocks followed
by three exact coefficient identities.  Order two matches a degree-four target
and stores two PSD blocks followed by five identities.  The original
\code{direct} object remains unchanged, and the selection made from
\code{polya} replaces P\'olya rather than elevating its already assembled
constraints.  Likewise, selecting Putinar from \code{boxHigher} rebuilds from
the original residual and clears the full-box selection.

\subsection{Parameter-Dependent Lyapunov Solver Smoke}
\label{sec:parameter-dependent-lyapunov-smoke}

The first solver smoke test in \code{+tests/+pdlmi/test_solver_smoke.m}
contrasts a constant Lyapunov matrix with a degree-one, parameter-dependent
one.  It solves the same decay and positivity constraints in both cases.  The
degree-one solution is then materialized as known \code{pdmat} data and
plotted.  \code{pdvar} is a decision-expression class and does not itself
provide \code{plot}; applying \code{value} to its local YALMIP payloads after
a successful solve supplies the numeric Bernstein coefficients for
\code{pdmat}.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
grid = {[0 1]};
A = pdmat(grid, @(x) (1 - x) * [-1 -1; 1 -1] + ...
    x * [-1 -10; 0.1 -1], Degree=1);
solver = 'lmilab';
if exist('mosekopt', 'file') ~= 0
    solver = 'mosek';
end
opts = sdpsettings('solver', solver, 'verbose', 0);

Pc = pdvar(2, grid, Degree=0);
CconstantDecay = Pc * A + A' * Pc <= -1e-10 * eye(2);
CconstantPositive = Pc >= eye(2);
solConstant = optimize([CconstantDecay.toYalmip, ...
    CconstantPositive.toYalmip], [], opts);

Pd = pdvar(2, grid);
CdependentDecay = Pd * A + A' * Pd <= -1e-10 * eye(2);
CdependentPositive = Pd >= eye(2);
solDependent = optimize([CdependentDecay.toYalmip, ...
    CdependentPositive.toYalmip], [], opts);
assert(solDependent.problem == 0, solDependent.info)

Pplot = pdmat(grid, ...
    {cellfun(@value, Pd.LocalValues{1}, UniformOutput=false)}, ...
    Degree=Pd.Degree);
figure(Name="Parameter-dependent Lyapunov matrix")
plot(Pplot, SamplesPerCell=40, LineWidth=1.5)
title("Solved parameter-dependent Lyapunov matrix")
\end{lstlisting}

The figure contains one curve for each entry of the solved \(2\)-by-\(2\)
matrix \(P(\rho)\), sampled on the parameter interval.  It is a diagnostic
visualization of the feasible solution, not a certificate beyond the
coefficient-wise constraints supplied to the solver.  The constant-case result
is retained to exercise the comparison in the smoke test; only a MOSEK solve
is used there to certify its intended infeasibility distinction.

\subsection{Rate-Dependent Block LMI Solver Smoke}
\label{sec:solver-example}

This example is adapted from \code{+tests/+pdlmi/test_solver_smoke.m}.  The
goal is to find a parameter-dependent \(P(\rho)\) and scalar \(\gamma\) such
that, on every Bernstein coefficient and rate vertex,
\[
\begin{bmatrix}
\dot P + PA + A^\top P & PB & C^\top\\
B^\top P & -\gamma I & D^\top\\
C & D & -\gamma I
\end{bmatrix}
\preceq 0,
\qquad
P(\rho)\succeq 0.
\]
The derivative term \(\dot P\) is built with \code{rhodiff(P, [-1 1])}.  The
comparison operators create \code{pdlmi} wrappers, and \code{toYalmip} hands
the coefficient-wise constraints to YALMIP.  In the second listing, the
\code{fprintf} call prints the solver- and tolerance-dependent numeric value of
\(\gamma\).  For this model, \code{numel(E1.Constraints)} returns (6), and
\code{numel(E2.Constraints)} returns (2).

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
grid = linspace(0, 1, 2);
A = pdmat(grid, @(x) [-1, 0.5; -1, -2] + ...
    x * [-1.3, -20; 2, -10], Degree=1);
B = pdmat(grid, @(x) [1, -4; -1, -1] + ...
    x * [2.2, 0.5; -6, -5], Degree=1);
Cmat = eye(2);
Dmat = zeros(2);

P = pdvar(2, grid);
diffP = rhodiff(P, [-1 1]);
gamma = pdvar(1, grid, Degree=0);
E1 = [diffP + P * A + A' * P, P * B, Cmat';
      B' * P, -gamma * eye(2), Dmat';
      Cmat, Dmat, -gamma * eye(2)] <= 0;
E2 = P >= 0;
numel(E1.Constraints)
numel(E2.Constraints)
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
ans =
     6

ans =
     2
\end{lstlisting}

\begin{lstlisting}[
  style=dpmatlab,
  title={\textcolor{dpblue}{\sffamily\bfseries\footnotesize MATLAB input (continued)}}
]
objective = gamma.LocalValues{1}{1};
solver = 'lmilab';
if exist('mosekopt', 'file') ~= 0
    solver = 'mosek';
end
opts = sdpsettings('solver', solver, 'verbose', 0);
sol = optimize([E1.toYalmip, E2.toYalmip], objective, opts);
assert(sol.problem == 0, sol.info)
gammaValue = value(objective);
assert(isfinite(gammaValue))
fprintf("Optimal H-infinity gamma: %.6g\n", gammaValue)
\end{lstlisting}

The constraint counts above follow directly from the three independent
assembly axes: physical-cell identity, rate-row identity, and local Bernstein
label.  In this tested example the two wrappers use the same physical grid but
different rate-row and degree structures.

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=7mm and 10mm,
  lane/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=64mm,minimum height=12mm,
    inner sep=4pt},
  axes/.style={draw=black!55,fill=white,rounded corners=2pt,
    font=\scriptsize,align=left,minimum width=64mm,minimum height=24mm,
    inner sep=5pt},
  count/.style={draw=orange!80!black,fill=orange!10,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=64mm,minimum height=12mm,
    inner sep=4pt},
  merge/.style={draw=dpblue!85!black,fill=dpblue!12,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=75mm,minimum height=12mm,
    inner sep=4pt}]
  \node[lane] (e1) {\code{E1}: rate-dependent block residual\\
    \(R^{(c,v)}[i]\preceq0\)};
  \node[lane,right=of e1] (e2) {\code{E2}: ordinary positivity residual\\
    \(P^{(c)}[i]\succeq0\)};

  \node[axes,below=of e1] (a1) {
    physical cells: \(c\in\{1\}\) \hfill (1)\\
    rate rows: \(v\in\{-,+\}\) \hfill (2)\\
    degree-two local labels: \(i\in\{0,1,2\}\) \hfill (3)};
  \node[axes,below=of e2] (a2) {
    physical cells: \(c\in\{1\}\) \hfill (1)\\
    ordinary row (no rate enumeration) \hfill (1)\\
    degree-one local labels: \(i\in\{0,1\}\) \hfill (2)};

  \node[count,below=of a1] (n1)
    {\(1\times2\times3=6\) finite LMIs};
  \node[count,below=of a2] (n2)
    {\(1\times1\times2=2\) finite LMIs};

  \node[merge,below=14mm of n1,xshift=37mm] (export)
    {\code{E1.toYalmip} and \code{E2.toYalmip}\\
     enter one finite YALMIP model};
  \node[merge,below=of export] (solve)
    {\code{optimize([E1.toYalmip, E2.toYalmip], objective, opts)}};

  \draw[->,thick,dpblue] (e1)--(a1);
  \draw[->,thick,dpblue] (e2)--(a2);
  \draw[->,thick,dpblue] (a1)--(n1);
  \draw[->,thick,dpblue] (a2)--(n2);
  \draw[->,thick,dpblue] (n1)--(export);
  \draw[->,thick,dpblue] (n2)--(export);
  \draw[->,thick,dpblue] (export)--(solve);
\end{tikzpicture}
\end{center}

The numbers (6) and (2) are certificate-assembly counts: they state how
many finite semidefinite constraints represent \code{E1} and \code{E2}.
They are not evidence that the optimization succeeds.  Feasibility is checked
separately by \code{sol.problem}, and the finite objective is checked after the
shared \code{optimize} call.

\appendix

\section{Bernstein Basis Mathematics}
\label{app:bernstein-math}

This appendix develops the representation used by the package from the
one-dimensional degree-one case to cell-local tensor polynomials.  The reader
needs only scalar polynomials, matrices, and elementary calculus.  Every
conversion, elevation, product, derivative, and subdivision below is exact;
none is a sampling approximation.  Farouki gives a broader historical and
numerical account \cite{farouki2012}.

Each main construction follows the same reading path: \emph{Basic idea},
\emph{Formula}, \emph{Worked example}, and \emph{Visual conclusion}.  The
extra staging is intentional: first identify what the operation means, then
read its indices, and only afterward connect it to package storage.

\subsection{Convex Combinations Before Polynomials}

\ideastep{Basic idea}
A convex combination of numbers or matrices has the form
\[
  X=\sum_{i=0}^{m}w_iX_i,\qquad
  w_i\ge0,\qquad \sum_{i=0}^{m}w_i=1.
\]
For scalars, \(X\) lies between the smallest and largest \(X_i\).  For symmetric
matrices, if every \(X_i\succeq0\), then \(X\succeq0\), because
\[
  z^{\mathsf T}Xz=\sum_iw_i\,z^{\mathsf T}X_i z\ge0
  \quad\text{for every }z.
\]
\ideastep{Formula}
Bernstein bases are useful because their basis values are precisely such
weights at every point of a cell.

\ideastep{Worked example}
Take \(X_0=1\), \(X_1=4\), and weights \(0.75\) and \(0.25\).  Their
combination is \(1.75\), which remains between the two inputs.

\ideastep{Visual conclusion}

\begin{center}
\begin{tikzpicture}[x=1.25cm,y=1cm,>=Latex,
  p/.style={draw=dpblue!75!black,fill=dpbluewash,circle,inner sep=1.7pt}]
  \draw[very thick,black!55] (0,0) -- (6,0);
  \node[p,label=above:{\(X_0=1\)}] at (0,0) {};
  \node[p,label=above:{\(X_1=4\)}] at (6,0) {};
  \node[p] at (1.5,0) {};
  \node[font=\scriptsize,anchor=south] at (3,0.75)
    {\(0.75X_0+0.25X_1=1.75\)};
  \draw[->,dpblue,thick] (0.15,-0.45) -- (1.35,-0.1);
  \draw[->,dpblue,thick] (5.85,-0.45) -- (1.65,-0.1);
  \node[font=\scriptsize] at (3,-0.82)
    {nonnegative weights keep the result inside the coefficient hull};
\end{tikzpicture}
\end{center}

The implication is one-way.  A polynomial may be positive everywhere while one
Bernstein coefficient is negative; Appendix~\ref{app:certificates} uses that
fact to motivate stronger certificates.

\subsection{Degree-One Interpolation On One Physical Cell}

\ideastep{Basic idea}
Let a physical cell be
\([\rho_{c-1},\rho_c]\), with width \(h_c=\rho_c-\rho_{c-1}>0\).  Its forward
local coordinate is
\[
  \alpha=\frac{\rho-\rho_{c-1}}{h_c}\in[0,1].
\]
\ideastep{Formula}
The degree-one Bernstein basis is
\[
  B_0^1(\alpha)=1-\alpha,\qquad B_1^1(\alpha)=\alpha.
\]
Therefore
\[
  M_c(\rho)=(1-\alpha)C^{(c)}[0]+\alpha C^{(c)}[1].
\]
At the lower physical face, \(\alpha=0\) and the value is \(C^{(c)}[0]\); at
the upper face, \(\alpha=1\) and the value is \(C^{(c)}[1]\).  Between them the
matrix follows the straight segment joining those two coefficient matrices.

\ideastep{Worked example}
For example, \(C^{(c)}[0]=2\) and \(C^{(c)}[1]=4\) give
\(M_c(0.25)=0.75(2)+0.25(4)=2.5\), exactly as in
\cref{sec:pdmat-evaluate}.

\ideastep{Visual conclusion}
\begin{center}
\begin{tikzpicture}[x=5.3cm,y=1.1cm,>=Latex]
  \draw[->,black!60] (-0.05,0) -- (1.08,0) node[right,font=\scriptsize]{\(\alpha\)};
  \draw[->,black!60] (0,-0.05) -- (0,1.08) node[above,font=\scriptsize]{basis weight};
  \draw[very thick,dpblue] plot[domain=0:1,samples=40] (\x,{1-\x});
  \draw[very thick,orange!80!black] plot[domain=0:1,samples=40] (\x,{\x});
  \node[dpblue,font=\scriptsize,anchor=west] at (0.12,0.84) {\(B_0^1=1-\alpha\)};
  \node[orange!80!black,font=\scriptsize,anchor=west] at (0.60,0.72) {\(B_1^1=\alpha\)};
  \foreach \x in {0,1}{\draw (\x,0.03)--(\x,-0.03)
    node[below,font=\scriptsize]{\(\x\)};}
\end{tikzpicture}
\end{center}

\subsection{Higher Degree And The Partition Of Unity}

\ideastep{Basic idea}
Higher degree adds interior shape controls while keeping the same physical
cell.  It does not add physical grid nodes.

\ideastep{Formula}
For degree \(m\), define
\[
  B_i^m(\alpha)=\binom mi(1-\alpha)^{m-i}\alpha^i,
  \qquad i=0,\ldots,m.
\]
Each basis function is nonnegative on \([0,1]\).  The binomial theorem gives
\[
  \sum_{i=0}^{m}B_i^m(\alpha)
  =((1-\alpha)+\alpha)^m=1.
\]
Thus
\[
  M_c(\alpha)=\sum_{i=0}^{m}B_i^m(\alpha)C^{(c)}[i]
\]
is a convex combination of its local coefficient matrices.

\ideastep{Worked example}
For degree two,
\[
  B_0^2=(1-\alpha)^2,\qquad
  B_1^2=2(1-\alpha)\alpha,\qquad
  B_2^2=\alpha^2.
\]

\ideastep{Visual conclusion}
\begin{center}
\begin{tikzpicture}[x=5.3cm,y=2.0cm,>=Latex]
  \draw[->,black!60] (-0.05,0) -- (1.08,0) node[right,font=\scriptsize]{\(\alpha\)};
  \draw[->,black!60] (0,-0.03) -- (0,1.08) node[above,font=\scriptsize]{\(B_i^2\)};
  \draw[very thick,dpblue] plot[domain=0:1,samples=60] (\x,{(1-\x)^2});
  \draw[very thick,orange!80!black] plot[domain=0:1,samples=60]
    (\x,{2*(1-\x)*\x});
  \draw[very thick,green!45!black] plot[domain=0:1,samples=60] (\x,{\x^2});
  \node[dpblue,font=\scriptsize] at (0.12,0.78) {\(B_0^2\)};
  \node[orange!80!black,font=\scriptsize] at (0.50,0.57) {\(B_1^2\)};
  \node[green!45!black,font=\scriptsize] at (0.87,0.78) {\(B_2^2\)};
  \foreach \x in {0,0.5,1}{\draw (\x,0.02)--(\x,-0.02)
    node[below,font=\scriptsize]{\(\x\)};}
\end{tikzpicture}

\textit{At every \(\alpha\), the three plotted heights are nonnegative and sum
to one.}
\end{center}

The label \(i\) identifies a basis position inside the selected physical cell.
It is not a physical grid-node subscript.

\subsection{Worked Conversion: \texorpdfstring{\(1+2\alpha+3\alpha^2\)}
{1+2 alpha+3 alpha squared}}

Consider
\[
  p(\alpha)=1+2\alpha+3\alpha^2.
\]
Seek degree-two Bernstein coefficients \(b_0,b_1,b_2\):
\[
  p=b_0(1-\alpha)^2+2b_1(1-\alpha)\alpha+b_2\alpha^2.
\]
Matching powers of \(\alpha\) gives
\[
  b_0=1,\qquad b_1=2,\qquad b_2=6.
\]
Indeed,
\[
  1B_0^2+2B_1^2+6B_2^2
  =(1-2\alpha+\alpha^2)+(4\alpha-4\alpha^2)+6\alpha^2
  =1+2\alpha+3\alpha^2.
\]
The three values \(1,2,6\) are coefficients of basis functions, not samples at
three equally spaced physical points.  Only the two endpoint coefficients are
endpoint values.  At \(\alpha=1/2\),
\[
  p(1/2)=\tfrac14(1)+\tfrac12(2)+\tfrac14(6)=2.75.
\]

\begin{lstlisting}[style=dpmatlab]
p = pdmat({[0 1]}, {1, 2, 6}, Degree=2);
fprintf("p(0.5)=%.2f\n", p.evaluate(0.5));
disp(bernsteinTable(p, "oneLine"))
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
p(0.5)=2.75
    CellSubscript                    Expression
    _____________    __________________________________________

        {[1]}        "(1-alpha)^2*1 + 2(1-alpha)alpha*2 + alpha^2*6"
\end{lstlisting}

\subsection{Exact Power--Bernstein Conversion}

More generally, use brackets for algebraic coefficient labels as well:
\[
  p(\alpha)=\sum_{j=0}^{n}a[j]\alpha^j
  =\sum_{k=0}^{n}b[k]B_k^n(\alpha).
\]
Power coefficients convert to Bernstein coefficients by
\[
  b[k]=\sum_{j=0}^{k}
  \frac{\binom{k}{j}}{\binom{n}{j}}a[j],
  \qquad k=0,\ldots,n.
\]
The inverse map is
\[
  a[j]=\binom{n}{j}\sum_{i=0}^{j}
  (-1)^{j-i}\binom{j}{i}b[i],
  \qquad j=0,\ldots,n.
\]
These are changes of basis inside the same degree-\(n\) polynomial space.
For matrix polynomials they act entrywise.  For tensor polynomials they act
along one coordinate axis at a time.

\subsection{Lossless Degree Elevation}

A polynomial on physical cell (c) with degree (m) also has a Bernstein
representation of every higher degree.  One elevation step gives
\[
  \widehat C^{(c)}[0]=C^{(c)}[0],\qquad
  \widehat C^{(c)}[k]=
    \frac{k}{m+1}C^{(c)}[k-1]
    +\left(1-\frac{k}{m+1}\right)C^{(c)}[k],\qquad
  \widehat C^{(c)}[m+1]=C^{(c)}[m].
\]
For the worked coefficients \([1,2,6]\), elevation from degree two to degree
three produces
\[
  \left[1,\frac53,\frac{10}{3},6\right].
\]

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=6mm and 8mm,
  c/.style={draw=black!55,fill=white,rounded corners=2pt,font=\scriptsize,
    minimum width=12mm,minimum height=7mm},
  e/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,minimum width=14mm,minimum height=7mm}]
  \node[c] (c0) {\(1\)}; \node[c,right=of c0] (c1) {\(2\)};
  \node[c,right=of c1] (c2) {\(6\)};
  \node[e,below=10mm of c0] (e0) {\(1\)};
  \node[e,right=of e0] (e1) {\(5/3\)};
  \node[e,right=of e1] (e2) {\(10/3\)};
  \node[e,right=of e2] (e3) {\(6\)};
  \draw[->,dpblue] (c0)--(e0); \draw[->,dpblue] (c0)--(e1);
  \draw[->,dpblue] (c1)--(e1); \draw[->,dpblue] (c1)--(e2);
  \draw[->,dpblue] (c2)--(e2); \draw[->,dpblue] (c2)--(e3);
  \node[above=4pt of c1,font=\scriptsize]{degree 2};
  \node[below=4pt of e1,font=\scriptsize]{degree 3, same polynomial};
\end{tikzpicture}
\end{center}

Direct elevation from \(m\) to \(M\ge m\) is
\[
  \widehat C^{(c)}[k]=
  \sum_{i=\max(0,k-M+m)}^{\min(m,k)}
  C^{(c)}[i]\frac{\binom mi\binom{M-m}{k-i}}{\binom Mk}.
\]
The new coefficients are fixed convex combinations of the old ones.  Elevation
therefore changes representation but adds no decision freedom.  Constructing a
new higher-degree \code{pdvar} does enlarge the decision parameterization.

\subsection{Physical Tensor Grids And Local Tensor Labels}

Suppose direction \(s\) has physical nodes
\[
  \rho_{s,0}<\rho_{s,1}<\cdots<\rho_{s,N_s-1}.
\]
The counts \(N_s\) may differ.  A physical node is identified by the
\(\ell\)-vector \(\boldsymbol k\); a physical cell is identified separately by
\(\boldsymbol c=(c_1,\ldots,c_\ell)\).  On that cell,
\[
  \alpha_s=
  \frac{\rho_s-\rho_{s,c_s-1}}
       {\rho_{s,c_s}-\rho_{s,c_s-1}},
  \qquad s=1,\ldots,\ell.
\]
For the package's scalar degree \(m\),
\[
  M_{\boldsymbol c}(\boldsymbol\alpha)=
  \sum_{\boldsymbol i\in\{0,\ldots,m\}^{\ell}}
  B_{\boldsymbol i}^{m}(\boldsymbol\alpha)
  C^{(\boldsymbol c)}[\boldsymbol i],
  \qquad
  B_{\boldsymbol i}^{m}=
  \prod_{s=1}^{\ell}B_{i_s}^{m}(\alpha_s).
\]
There are \(\prod_s(N_s-1)\) physical cells and \((m+1)^\ell\) local
coefficients per cell.

\begin{lstlisting}[style=dpmatlab]
obj = pdbase({[0 1 2], [10 20 30 40]}, [1 1], 1);
fprintf("physical cells=%d, local coefficients/cell=%d\n", ...
    obj.ncell(), obj.ncoeff());
disp(obj.lbls())
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
physical cells=6, local coefficients/cell=4
     0     0
     0     1
     1     0
     1     1
\end{lstlisting}

The nested tree
\code{LocalValues\{c1\}\{c2\}...\{cell\}} follows physical-cell coordinates.
Only after reaching a leaf does the flat coefficient cell follow the
\code{lbls} order.  Earlier dimensions vary more slowly in both
\code{cells} and \code{lbls}.

\begin{center}
\begin{tikzpicture}[node distance=11mm,
  box/.style={dp/neutral,font=\scriptsize,align=center,
    minimum width=52mm,minimum height=12mm,inner sep=5pt},
  result/.style={dp/result,font=\scriptsize,align=center,
    minimum width=82mm,minimum height=15mm,inner sep=5pt}]
  \node[box] (tree) {physical cell \((2,3)\)\\
    \code{LocalValues\{2\}\{3\}}};
  \node[result,below=of tree] (leaf) {local coefficient labels\\[2pt]
    \(C^{(2,3)}[0,0]\), \(C^{(2,3)}[0,1]\),
    \(C^{(2,3)}[1,0]\), \(C^{(2,3)}[1,1]\)};
  \draw[dp/arrow] (tree) -- (leaf);
\end{tikzpicture}

\textit{Select the physical-cell leaf first; only then read the flat local
coefficient labels in \code{lbls} order.}
\end{center}

\subsection{Products As Ordered Weighted Convolution}
\label{sec:bernstein-product-math}

\ideastep{Basic idea}
Multiplying two Bernstein polynomials creates every ordered pair of input
coefficients.  Pairs with the same label sum contribute to the same output.
For matrix payloads, ``ordered'' matters: the left factor stays on the left.

\ideastep{Formula}
Fix a physical cell (c), and let
\[
  P=\sum_{i=0}^{m}B_i^mP^{(c)}[i],\qquad
  Q=\sum_{j=0}^{n}B_j^nQ^{(c)}[j].
\]
The basis-product identity
\[
  B_i^mB_j^n=
  \frac{\binom mi\binom nj}{\binom{m+n}{i+j}}B_{i+j}^{m+n}
\]
gives
\[
  (PQ)^{(c)}[k]=\sum_{i+j=k}P^{(c)}[i]Q^{(c)}[j]
  \frac{\binom mi\binom nj}{\binom{m+n}{k}}.
\]
This is an ordered matrix convolution.  In general
\(P^{(c)}[i]Q^{(c)}[j]\ne Q^{(c)}[j]P^{(c)}[i]\).

\ideastep{Worked example}
Take quadratic coefficients \(P=[1,2,6]\) and affine coefficients
\(Q=[2,4]\).  The anti-diagonals give
\[
\begin{aligned}
  R[0]&=1(2)=2,\\
  R[1]&=\bigl(1(4)+2\,2(2)\bigr)/3=4,\\
  R[2]&=\bigl(2\,2(4)+6(2)\bigr)/3=28/3,\\
  R[3]&=6(4)=24.
\end{aligned}
\]

\begin{lstlisting}[style=dpmatlab]
P = pdmat({[0 1]}, {1, 2, 6}, Degree=2);
Q = pdmat({[0 1]}, {2, 4}, Degree=1);
R = P * Q;
c = R.coeffs(1);
fprintf("product degree=%d\n", R.Degree);
disp([c{:}])
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
product degree=3
    2.0000    4.0000    9.3333   24.0000
\end{lstlisting}

The product degree in the transcript is three: degrees add, so
\(2+1=3\).  The four outputs are coefficients in that cubic Bernstein basis,
not values sampled at four points.

\ideastep{Visual conclusion}
\begin{center}
\begin{tikzpicture}[node distance=11mm,
  panel/.style={minimum width=42mm,minimum height=34mm,font=\scriptsize,
    align=left,inner sep=5pt}]
  \node[panel,dp/neutral] (input) {\textbf{1. Input coefficients}\\[2pt]
    \(P:[1,2,6]\) (degree 2)\\
    \(Q:[2,4]\) (degree 1)};
  \node[panel,dp/contribution,right=of input] (groups) {
    \textbf{2. Group \(i+j=k\)}\\[2pt]
    \(k=0:(0,0)\)\\
    \(k=1:(0,1),(1,0)\)\\
    \(k=2:(1,1),(2,0)\)\\
    \(k=3:(2,1)\)};
  \node[panel,dp/result,right=of groups] (output) {
    \textbf{3. Normalize}\\[2pt]
    \(R:[2,4,28/3,24]\)\\
    degree \(3\)};
  \draw[dp/arrow] (input)--node[above,font=\tiny]{enumerate}(groups);
  \draw[dp/arrow] (groups)--node[above,font=\tiny]{binomial weights}(output);
\end{tikzpicture}

\textit{Only the panel-to-panel arrows encode flow.  Contributions are listed
inside their anti-diagonal group, so no edges cross labels or equations.}
\end{center}

This is the algorithm behind protected \code{bernProd}
(\cref{sec:pdbase-bernprod}) and public \code{pdmat}/\code{pdvar}
multiplication (\cref{sec:pdmat-operator-entries,sec:pdvar-algebra}).

For tensor labels,
\[
  (PQ)^{(\boldsymbol c)}[\boldsymbol k]=
  \sum_{\boldsymbol i+\boldsymbol j=\boldsymbol k}
  P^{(\boldsymbol c)}[\boldsymbol i]
  Q^{(\boldsymbol c)}[\boldsymbol j]
  \prod_{s=1}^{\ell}
  \frac{\binom m{i_s}\binom n{j_s}}
       {\binom{m+n}{k_s}}.
\]
Labels add componentwise; physical-cell identity does not change.

\subsection{Differentiation And Rate Vertices}

\ideastep{Basic idea}
Differentiate inside each physical cell.  Adjacent control differences produce
the derivative coefficients; multiplying by a bounded rate then creates one
stored row per rate-box vertex.  Value continuity of \(P\) does not force the
cell-local derivatives to match across a face.

\ideastep{Formula}
On physical cell (c) of width \(h_c\),
\[
  \frac{\partial P}{\partial\rho}
  =\frac{m}{h_c}\sum_{i=0}^{m-1}
    \bigl(C^{(c)}[i+1]-C^{(c)}[i]\bigr)B_i^{m-1}(\alpha).
\]
\ideastep{Worked example}
For the worked polynomial \([1,2,6]\) on \([0,2]\), \(m=2\) and \(h=2\), so
the derivative coefficients are
\[
  \left[\frac{2}{2}(2-1),\frac{2}{2}(6-2)\right]=[1,4].
\]
This represents \(dp/d\rho=1+3\alpha\).

In \(\ell\) dimensions, differentiation with respect to \(\rho_s\) replaces
each coefficient by
\[
  \frac{m}{h_s}
  \left(C^{(\boldsymbol c)}[\boldsymbol i+\boldsymbol e_s]
       -C^{(\boldsymbol c)}[\boldsymbol i]\right),
\]
reduces the degree along direction \(s\), and leaves the other label components
unchanged.  The implementation elevates partial derivatives to a common tensor
degree before adding them.

Finally,
\[
  \dot P=\sum_{s=1}^{\ell}
  \frac{\partial P}{\partial\rho_s}\dot\rho_s.
\]
This matrix expression is affine in \(\dot{\boldsymbol\rho}\).  Every value
inside the rate box is therefore a convex combination of the \(2^\ell\)
vertex matrices.  Because the positive- and negative-semidefinite cones are
convex, the requested semidefinite sign holds on the whole rate box whenever
it holds at every vertex.  \code{rhodiff} stores one row for each vertex.

\ideastep{Visual conclusion}
\begin{center}
\begin{tikzpicture}[>=Latex,node distance=7mm and 9mm,
  box/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=28mm,minimum height=9mm}]
  \node[box] (p1) {\(\partial P/\partial\rho_1\)};
  \node[box,below=of p1] (p2) {\(\partial P/\partial\rho_2\)};
  \node[box,right=of p1,yshift=-7mm] (sum) {
    \(\dot\rho_1P_{\rho_1}+\dot\rho_2P_{\rho_2}\)};
  \node[box,right=of sum,yshift=13mm] (v1) {\((\underline v_1,\underline v_2)\)};
  \node[box,right=of sum,yshift=4mm] (v2) {\((\underline v_1,\overline v_2)\)};
  \node[box,right=of sum,yshift=-5mm] (v3) {\((\overline v_1,\underline v_2)\)};
  \node[box,right=of sum,yshift=-14mm] (v4) {\((\overline v_1,\overline v_2)\)};
  \draw[->,dpblue] (p1)--(sum); \draw[->,dpblue] (p2)--(sum);
  \foreach \v in {v1,v2,v3,v4}\draw[->,dpblue] (sum)--(\v);
\end{tikzpicture}
\end{center}

\subsection{de Casteljau Evaluation And Exact Subdivision}

de Casteljau recursion evaluates a Bernstein polynomial using only affine
combinations.  On a fixed physical cell (c), begin with
\(C^{(c,0)}[i]=C^{(c)}[i]\) and define
\[
  C^{(c,r)}[i]=(1-\tau)C^{(c,r-1)}[i]
    +\tau C^{(c,r-1)}[i+1],
  \qquad r=1,\ldots,m.
\]
The value at \(\alpha=\tau\) is \(C^{(c,m)}[0]\).

For \([1,2,6]\) and \(\tau=1/2\), the recursion is
\[
  [1,2,6]\ \longrightarrow\ [1.5,4]\ \longrightarrow\ [2.75].
\]
The left edge of this triangle gives exact coefficients
\([1,1.5,2.75]\) on the left subinterval; the right edge gives
\([2.75,4,6]\) on the right.

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=7mm and 11mm,
  c/.style={draw=black!55,fill=white,circle,font=\scriptsize,minimum size=8mm},
  e/.style={draw=dpblue!75!black,fill=dpbluewash,circle,font=\scriptsize,
    minimum size=8mm}]
  \node[c] (a0) {\(1\)}; \node[c,below=of a0] (a1) {\(2\)};
  \node[c,below=of a1] (a2) {\(6\)};
  \node[e,right=of a0,yshift=-7mm] (b0) {\(1.5\)};
  \node[e,right=of a1,yshift=-7mm] (b1) {\(4\)};
  \node[e,right=of b0,yshift=-7mm] (v) {\(2.75\)};
  \draw[->,dpblue] (a0)--(b0); \draw[->,dpblue] (a1)--(b0);
  \draw[->,dpblue] (a1)--(b1); \draw[->,dpblue] (a2)--(b1);
  \draw[->,dpblue] (b0)--(v); \draw[->,dpblue] (b1)--(v);
\end{tikzpicture}

\medskip
\textit{One recursion triangle supplies both the evaluation and the two
child-cell coefficient lists.}
\end{center}

Tensor evaluation or subdivision applies the same recursion along one parameter
direction at a time.  Subdivision changes the physical-cell partition and can
tighten coefficient hulls while preserving the polynomial.  The package uses
exact coefficient transformations internally where documented, but it does not
currently expose a public de Casteljau, subdivision, or adaptive-refinement API.

\subsection{What Changes, And What Does Not}

The following distinctions prevent several common modeling mistakes.

\begin{tabularx}{\textwidth}{@{}p{0.26\textwidth}Y Y@{}}
\toprule
Operation & Changes & Preserves \\
\midrule
Basis conversion & coefficient coordinates & polynomial, degree, physical cell \\
Degree elevation & Bernstein degree and coefficient list & polynomial, physical grid, decision freedom \\
Grid subdivision & physical-cell partition and local coefficients & polynomial on the original domain \\
Higher-degree \code{pdvar} & number of decision coefficients & grid and matrix size \\
Multiplication & polynomial degree and coefficients & physical-cell identity \\
Differentiation & degree and coefficient values & physical-cell partition before rate rows \\
\bottomrule
\end{tabularx}

These representation facts support the finite certificates in the next
appendix.  They do not by themselves select a solver or prove that one
certificate is necessary.
\section{Polynomial Matrix Sign Certificates On A Hypercube}
\label{app:certificates}

This appendix starts with positive semidefinite matrices, builds SOS and Gram
representations, and then explains the four certificate choices implemented
by \code{pdlmi}.  Background constructions are included so a new reader can
recognize the mathematical landscape; only explicitly marked methods are
package APIs.

As in the preceding appendix, each major certificate is revealed as
\emph{Basic idea} \(\rightarrow\) \emph{Formula} \(\rightarrow\)
\emph{Worked example} \(\rightarrow\) \emph{Visual conclusion}.  Keep two
questions separate while reading: what polynomial matrix is represented, and
what finite cone is being used to certify its sign?

For a residual \(F\preceq0\), define \(S=-F\).  For \(F\succeq0\), define
\(S=F\).  The common problem is
\[
  S(\boldsymbol\alpha)\succeq0
  \quad\text{for every }\boldsymbol\alpha\in[0,1]^\ell.
\]
All Gram constraints below prove non-strict inequalities.  A strict model must
state its own margin, for example \(S-\varepsilon I\succeq0\).  The package
adds no hidden margin.

\subsection{Positive Semidefinite Matrices And LMIs}

\ideastep{Basic idea}
A real symmetric matrix \(Q\) is positive semidefinite when
\[
  z^{\mathsf T}Qz\ge0\qquad\text{for every vector }z.
\]
\ideastep{Formula}
An LMI is an affine matrix expression constrained to the PSD cone:
\[
  Q(x)=Q_0+x_1Q_1+\cdots+x_rQ_r\succeq0.
\]
The unknowns enter affinely, so this is a semidefinite program.  A polynomial
matrix sign condition appears infinite because it asks for one PSD matrix at
every point.  A certificate replaces those pointwise conditions by finitely
many PSD matrices and affine coefficient identities.

\ideastep{Visual conclusion}
\begin{center}
\begin{tikzpicture}[>=Latex,node distance=8mm,
  box/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=38mm,minimum height=11mm}]
  \node[box] (x) {decision vector \(x\)};
  \node[box,right=of x] (q) {affine matrix \(Q(x)\)};
  \node[box,right=of q] (psd) {\(Q(x)\succeq0\)};
  \draw[->,thick,dpblue] (x)--(q);
  \draw[->,thick,dpblue] (q)--node[above,font=\scriptsize]{one LMI}(psd);
\end{tikzpicture}
\end{center}

\subsection{SOS And Gram Matrices}

\ideastep{Basic idea}
A scalar polynomial is a sum of squares (SOS) if
\[
  p(z)=q_1(z)^2+\cdots+q_s(z)^2.
\]
Every SOS polynomial is nonnegative.  Choose a polynomial basis vector
\(v_d(z)\).  The Gram form
\[
  p(z)=v_d(z)^{\mathsf T}Qv_d(z),\qquad Q\succeq0,
\]
is SOS because a factorization \(Q=L^{\mathsf T}L\) yields
\(p=\lVert Lv_d\rVert_2^2\).  Expanding the Gram form and matching every
coefficient of \(p\) gives equations linear in the entries of \(Q\).  Thus an
SOS search is a finite PSD constraint plus affine equalities.

\ideastep{Formula}
For a symmetric \(n\)-by-\(n\) polynomial matrix, set
\[
  Z_d(z)=v_d(z)\otimes I_n,\qquad
  S(z)=Z_d(z)^{\mathsf T}QZ_d(z),\quad Q\succeq0.
\]
Then \(y^{\mathsf T}S(z)y=(Z_dy)^{\mathsf T}Q(Z_dy)\ge0\) for every \(y\), so
\(S(z)\succeq0\).  Matrix-SOS relaxations are developed in
\cite{schererhol2006}.

\ideastep{Visual conclusion}
\begin{center}
\begin{tikzpicture}[>=Latex,node distance=7mm,
  box/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=35mm,minimum height=11mm}]
  \node[box] (basis) {choose basis \(v_d\)};
  \node[box,right=of basis] (gram) {unknown \(Q\succeq0\)};
  \node[box,right=of gram] (expand) {expand \(v_d^{\mathsf T}Qv_d\)};
  \node[box,right=of expand] (match) {match coefficients};
  \draw[->,thick,dpblue] (basis)--(gram);
  \draw[->,thick,dpblue] (gram)--(expand);
  \draw[->,thick,dpblue] (expand)--(match);
\end{tikzpicture}
\end{center}

\subsection{Full-Space SOS}

An unweighted Gram form proves \(S(z)\succeq0\) on all of
\(\mathbb R^\ell\).  That can be much stronger than positivity only on a box.
For example, \(\alpha(1-\alpha)\) is nonnegative on \([0,1]\) but negative
outside it.  The package does not expose a general full-space SOS parser; this
construction is background for understanding weighted certificates.

\subsection{Direct Bernstein Coefficients}

\ideastep{Basic idea}
If every local coefficient matrix has the requested sign, every convex
combination of those coefficients has the same sign.  This yields a small,
transparent sufficient certificate.

\ideastep{Formula}
On physical cell \(\boldsymbol c\) and one rate row, write
\[
  S(\boldsymbol\alpha)=
  \sum_{\boldsymbol i}B_{\boldsymbol i}^m(\boldsymbol\alpha)
  C^{(\boldsymbol c)}[\boldsymbol i].
\]
Because the basis weights are nonnegative and sum to one,
\[
  C^{(\boldsymbol c)}[\boldsymbol i]\succeq0\ \forall\boldsymbol i
  \quad\Longrightarrow\quad S(\boldsymbol\alpha)\succeq0.
\]
This is the package default.  It introduces no Gram variables and creates one
constraint per physical cell, rate row, and local label.  The converse is
false.

\ideastep{Worked example}
Consider
\[
  p(\alpha)=\alpha^2-\alpha+0.35=(\alpha-0.5)^2+0.10.
\]
It is strictly positive, with minimum \(0.10\), but its degree-two Bernstein
coefficients are
\[
  [\,0.35,\,-0.15,\,0.35\,].
\]
The negative middle coefficient defeats the direct certificate without making
the polynomial negative.

\ideastep{Visual conclusion}
\begin{center}
\begin{tikzpicture}[x=5.2cm,y=4.0cm,>=Latex,
  coeff/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,minimum width=14mm,minimum height=7mm}]
  \draw[->,black!60] (-0.05,0) -- (1.08,0) node[right,font=\scriptsize]{\(\alpha\)};
  \draw[->,black!60] (0,-0.02) -- (0,0.42) node[above,font=\scriptsize]{\(p\)};
  \draw[very thick,dpblue] plot[domain=0:1,samples=60]
    (\x,{\x*\x-\x+0.35});
  \draw[dashed,black!45] (0,0.10)--(1,0.10);
  \node[font=\scriptsize,anchor=west] at (0.52,0.12) {minimum \(0.10\)};
  \node[coeff] at (0.12,-0.14) {\(0.35\)};
  \node[coeff,fill=red!10,draw=red!65!black] at (0.50,-0.14) {\(-0.15\)};
  \node[coeff] at (0.88,-0.14) {\(0.35\)};
\end{tikzpicture}

\medskip
\textit{One negative Bernstein coefficient does not imply a negative curve.}
\end{center}

The same polynomial has the PSD Bernstein Gram representation
\[
  \zeta_1=\begin{bmatrix}1-\alpha\\\alpha\end{bmatrix},\qquad
  Q=\begin{bmatrix}0.35&-0.15\\-0.15&0.35\end{bmatrix}\succeq0,
  \qquad p=\zeta_1^{\mathsf T}Q\zeta_1.
\]
The eigenvalues of \(Q\) are \(0.20\) and \(0.50\).  A complete Gram
certificate can therefore succeed when coefficient-wise signs fail.

\subsection{P\'olya As Lossless Elevation}

\ideastep{Basic idea}
P\'olya selection does not change the residual polynomial.  It rewrites that
same polynomial at a higher Bernstein degree and then retries the direct
coefficient test.

\ideastep{Formula}
Classical P\'olya theory concerns homogeneous polynomials strictly positive on
a simplex \cite{powersreznick2001}.  For one box coordinate,
\[
  \lambda_0=1-\alpha,\quad \lambda_1=\alpha,\quad
  \lambda_0+\lambda_1=1.
\]
For \(\ell\) coordinates, the implemented multiplier is
\[
  \prod_{s=1}^{\ell}
  (\lambda_{s,0}+\lambda_{s,1})^d=1.
\]
Regrouping is exactly Bernstein degree elevation: the represented residual does
not change.

\ideastep{Worked example}
For the worked coefficients,
\[
  [\,0.35,-0.15,0.35\,]
  \longrightarrow
  \left[\,0.35,\frac1{60},\frac1{60},0.35\,\right]
\]
after one elevation.  Every new coefficient is positive, so the elevated
coefficient test succeeds.

\ideastep{Visual conclusion}
\begin{center}
\begin{tikzpicture}[>=Latex,node distance=7mm and 9mm,
  box/.style={draw=black!55,fill=white,rounded corners=2pt,font=\scriptsize,
    minimum width=14mm,minimum height=7mm},
  ok/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,minimum width=14mm,minimum height=7mm}]
  \node[box] (a0) {\(0.35\)}; \node[box,right=of a0] (a1) {\(-0.15\)};
  \node[box,right=of a1] (a2) {\(0.35\)};
  \node[ok,below=11mm of a0] (b0) {\(0.35\)};
  \node[ok,right=of b0] (b1) {\(1/60\)};
  \node[ok,right=of b1] (b2) {\(1/60\)};
  \node[ok,right=of b2] (b3) {\(0.35\)};
  \draw[->,dpblue] (a0)--(b0); \draw[->,dpblue] (a0)--(b1);
  \draw[->,dpblue] (a1)--(b1); \draw[->,dpblue] (a1)--(b2);
  \draw[->,dpblue] (a2)--(b2); \draw[->,dpblue] (a2)--(b3);
  \node[above=4pt of a1,font=\scriptsize]{direct test fails};
  \node[below=4pt of b1,font=\scriptsize]{same polynomial, elevated test succeeds};
\end{tikzpicture}
\end{center}

\code{applyPolya(d)} applies one scalar increment in every parameter direction
and then coefficient constraints to every cell and rate row.  It adds neither
Gram variables nor decision freedom.  A fixed-level failure is inconclusive;
the package makes no tensor-box completeness claim.

\subsection{Exact Interval Markov--Luk\'acs Forms}

\ideastep{Basic idea}
On one interval, endpoint factors can encode the domain exactly.  Even and odd
target degrees require different weighted Gram patterns, so parity is a
structural choice rather than a cosmetic one.

\ideastep{Formula}
In one variable, interval nonnegativity has exact parity-specific descriptions
\cite{lukacs1918,deklerkhesslaurent2017}.  If \(\deg p\le2r\),
\[
  p\ge0\text{ on }[0,1]
  \Longleftrightarrow
  p=s_0+\alpha(1-\alpha)s_1,
\]
where \(s_0,s_1\) are SOS of degrees at most \(2r\) and \(2r-2\).  If
\(\deg p\le2r+1\),
\[
  p\ge0\text{ on }[0,1]
  \Longleftrightarrow
  p=(1-\alpha)s_L+\alpha s_U,
\]
where \(s_L,s_U\) are SOS of degree at most \(2r\).

For polynomial matrices, with
\[
  \mathcal Z_d(\alpha)=
  \begin{bmatrix}B_0^dI_n&\cdots&B_d^dI_n\end{bmatrix}^{\mathsf T},
\]
the even and odd forms are
\[
  S=\mathcal Z_r^{\mathsf T}Q_0\mathcal Z_r
   +\alpha(1-\alpha)\mathcal Z_{r-1}^{\mathsf T}Q_1\mathcal Z_{r-1},
  \qquad Q_0,Q_1\succeq0,
\]
and
\[
  S=(1-\alpha)\mathcal Z_r^{\mathsf T}Q_L\mathcal Z_r
    +\alpha\mathcal Z_r^{\mathsf T}Q_U\mathcal Z_r,
  \qquad Q_L,Q_U\succeq0.
\]
The second even block is absent at \(r=0\).  See
\cite{aylwarditaniparrilo2007,schererhol2006} for polynomial-matrix context.

\ideastep{Worked example}
A cubic residual has degree \(2r+1\) with \(r=1\).  It therefore uses two
degree-one Gram bases: one multiplied by \(1-\alpha\) and one by
\(\alpha\).  This is the one-dimensional full-box pattern used for the cubic
residual in the introductory DPD-LMI example.

\ideastep{Visual conclusion}
\begin{center}
\begin{tikzpicture}[>=Latex,node distance=9mm,
  box/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=57mm,minimum height=15mm}]
  \node[box] (even) {even \(2r\)\\unweighted Gram \(+\)
    \(\alpha(1-\alpha)\)-weighted Gram};
  \node[box,right=of even] (odd) {odd \(2r+1\)\\
    \((1-\alpha)\) lower-face Gram \(+\)
    \(\alpha\) upper-face Gram};
\end{tikzpicture}
\end{center}

Partition \(Q\) into \(n\)-by-\(n\) blocks \(Q_{ij}\).  The Bernstein
coefficient at label \(k\) of
\(\alpha^a(1-\alpha)^b\mathcal Z_d^{\mathsf T}Q\mathcal Z_d\), expressed at
total degree \(2d+a+b\), is
\[
  \mathcal G_k^{d,a,b}(Q)=
  \frac{1}{\binom{2d+a+b}{k}}
  \sum_{\substack{0\le i,j\le d\\i+j=k-a}}
  \binom di\binom dj\,Q_{ij}.
\]
Equating this affine map to each target coefficient gives exact linear
identities; \(Q\succeq0\) supplies the LMIs.  For one parameter,
\code{applyFullBoxPreorder} implements these exact parity forms.  Its minimum
absolute order is \(\lfloor m/2\rfloor\).

\subsection{Putinar Modules And Schm\"udgen Preorderings}

For
\[
  K=\{z:g_1(z)\ge0,\ldots,g_q(z)\ge0\},
\]
a Putinar quadratic module has the form
\[
  \sigma_0+\sum_{j=1}^{q}\sigma_jg_j,\qquad \sigma_j\text{ SOS}.
\]
Under the Archimedean hypothesis, every polynomial strictly positive on \(K\)
has such a representation at some degree \cite{putinar1993}.  Compactness
alone is not that theorem statement.  The package implements one box-specific,
fixed-order instance, not a general-domain or general-generator parser.  With
the local box generators
\[
  g_s(\boldsymbol\alpha)=\alpha_s(1-\alpha_s),
\]
\code{applyPutinar(r)} uses
\[
  F=S_0+\sum_{s=1}^{\ell}g_sS_s,
  \qquad
  S_0=\mathcal Z_{r\boldsymbol1}^{\mathsf T}Q_0
       \mathcal Z_{r\boldsymbol1},\quad
  S_s=\mathcal Z_{r\boldsymbol1-\boldsymbol e_s}^{\mathsf T}Q_s
       \mathcal Z_{r\boldsymbol1-\boldsymbol e_s},
\]
where every present \(Q_s\succeq0\).  The order is absolute and satisfies
\(r\ge\lceil m/2\rceil\).  Exact identities match the sign-normalized target,
elevated without changing it, at tensor degree \(2r\).  Negative-degree
multiplier blocks are omitted at \(r=0\).  Each physical cell and active rate
row owns independent Gram and equality blocks; no generator products, implicit
margin, or solver call are introduced.

A Schm\"udgen full preordering includes every square-free generator product:
\[
  p=\sum_{\boldsymbol\nu\in\{0,1\}^{q}}
  \sigma_{\boldsymbol\nu}\prod_{j=1}^{q}g_j^{\nu_j},
  \qquad \sigma_{\boldsymbol\nu}\text{ SOS}.
\]
Strict positivity on a compact basic closed semialgebraic set has such a
representation \cite{schmudgen1991}.  A finite truncation is still a sufficient
certificate, and up to \(2^q\) multiplier blocks may be required.

\subsection{The Implemented Multivariate Full Box Preordering}

\ideastep{Basic idea}
For several parameters, use every square-free product of the box generators.
Each subset selects one weighted Gram block, and exact coefficient identities
make the sum equal the stored residual on one physical cell and rate row.

\ideastep{Formula}
For the hypercube, use
\[
  g_s(\boldsymbol\alpha)=\alpha_s(1-\alpha_s),
  \qquad s=1,\ldots,\ell.
\]
\ideastep{Worked example}
For two parameters, the four subset products are
\(1,g_1,g_2,g_1g_2\).

\ideastep{Visual conclusion}
\begin{center}
\begin{tikzpicture}[>=Latex,node distance=6mm and 7mm,
  box/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=31mm,minimum height=10mm}]
  \node[box] (e) {\(J=\varnothing\)\\\(1\)};
  \node[box,right=of e] (g1) {\(J=\{1\}\)\\\(\alpha_1(1-\alpha_1)\)};
  \node[box,below=of e] (g2) {\(J=\{2\}\)\\\(\alpha_2(1-\alpha_2)\)};
  \node[box,right=of g2] (g12) {\(J=\{1,2\}\)\\\(g_1g_2\)};
\end{tikzpicture}
\end{center}

For \(\ell\ge2\) and absolute order \(r\), define
\[
  \mathcal Z_{\boldsymbol d}=
  \left(\bigotimes_{s=1}^{\ell}
  \begin{bmatrix}B_0^{d_s}&\cdots&B_{d_s}^{d_s}\end{bmatrix}^{\mathsf T}
  \right)\otimes I_n.
\]
The implemented form is
\[
  S=
  \sum_{J\subseteq\{1,\ldots,\ell\}}
  \left(\prod_{s\in J}g_s\right)
  \mathcal Z_{r\boldsymbol1-\boldsymbol\chi_J}^{\mathsf T}
  Q_J
  \mathcal Z_{r\boldsymbol1-\boldsymbol\chi_J},
  \qquad Q_J\succeq0.
\]
Terms with negative tensor degree are omitted.  A present block has dimension
\[
  n\prod_{s=1}^{\ell}\left(r+1-\mathbf1_{s\in J}\right).
\]
Every physical cell and active rate row gets independent dense Gram blocks and
exact coefficient identities.  The minimum order is \(\lceil m/2\rceil\) for
\(\ell\ge2\).  This is a specific dense Bernstein realization of the complete
box preordering, not a full-space SOS, Putinar-only, sparse, or general-domain
parser.  A fixed multivariate order is sufficient rather than exact.

\begin{lstlisting}[style=dpmatlab]
yalmip('clear')
P = pdvar(1, {[0 1]}, Degree=2);
direct = P >= 0;
polya = direct.applyPolya(1);
putinar = direct.applyPutinar();
box = direct.applyFullBoxPreorder();
fprintf("direct: constraints=%d\n", numel(direct.Constraints));
fprintf("Polya: increment=%d, constraints=%d\n", ...
    polya.PolyaDegree, numel(polya.Constraints));
fprintf("Putinar: order=%d, constraints=%d\n", ...
    putinar.PutinarOrder, numel(putinar.Constraints));
fprintf("full box: order=%d, constraints=%d\n", ...
    box.FullBoxOrder, numel(box.Constraints));
\end{lstlisting}

\begin{lstlisting}[style=dpoutput]
direct: constraints=3
Polya: increment=1, constraints=4
Putinar: order=1, constraints=5
full box: order=1, constraints=5
\end{lstlisting}

Selections rebuild from the stored original \code{Residual} and
\code{Relation}.  Reapplying P\'olya, Putinar, or full-box replaces the previous
selection; the certificates never compound, and the three opt-in families are
mutually exclusive.

\subsection{Five Different Modeling Choices}

These choices act on different parts of the problem.

\begin{center}
\begin{tikzpicture}[>=Latex,node distance=5mm,
  box/.style={draw=dpblue!75!black,fill=dpbluewash,rounded corners=2pt,
    font=\scriptsize,align=center,minimum width=40mm,minimum height=10mm}]
  \node[box] (repr) {basis representation\\same polynomial};
  \node[box,below=of repr] (decision) {decision degree\\larger search space};
  \node[box,below=of decision] (grid) {physical subdivision\\more local cells};
  \node[box,below=of grid] (polya) {P\'olya increment\\same residual, elevated basis};
  \node[box,below=of polya] (gram) {Gram order\\larger PSD certificate};
  \node[box,right=17mm of grid] (ask) {ask what changes:\\polynomial? decisions?\\
    cells? certificate?};
  \foreach \x in {repr,decision,grid,polya,gram}
    \draw[->,thick,dpblue] (\x.east)--(ask.west);
\end{tikzpicture}
\end{center}

\begin{tabularx}{\textwidth}{@{}p{0.23\textwidth}Y Y@{}}
\toprule
Choice & Changes & Does not mean \\
\midrule
basis conversion & coefficient coordinates & more decision freedom \\
decision degree & independent decision coefficients & certificate order \\
subdivision & physical cells and local hulls & higher polynomial degree \\
P\'olya degree & elevated residual coefficients & Gram variables \\
Putinar/full-box order & Gram bases and PSD block sizes & decision degree \\
\bottomrule
\end{tabularx}

When a certificate fails, do not infer continuous infeasibility.  Decide
separately whether the model needs a richer decision degree, a finer physical
grid, a larger P\'olya increment, or a higher Putinar or full-box order.

\subsection{Implemented Boundary}

\begin{tabularx}{\textwidth}{@{}p{0.36\textwidth}Y@{}}
\toprule
Implemented public behavior & Background only \\
\midrule
direct coefficient assembly & general full-space SOS parser \\
uniform \code{applyPolya([d])} & direction-wise or automatic P\'olya search \\
dense box-specific \code{applyPutinar([r])} & general-domain/general-generator Putinar interface \\
dense \code{applyFullBoxPreorder([r])} & general-domain Schm\"udgen parser \\
exact one-dimensional parity forms & automatic or adaptive Gram-order search \\
all cells and active rate rows & sparse Gram selection or adaptive refinement \\
explicit user margins & package-owned strictness or solver policy \\
\bottomrule
\end{tabularx}

Only the left column is callable package behavior.  The background theorems
explain alternative cones; they do not expand the MATLAB API.
\clearpage
\addcontentsline{toc}{section}{References}
\begin{thebibliography}{99}

\bibitem{farouki2012}
R.~T. Farouki,
``The Bernstein polynomial basis: A centennial retrospective,''
\emph{Computer Aided Geometric Design}, vol.~29, no.~6, pp.~379--419, 2012.
\href{https://doi.org/10.1016/j.cagd.2012.03.001}
     {doi:10.1016/j.cagd.2012.03.001}.

\bibitem{zettler1998}
M. Zettler and J. Garloff,
``Robustness analysis of polynomials with polynomial parameter dependency
using Bernstein expansion,''
\emph{IEEE Transactions on Automatic Control}, vol.~43, no.~3,
pp.~425--431, 1998.
\href{https://doi.org/10.1109/9.661615}{doi:10.1109/9.661615}.

\bibitem{masubuchi1998}
I. Masubuchi, A. Kume, and E. Shimemura,
``Spline-type solution to parameter-dependent LMIs,''
in \emph{Proceedings of the 37th IEEE Conference on Decision and Control},
vol.~2, 1998.
\href{https://doi.org/10.1109/CDC.1998.758549}
     {doi:10.1109/CDC.1998.758549}.

\bibitem{xu2024}
Y. Xu and F. Jabbari,
``Spline-based parameter varying output feedback synthesis with improved
\(L_2\) gain,''
in \emph{2024 IEEE 63rd Conference on Decision and Control},
pp.~1455--1460, 2024.
\href{https://doi.org/10.1109/CDC56724.2024.10886461}
     {doi:10.1109/CDC56724.2024.10886461}.

\bibitem{chesi2010}
G. Chesi,
``LMI techniques for optimization over polynomials in control: a survey,''
\emph{IEEE Transactions on Automatic Control}, vol.~55, no.~11,
pp.~2500--2510, 2010.
\href{https://doi.org/10.1109/TAC.2010.2046926}
     {doi:10.1109/TAC.2010.2046926}.

\bibitem{lukacs1918}
F.~Luk\'acs,
``Versch\"arfung des ersten Mittelwertsatzes der Integralrechnung f\"ur
rationale Polynome,''
\emph{Mathematische Zeitschrift}, vol.~2, pp.~295--305, 1918.
\href{https://eudml.org/doc/167484}{EuDML record}.

\bibitem{deklerkhesslaurent2017}
E.~de Klerk, R.~Hess, and M.~Laurent,
Improved convergence rates for Lasserre-type hierarchies of upper bounds
for box-constrained polynomial optimization,
\emph{SIAM Journal on Optimization}, vol.~27, no.~1, pp.~347--367, 2017.
\href{https://doi.org/10.1137/16M1065264}
     {doi:10.1137/16M1065264}.

\bibitem{aylwarditaniparrilo2007}
E.~M. Aylward, S.~M. Itani, and P.~A. Parrilo,
Explicit SOS decompositions of univariate polynomial matrices and the
Kalman--Yakubovich--Popov lemma,
in \emph{Proceedings of the 46th IEEE Conference on Decision and Control},
pp.~5660--5665, 2007.
\href{https://www.mit.edu/~parrilo/pubs/files/AylwardItaniParrilo-ExplicitSOSDecompositionsOfUnivariatePolynomialMatricesAndTheKYPLemma.pdf}
     {author manuscript}.

\bibitem{schererhol2006}
C.~W. Scherer and C.~W.~J. Hol,
Matrix sum-of-squares relaxations for robust semi-definite programs,
\emph{Mathematical Programming}, vol.~107, pp.~189--211, 2006.
\href{https://doi.org/10.1007/s10107-005-0684-2}
     {doi:10.1007/s10107-005-0684-2}.

\bibitem{putinar1993}
M.~Putinar,
``Positive polynomials on compact semi-algebraic sets,''
\emph{Indiana University Mathematics Journal}, vol.~42, no.~3,
pp.~969--984, 1993.
\href{https://www.jstor.org/stable/24897130}{JSTOR 24897130}.

\bibitem{schmudgen1991}
K.~Schm\"udgen,
``The \(K\)-moment problem for compact semi-algebraic sets,''
\emph{Mathematische Annalen}, vol.~289, pp.~203--206, 1991.
\href{https://doi.org/10.1007/BF01446568}
     {doi:10.1007/BF01446568}.

\bibitem{powersreznick2001}
V.~Powers and B.~Reznick,
``A new bound for P\'olya's theorem with applications to polynomials
positive on polyhedra,''
\emph{Journal of Pure and Applied Algebra}, vol.~164, nos.~1--2,
pp.~221--229, 2001.
\href{https://doi.org/10.1016/S0022-4049(00)00155-9}
     {doi:10.1016/S0022-4049(00)00155-9}.

\end{thebibliography}

\end{document}
