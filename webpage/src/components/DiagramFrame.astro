---
interface Props {
  caption: string;
  label?: string;
  class?: string;
}

const { caption, label = caption, class: className = "" } = Astro.props;
---

<figure class:list={["diagram-frame", className]} aria-label={label}>
  <div class="diagram-frame__body">
    <slot />
  </div>
  <figcaption>{caption}</figcaption>
</figure>
