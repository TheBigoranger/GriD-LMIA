<figure
  class="journey-pipeline"
  role="img"
  aria-labelledby="journey-title"
  aria-describedby="journey-desc"
>
  <figcaption id="journey-title" class="journey-title">
    PD-LMI modeling, certification, solution, and feedback workflow
  </figcaption>
  <p id="journey-desc" class="journey-description">
    Five stages progress from the LPV model through cell-local Bernstein data,
    a finite certificate, a YALMIP or SDP solve, and solved scheduled matrices.
    A separate lower lane returns the result to the LPV model.
  </p>

  <ol class="journey-stages">
    <li><span>01</span><strong>LPV model</strong></li>
    <li><span>02</span><strong>cell-local Bernstein data</strong></li>
    <li><span>03</span><strong>finite certificate</strong></li>
    <li class="journey-stage--result"><span>04</span><strong>YALMIP / SDP solve</strong></li>
    <li class="journey-stage--result"><span>05</span><strong>solved scheduled matrices</strong></li>
  </ol>

  <div class="journey-return" aria-hidden="true">
    <span>feedback into LPV model</span>
  </div>
</figure>

<style>
  .journey-pipeline {
    --journey-gap: clamp(1.15rem, 2.2vw, 1.75rem);
    display: grid;
    gap: 1rem;
    min-width: 0;
    margin: 0;
  }

  .journey-title,
  .journey-description {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .journey-stages {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: var(--journey-gap);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .journey-stages li {
    position: relative;
    display: grid;
    place-items: center;
    align-content: center;
    gap: 0.45rem;
    min-height: 7.25rem;
    margin: 0;
    border: 1px solid var(--diagram-border);
    border-radius: 14px;
    padding: 1rem 0.75rem;
    color: var(--sl-color-text);
    background: var(--diagram-surface);
    text-align: center;
  }

  .journey-stages li:not(:last-child)::after {
    position: absolute;
    z-index: 1;
    content: "";
    inset-block-start: 50%;
    inset-inline-start: 100%;
    width: var(--journey-gap);
    border-block-start: 3px solid var(--diagram-result);
    transform: translateY(-50%);
  }

  .journey-stages li:not(:last-child)::before {
    position: absolute;
    z-index: 2;
    content: "";
    inset-block-start: 50%;
    inset-inline-start: calc(100% + var(--journey-gap) - 0.45rem);
    border-block: 0.35rem solid transparent;
    border-inline-start: 0.45rem solid var(--diagram-result);
    transform: translateY(-50%);
  }

  .journey-stages span {
    display: grid;
    place-items: center;
    width: 2rem;
    height: 2rem;
    border: 3px solid var(--sl-color-accent);
    border-radius: 50%;
    color: var(--sl-color-text-accent);
    background: var(--sl-color-bg);
    font-size: 0.72rem;
    font-weight: 850;
    line-height: 1;
  }

  .journey-stages .journey-stage--result span {
    border-color: var(--diagram-result);
    color: var(--diagram-result);
  }

  .journey-stages strong {
    font-size: clamp(0.88rem, 1.35vw, 1rem);
    line-height: 1.25;
  }

  .journey-return {
    position: relative;
    display: grid;
    place-items: center;
    min-height: 2.2rem;
    margin-inline: clamp(1rem, 4vw, 3rem);
    color: var(--diagram-result);
    font-size: 0.86rem;
    font-weight: 750;
  }

  .journey-return::before {
    position: absolute;
    z-index: 0;
    content: "";
    inset: 50% 0 auto;
    border-block-start: 2px dashed var(--diagram-result);
  }

  .journey-return::after {
    position: absolute;
    z-index: 1;
    content: "";
    inset-block-start: calc(50% - 0.32rem);
    inset-inline-start: -0.05rem;
    border-block: 0.32rem solid transparent;
    border-inline-end: 0.45rem solid var(--diagram-result);
  }

  .journey-return span {
    z-index: 1;
    padding-inline: 0.8rem;
    background: var(--diagram-surface);
  }

  @media (max-width: 700px) {
    .journey-pipeline { gap: 1.25rem; }

    .journey-stages {
      grid-template-columns: minmax(0, 1fr);
      gap: 1.65rem;
    }

    .journey-stages li { min-height: 5.5rem; }

    .journey-stages li:not(:last-child)::after {
      inset-block-start: 100%;
      inset-inline-start: 50%;
      width: 0;
      height: 1.65rem;
      border-block-start: 0;
      border-inline-start: 3px solid var(--diagram-result);
      transform: translateX(-50%);
    }

    .journey-stages li:not(:last-child)::before {
      inset-block-start: calc(100% + 1.65rem - 0.45rem);
      inset-inline-start: 50%;
      border-inline: 0.35rem solid transparent;
      border-block-start: 0.45rem solid var(--diagram-result);
      transform: translateX(-50%);
    }

    .journey-stages strong { font-size: 0.95rem; }
    .journey-return { margin-inline: 0.5rem; }
  }
</style>
