---
title: Install And Download
description: Install GriD-LMIA, open the synchronized manual, and verify the MATLAB/YALMIP setup.
---

import { versionInfo } from "../../data/version.js";

The online and printable manuals describe documentation version
**{versionInfo.current}**. Package v1.2.0 remains the latest tagged GitHub
Release.

## Downloads

| Item | Link |
| :--- | :--- |
| {versionInfo.current} manual | [Open the synchronized printable manual](/GriD-LMIA/manual.pdf) |
| Latest GitHub Release (v1.2.0) | [Open the GitHub Release](https://github.com/TheBigoranger/GriD-LMIA/releases/tag/v1.2.0) |
| {versionInfo.current} source snapshot | [Download `main` as ZIP](https://github.com/TheBigoranger/GriD-LMIA/archive/refs/heads/main.zip) |
| GitHub repository | [TheBigoranger/GriD-LMIA](https://github.com/TheBigoranger/GriD-LMIA) |
| Version history | [Open version history](/GriD-LMIA/version-history/) |
| Previous v1.0.0 release | [Open the completed 1.0 line](https://github.com/TheBigoranger/GriD-LMIA/releases/tag/v1.0.0) |

The project does not publish the PDF manual as a separately named release
asset. The manual link is the synchronized {versionInfo.current} site
artifact, while the
Release link targets the immutable v1.2.0 package snapshot.

## Requirements

- MATLAB.
- YALMIP already on the current MATLAB path. The installer requires its `yalmip`,
  `sdpvar`, `sdpsettings`, `optimize`, and `getavailablesolvers` entry points;
  it does not download YALMIP or scan disks for it.
- A working SDP solver exposed by YALMIP. The installer probes MOSEK, COPT,
  SeDuMi, SDPT3, then LMILAB, accepting the first candidate that solves a
  small bounded SDP successfully.

## One-Call Setup

```matlab
report = install_pd_lmi();
```

Run the command from the package root. It adds only that repository root at
the end of the MATLAB path. It deliberately does not call `genpath` or add
`doc`, `webpage`, test, metadata, or generated directories recursively.
Exact-path idempotence means a second call leaves the root in place without
duplicating it. The installer checks for class shadowing before saving the
MATLAB path, so a failure restores the previous path instead of leaving a
partial installation.

`report` contains `PackageRoot`, `YALMIPRoot`, the working `Solver`, the
`AddedPaths`, and whether the path was `Persisted`. Calling `install_pd_lmi()`
without an output performs the same idempotent installation.

## Failure Behavior

The installer stops with a stable error identifier rather than guessing a
configuration. Typical failures are `install_pd_lmi:MissingYALMIP`,
`install_pd_lmi:IncompleteYALMIP`, `install_pd_lmi:NoWorkingSDPSolver`,
`install_pd_lmi:PathConflict`, and `install_pd_lmi:PathSaveFailed`. Resolve
the reported dependency, solver, or path conflict and run the one command
again; do not add package paths manually or edit `startup.m` for this setup.

## Verify

```matlab
results = tests.run_all();
```

The release gate is `tests.run_all()` with zero failed and zero incomplete
results. The release notes record the exact v1.2.0 validation counts.

## First Lookup

- [Reference lookup table](/GriD-LMIA/documents/reference-index/)
- [Mathematical guide](/GriD-LMIA/documents/math/modeling-and-analysis/dpd-lmi-and-lpv-l2-gain/)
- [pdmat constructor](/GriD-LMIA/documents/reference/pdmat/constructor/)
- [pdvar constructor](/GriD-LMIA/documents/reference/pdvar/constructor/)
- [Solver smoke examples](/GriD-LMIA/examples/solver-smoke/)
