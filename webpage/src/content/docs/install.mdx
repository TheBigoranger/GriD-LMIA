---
title: Install And Download
description: Install the PD-LMI package, open the synchronized manual, and verify the MATLAB/YALMIP setup.
---

import { versionInfo } from "../../data/version.js";

The online and printable manuals describe documentation version **{versionInfo.current}**.
Use the current `main` source snapshot for that interface. The older tagged
`v0.2.0` release remains available when an immutable historical package
snapshot is required.

## Downloads

| Item | Link |
| :--- | :--- |
| {versionInfo.current} manual | [Open the synchronized printable manual](/PD-LMI-package/manual.pdf) |
| Current source snapshot | [Download `main` as ZIP](https://github.com/TheBigoranger/PD-LMI-package/archive/refs/heads/main.zip) |
| GitHub repository | [TheBigoranger/PD-LMI-package](https://github.com/TheBigoranger/PD-LMI-package) |
| Version history | [Open version history](/PD-LMI-package/version-history/) |
| Historical v0.2.0 tag | [Open the immutable release](https://github.com/TheBigoranger/PD-LMI-package/releases/tag/v0.2.0) |

The project does not publish the PDF manual as a separately named release
asset, so the manual link above is explicitly the current synchronized site
snapshot.

## Requirements

- MATLAB.
- YALMIP already on the current MATLAB path. The installer requires its `yalmip`,
  `sdpvar`, `sdpsettings`, `optimize`, and `getavailablesolvers` entry points;
  it does not download YALMIP or scan disks for it.
- A working SDP solver exposed by YALMIP. The installer probes MOSEK, COPT,
  SeDuMi, SDPT3, then LMILAB, accepting the first candidate that solves a
  small bounded SDP successfully.

## One-Call Setup

```matlab
report = install_pd_lmi();
```

Run the command from the package root. It adds the package source paths while
excluding documentation, the Web site, independent SOS validation, metadata,
and generated build directories. It checks for class shadowing before saving
the MATLAB path, so a failure restores the previous path instead of leaving a
partial installation.

`report` contains `PackageRoot`, `YALMIPRoot`, the working `Solver`, the
`AddedPaths`, and whether the path was `Persisted`. Calling `install_pd_lmi()`
without an output performs the same idempotent installation.

## Failure Behavior

The installer stops with a stable error identifier rather than guessing a
configuration. Typical failures are `install_pd_lmi:MissingYALMIP`,
`install_pd_lmi:IncompleteYALMIP`, `install_pd_lmi:NoWorkingSDPSolver`,
`install_pd_lmi:PathConflict`, and `install_pd_lmi:PathSaveFailed`. Resolve
the reported dependency, solver, or path conflict and run the one command
again; do not add package paths manually or edit `startup.m` for this setup.

## Verify

```matlab
results = tests.run_all();
```

## First Lookup

- [Reference lookup table](/PD-LMI-package/documents/reference-index/)
- [pdmat constructor](/PD-LMI-package/documents/reference/pdmat/constructor/)
- [pdvar constructor](/PD-LMI-package/documents/reference/pdvar/constructor/)
- [Solver smoke examples](/PD-LMI-package/examples/solver-smoke/)
