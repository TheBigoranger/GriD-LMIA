---
title: SparseFullBox And FullBox
description: Full-box preorderings, band-limited Gram support, bandwidth endpoints, and exact coefficient matching.
---

import CertificateSpectrum from "../../../../../components/CertificateSpectrum.astro";

<nav class="manual-trail">
  <a href="/PD-LMI-package/documents/">Documents</a>
  <span>/</span>
  <span>Finite Certificates</span>
  <span>/</span>
  <span>SparseFullBox And FullBox</span>
</nav>

<CertificateSpectrum selected="sparsefullbox" />

The mathematical construction is FullBox with **band-limited Gram support**;
the software state is `SparseFullBox`. The dense construction is the
**full-box preordering**, with software state `FullBox`.

## Full-Box Preordering

For each subset $J\subseteq\{1,\ldots,\ell\}$, define

$$
g_J(\boldsymbol\alpha)
=\prod_{s\in J}\alpha_s(1-\alpha_s),
\qquad g_{\varnothing}=1.
$$

FullBox represents

$$
S^{(\mathbf c)}(\boldsymbol\alpha)
=-\mathcal F^{(\mathbf c)}(\boldsymbol\alpha)
=\sum_{J\subseteq[\ell]}
g_J(\boldsymbol\alpha)S_J(\boldsymbol\alpha),
\qquad
C^{(\mathbf c)}=-\mathcal C^{(\mathbf c)},
$$

where every $S_J$ has a free positive-semidefinite Gram matrix. The weighted
Gram maps are assembled in the Bernstein basis and matched exactly to every
$C^{(\mathbf c)}[\mathbf i]$.

```matlab
selected = L.applyFullBoxPreorder();
selected = L.applyFullBoxPreorder(r);
```

## SparseFullBox Band-Limited Support

SparseFullBox retains every FullBox parity or generator-mask term and every
exact target-coefficient identity, but replaces each dense Gram matrix $Q_J$
by the symmetric block-band Gram matrix $Q_J^{(w)}$ of bandwidth $w$. For
selected absolute order $r$, use the paper notation
$\mathbf a_J=r\boldsymbol 1-\boldsymbol\chi_J$:

$$
S^{(\mathbf c)}(\boldsymbol\alpha)
=\sum_{J\subseteq\{1,\ldots,\ell\}}
\left(\prod_{s\in J}g_s(\boldsymbol\alpha)\right)
\left(\mathbf b_{\mathbf a_J}(\boldsymbol\alpha)\otimes I_n\right)^{\mathsf T}
Q_J^{(w)}
\left(\mathbf b_{\mathbf a_J}(\boldsymbol\alpha)\otimes I_n\right),
\qquad Q_J^{(w)}\succeq0.
$$

Terms with a negative component in $\mathbf a_J$ are omitted. The
one-parameter parity-specific formulas use the same replacement
$Q_J\mapsto Q_J^{(w)}$. Exact coefficient matching is unchanged.

```matlab
selected = L.applySparseFullBoxPreorder();
selected = L.applySparseFullBoxPreorder(w);
selected = L.applySparseFullBoxPreorder(w, r);
```

## Canonical Endpoints

- $w=1$ canonicalizes to actual Direct state after validating $r$.
- $1<w<\max_s(r_s+1)$ stores actual SparseFullBox state.
- $w\ge\max_s(r_s+1)$ canonicalizes to actual FullBox state.

These are state transitions, not merely descriptive similarities. Inspect the
read-only `pdlmi` properties when code must report the selected state.

## One-Parameter Boundary

In one parameter, the parity-specific Markov–Lukács terms are used. For even
$M=4$ and $r=2$, $Q_J^{(1)}$ is block diagonal and canonicalizes to Direct,
$Q_J^{(2)}$ is block tridiagonal, and $Q_J^{(3)}$ is block pentadiagonal.
Because $w=3=r+1$, that endpoint canonicalizes to FullBox.

## Multivariate Boundary

For $\ell\ge2$, the minimum absolute order is componentwise
$\mathbf r=\lceil\mathbf M/2\rceil$. FullBox can grow rapidly because every generator
subset and dense tensor basis block contributes. SparseFullBox restricts each
$Q_J$ to the symmetric block-band form $Q_J^{(w)}$ while retaining exact
coefficient matching.

## Selection Guidance

Use Direct as the baseline. Use SparseFullBox when a Gram certificate is
desired but dense FullBox blocks are too large, and vary $w$ independently
of $r$. Use FullBox when the dense preordering is computationally acceptable.
Always record $\mathcal G_s$, $\mathbf m$, $\mathbf M$, $\mathbf r$, $w$, solver, and solver
status.

## Failure Interpretation

A fixed $r$ or $w$ failure is inconclusive. A solver abort, memory limit,
or nonzero `diagnostics.problem` is not objective data and does not establish
continuous infeasibility.

## Limitations

SparseFullBox uses symmetric block-band Gram matrices of a user-selected
bandwidth $w$; it does not search arbitrary chordal patterns or adapt $w$
automatically. FullBox is limited to box
generators and can become large with dimension and order. Neither selector
calls a solver or inserts a positivity margin.

## Further Reading

- Schmüdgen, [The K-moment problem for compact semi-algebraic sets](https://doi.org/10.1007/BF01234651), for preorderings.
- Harris and Parrilo, [Improved nonnegativity testing in the Bernstein basis via geometric means](https://arxiv.org/abs/2309.10675), for a related univariate sparse Bernstein-Gram precedent.
- Zheng and Fantuzzi, [Sum-of-squares chordal decomposition of polynomial matrix inequalities](https://arxiv.org/abs/2007.11410), for broader sparse polynomial-matrix Gram context.

These sparse-Gram sources motivate structure; they do not define the package's
block-band Gram matrices.

## See Also

[Previous: Markov–Lukács And Putinar](/PD-LMI-package/documents/math/finite-certificates/markov-lukacs-and-putinar/) ·
[Next: `pdlmi` Overview](/PD-LMI-package/documents/reference/pdlmi/) ·
[Notation](/PD-LMI-package/documents/math/notation/#gram-basis-and-certificate-parameters) ·
[`applySparseFullBoxPreorder`](/PD-LMI-package/documents/reference/pdlmi/applysparsefullboxpreorder/) ·
[`applyFullBoxPreorder`](/PD-LMI-package/documents/reference/pdlmi/applyfullboxpreorder/)
