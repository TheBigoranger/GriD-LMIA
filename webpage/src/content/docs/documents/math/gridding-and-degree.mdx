---
title: Gridding And Bernstein Degree
description: Read parameter-grid inputs, global coefficient grids, cell-local storage, and the common Bernstein degree without conflating them.
---

import CellStorageDiagram from "../../../../components/CellStorageDiagram.astro";

<nav class="manual-trail">
  <a href="/PD-LMI-package/documents/">Documents</a>
  <span>/</span>
  <a href="/PD-LMI-package/documents/math/bernstein-polynomial/">Bernstein Polynomial</a>
  <span>/</span>
  <span>Gridding And Bernstein Degree</span>
</nav>

## The Two Independent Choices

Every coefficient-backed object has two related but different discretizations:

- the **physical parameter grid** partitions the scheduling domain into cells;
- the scalar **Bernstein `Degree`** chooses how many local coefficient matrices
  represent the polynomial in every cell.

Throughout the manual, $\rho_s^{(j)}$ denotes node $j$ on axis $s$, so
$\mathcal G_s=\{\rho_s^{(1)},\ldots,\rho_s^{(k_s)}\}$. The full grid is

$$
\mathcal G=\mathcal G_1\times\cdots\times\mathcal G_\ell,
\qquad
N_{\mathrm{node}}=\prod_{s=1}^{\ell}k_s,
\qquad
N_{\mathrm{cell}}=\prod_{s=1}^{\ell}(k_s-1).
$$

A physical hypercube is indexed by the one-based vector
$\mathbf c=(c_1,\ldots,c_\ell)$. Its local Bernstein coefficients use
zero-based tensor labels: $C^{(\mathbf c)}[\mathbf i]$. Only the
one-dimensional case shortens $\mathbf c$ to the scalar $c$.

Adding a grid node makes more physical cells. Raising `Degree` makes more
Bernstein labels inside each existing cell. Neither action is a shorthand for
the other. Degree 1 is the simplest affine case, not the package target:
finite nonnegative degrees represent continuous piecewise-polynomial data on
the same physical partition.

<CellStorageDiagram />

## One-Dimensional Input

For one scheduling parameter, `pdmat` and `pdvar` accept a numeric vector as
shorthand for a one-element cell array. The vector must be finite, strictly
increasing, and have at least two entries.

```matlab
rhoGrid = [0 1 2];
data = {10, 11, 12, 13, 14};
A = pdmat(rhoGrid, data, Degree=2);

A.ncell()
A.ncoeff()
A.coeffs(1)
A.coeffs(2)
```

```text
ans =
     2

ans =
     3

ans =
  1x3 cell array
    {[10]}    {[11]}    {[12]}

ans =
  1x3 cell array
    {[12]}    {[13]}    {[14]}
```

Here `rhoGrid` has three nodes, so it creates two physical intervals. Degree
2 gives the three local labels `0`, `1`, and `2`, hence three coefficients per
interval. The global input has five entries because its required length is

$$
(k_1-1)m+1=(3-1)2+1=5.
$$

The shared value `12` is the common boundary coefficient of the two global
degree-two cells. It is a Bernstein coefficient, not an extra physical grid
node and not necessarily a sampled function value. Adjacent `pdvar` cells
share this complete face value, but their cell-local derivatives need not
match. This is value continuity, not a B-spline or a global differentiability
claim.

## Two-Dimensional Input

For two scheduling parameters, pass one node vector per axis. A global
coefficient source is a MATLAB cell array whose dimensions follow the two
axes:

```matlab
grid = {[0 1 2 3], [10 20 30]};
data = {11, 12, 13; 21, 22, 23; 31, 32, 33; 41, 42, 43};
A = pdmat(grid, data, Degree=1);

A.ncell()
A.ncoeff()
A.coeffs([1 1])
A.coeffs([3 2])
A.LocalValues{2}{1}
```

```text
ans =
     6

ans =
     4

ans =
  1x4 cell array
    {[11]}    {[12]}    {[21]}    {[22]}

ans =
  1x4 cell array
    {[32]}    {[33]}    {[42]}    {[43]}

ans =
  1x4 cell array
    {[21]}    {[22]}    {[31]}    {[32]}
```

There are $3\times2=6$ physical rectangles. The axes deliberately use
different node counts. The common degree is 1 along
both axes, so each rectangle has $(1+1)^2=4$ local tensor coefficients. Their
flat package order is the row order returned by `A.lbls()`:

```text
     0     0
     0     1
     1     0
     1     1
```

The same order explains `A.coeffs([1 1])`. The first grid dimension varies
more slowly in that local-label order. Physical cells, in contrast, are stored
as a nested tree: `LocalValues{c1}{c2}`. Thus `A.LocalValues{2}{1}` selects
the second cell along the first axis and the first cell along the second.

For this degree-one example, the global coefficient-grid dimensions are

$$
(k_1-1)m+1=(4-1)1+1=4,
\qquad
(k_2-1)m+1=(3-1)1+1=3,
$$

which is why `data` is a 4-by-3 cell array. For a global coefficient-backed
`pdmat`, every axis must satisfy this shape rule.

## The ell-Dimensional Case

The same convention extends directly to $\ell$ scheduling parameters:

```matlab
gridVectors = {rho1Nodes, rho2Nodes, ..., rhoEllNodes};
```

If axis $s$ has $k_s$ nodes and the common scalar degree is `m`, the package
has

$$
N_{\mathrm{cell}}=\prod_{s=1}^{\ell}(k_s-1),
\qquad
N_{\mathrm{coeff/cell}}=(m+1)^\ell.
$$

The physical-cell tree is `LocalValues{c1}{c2}...{c_ell}`. Its leaf is a flat
cell array whose label rows come from `lbls()`; labels range independently
from `0` through `m` in each coordinate. A global coefficient-backed `pdmat`
uses one source-array dimension per parameter, of length
`(k_s - 1) * m + 1` on axis $s$.

`Degree` is a single scalar property in the implemented package, not a
per-axis degree vector. `pdmat` accepts any nonnegative integer degree;
constructor-created `pdvar` accepts every finite nonnegative integer degree. Products may
create higher-degree expressions through coefficient algebra.

## Do Not Conflate These Operations

<table class="mobile-stack-table">
  <caption>Effects of grid, representation-degree, and decision-degree changes</caption>
  <thead>
    <tr>
      <th scope="col">Operation</th>
      <th scope="col">Changes physical cells?</th>
      <th scope="col">Changes coefficients per cell?</th>
      <th scope="col">Current package boundary</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row" data-label="Operation">Add grid nodes</th>
      <td data-label="Physical cells">Yes</td>
      <td data-label="Coefficients per cell">No, for a fixed degree</td>
      <td data-label="Package boundary">Common-grid algebra is supported; there is no general public adaptive-subdivision command.</td>
    </tr>
    <tr>
      <th scope="row" data-label="Operation">Raise or align Bernstein degree</th>
      <td data-label="Physical cells">No</td>
      <td data-label="Coefficients per cell">Yes</td>
      <td data-label="Package boundary">Exact degree elevation aligns compatible coefficient-backed operands.</td>
    </tr>
    <tr>
      <th scope="row" data-label="Operation">Choose a higher <code>pdvar</code> decision degree</th>
      <td data-label="Physical cells">No</td>
      <td data-label="Coefficients per cell">Yes</td>
      <td data-label="Package boundary">Constructor-created <code>pdvar</code> accepts every finite nonnegative integer degree.</td>
    </tr>
  </tbody>
</table>

The [Bernstein Polynomial](/PD-LMI-package/documents/math/bernstein-polynomial/)
chapter gives the basis, local coordinate, degree-elevation formula, and
coefficient-product rule. The [`pdmat` constructor](/PD-LMI-package/documents/reference/pdmat/constructor/)
reference lists the accepted source forms, and the [`pdvar` constructor](/PD-LMI-package/documents/reference/pdvar/constructor/)
lists its decision-variable forms.
