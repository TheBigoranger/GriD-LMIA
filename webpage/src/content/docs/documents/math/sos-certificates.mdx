---
title: Certificate Map And Selection Guide
description: Choose among Direct, Pólya, Putinar, SparsePutinar, SparseFullBox, and FullBox without confusing finite certificates with exact rate reduction.
---

import CertificateChooser from "../../../../components/CertificateChooser.astro";
import CertificateFlow from "../../../../components/CertificateFlow.astro";
import ConstraintAssemblyDiagram from "../../../../components/ConstraintAssemblyDiagram.astro";

<nav class="manual-trail">
  <a href="/GriD-LMIA/documents/">Documents</a>
  <span>/</span>
  <span>Finite Certificates</span>
  <span>/</span>
  <span>Certificate Map And Selection Guide</span>
</nav>

`pdlmi` exposes exactly six certificate states: **Direct**, **Pólya**,
**Putinar**, **SparsePutinar**, **SparseFullBox**, and **FullBox**. One-parameter
Markov–Lukács is the interval form used by Putinar and FullBox, not a sixth
software state.

## Certificate Map

<CertificateFlow />

All six paths start from the same cell-local original residual
$\mathcal F^{(\mathbf c)}$. The comparison relation determines
$S^{(\mathbf c)}=-\mathcal F^{(\mathbf c)}$ and
$C^{(\mathbf c)}=-\mathcal C^{(\mathbf c)}$.
Alternative selectors rebuild from $\mathcal F$; they do not compound.

<CertificateChooser />

Exact rate-box vertex reduction occurs before this map. Each certificate is a
finite sufficient condition over the remaining parameter cell. These are
different logical steps.

## Explore The Stored Constraint Shape

<ConstraintAssemblyDiagram />

`toYalmip` returns the already assembled finite YALMIP constraints. It neither
selects a solver nor calls `optimize`.

## A Tiny Example: Positive Polynomial, Failed Direct Test

A polynomial may be positive on $[0,1]$ even when one Bernstein coefficient
is negative. Failure of Direct therefore does not prove negativity. Pólya or a
Gram certificate may certify the same target without changing the underlying
continuous inequality.

## Full-Space SOS

Full-space SOS is useful background but is not a selectable `pdlmi` state.
The implemented Gram paths use interval or box generators and exact Bernstein
coefficient matching on $[0,1]^\ell$.

## Direct Bernstein Coefficient Certificate

Direct requires

$$
C^{(\mathbf c)}[\mathbf i]\succeq0
$$

for every physical cell, stored rate row, and local label. It is the default,
has no Gram variables, and inserts no hidden positivity margin.

## <span id="polya-type-coefficient-relaxation"></span>Pólya-Type Coefficient Relaxation

Pólya applies exact degree elevation by direction-wise increment $\mathbf d$,
then runs the Direct
test on the elevated coefficients. Read
[Direct And Pólya](/GriD-LMIA/documents/math/finite-certificates/direct-and-polya/)
for the formula and selection boundary.

## Putinar Quadratic Modules

For one parameter, the minimum absolute order is
$r=\lfloor M/2\rfloor$. For two or more parameters
$(\ell\ge2)$, Putinar uses an unweighted Gram/SOS term plus the singleton
generators $g_s=\alpha_s(1-\alpha_s)$, with componentwise minimum
$\mathbf r=\lceil\mathbf M/2\rceil$.

### Implemented Putinar selector

```matlab
selected = L.usePutinar();   % default absolute order
selected = L.usePutinar(r);  % explicit absolute order
```

In one parameter this selector uses the exact parity-specific
Markov–Lukács interval form. See
[Markov–Lukács And Putinar](/GriD-LMIA/documents/math/finite-certificates/markov-lukacs-and-putinar/).

## SparsePutinar Tensor Windows

SparsePutinar keeps the Putinar parity or singleton-generator terms, but it
splits each dense Bernstein Gram basis into overlapping tensor windows. For a
Gram degree vector $\mathbf d$ and `CliqueSize` value $b$, the side length is
$w_s=\min(b,d_s+1)$. Each window has an independent PSD block, while every
target coefficient identity accumulates all overlapping contributions.

```matlab
selected = L.useSpPut();
selected = L.useSpPut(b);
selected = L.useSpPut(b, r);
```

The [SparsePutinar mathematics page](/GriD-LMIA/documents/math/finite-certificates/sparseputinar/)
defines the window labels, dimensions, counts, endpoints, and its distinction
from structural-matrix graph-chordal decomposition.

## Schmüdgen And Full Preorderings

The full-box preordering uses every square-free product
$g_J=\prod_{s\in J}\alpha_s(1-\alpha_s)$. The software state is
`FullBox`.

<span id="sparse-full-box-tensor-windows"></span>

## SparseFullBox Tensor-Window Certificate

The software state is `SparseFullBox`. Each dense tensor Bernstein basis is replaced by overlapping sliding windows with side length $w$, and each window receives an independent PSD Gram block. Side length one canonicalizes to Direct after order validation. A side that spans every active basis axis canonicalizes to FullBox.

### Worked full-box example: two parameters, degree three

<span id="worked-full-box-example-two-parameters-degree-three"></span>

For a two-parameter target with assembled degree $M=3$, FullBox introduces
Gram terms for the empty subset, both singleton generator subsets, and their
two-axis product. Each term is mapped into the Bernstein basis and the sum is
matched exactly to every $C^{(\mathbf c)}[\mathbf i]$. SparseFullBox retains
those term families and exact identities but replaces each dense basis by
overlapping tensor-window blocks of side length $w$.
The detailed basis-order boundary is documented in
[SparseFullBox And FullBox](/GriD-LMIA/documents/math/finite-certificates/sparsefullbox-and-fullbox/).

## One-Dimensional Markov–Lukács Forms

Even and odd assembled degrees use different endpoint weights. The package
implements those parity forms through Putinar, SparsePutinar, SparseFullBox, and FullBox
selectors in one parameter.

## Five Operations That Are Not Interchangeable

- Refine $\mathcal G_s$ to change physical cells.
- Raise decision degree $m$ to enlarge the decision family.
- Raise Pólya increment $d$ to elevate the representation.
- Raise absolute Gram order $r$ to change a Gram certificate.
- Change SparsePutinar `CliqueSize` value $b$ or SparseFullBox window side $w$ to
  change the sparse Gram support.

Assembled residual degree $M$ is determined after algebra and alignment; it
is not another user-selected certificate knob.

## Exact Software Boundary

The package implements six finite states only. It does not implement a
general-domain Putinar parser, an adaptive sparse-pattern search, or a
solver-owned hierarchy loop. A failed fixed $d$, $r$, or $w$, a nonzero
`diagnostics.problem`, or an aborted solve is inconclusive for the original
continuous feasibility question.

## Limitations

Every certificate is sufficient over a tensor-product cell. Gram blocks can
grow quickly with dimension and order. Equality assembly is direct-only, and
no certificate adds a strict margin automatically.
The mathematical target is $S^{(\mathbf c)}\succ0$; encode strictness
explicitly as $S^{(\mathbf c)}-\varepsilon I\succeq0$ before exporting the
non-strict software constraints.

## Further Reading

- Putinar, [Positive polynomials on compact semi-algebraic sets](https://www.jstor.org/stable/24897130).
- Schmüdgen, [The K-moment problem for compact semi-algebraic sets](https://doi.org/10.1007/BF01234651).
- Scherer and Hol, [Matrix sum-of-squares relaxations for robust semi-definite programs](https://doi.org/10.1007/s10107-005-0684-2).
- Zheng and Fantuzzi, [Sum-of-squares chordal decomposition of polynomial matrix inequalities](https://arxiv.org/abs/2007.11410), for structural-matrix chordal SOS and its distinct graph-clique setting.

## See Also

[Previous: Coefficient Algebra](/GriD-LMIA/documents/math/coordinates-and-bernstein/coefficient-algebra/) ·
[Next: Direct And Pólya](/GriD-LMIA/documents/math/finite-certificates/direct-and-polya/) ·
[Notation](/GriD-LMIA/documents/math/notation/#gram-basis-and-certificate-parameters) ·
[`pdlmi` overview](/GriD-LMIA/documents/reference/pdlmi/) ·
[`useSpPut`](/GriD-LMIA/documents/reference/pdlmi/usespput/) ·
[`toYalmip`](/GriD-LMIA/documents/reference/pdlmi/toyalmip/)
