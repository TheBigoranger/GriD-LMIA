---
title: pdvar value
description: Convert assigned symbolic coefficients to known pdmat data.
---

import DiagramFrame from "../../../../../components/DiagramFrame.astro";

<nav class="manual-trail">
  <a href="/GriD-LMIA/documents/">Documents</a>
  <span>/</span>
  <a href="/GriD-LMIA/documents/reference/pdvar/">pdvar</a>
  <span>/</span>
  <span>value</span>
</nav>

<DiagramFrame caption="value preserves the grid, degree, cell labels, and matrix shape while replacing every assigned symbolic coefficient with its numeric value." label="Symbolic pdvar coefficients become numeric pdmat coefficients">
  <div class="value-evidence" role="img" aria-label="Symbolic coefficients X zero X one X two become numeric coefficients 1 1.5 3">
    <span>X₀</span><span>X₁</span><span>X₂</span><b aria-hidden="true">→</b><strong>1</strong><strong>1.5</strong><strong>3</strong>
  </div>
</DiagramFrame>

## Purpose

Convert a `pdvar` expression whose symbolic coefficients have finite assigned
YALMIP values into known coefficient-backed `pdmat` data.

## Syntax

```matlab
A = value(P)
rows = value(rhodiff(P))
```

## Output

For an ordinary expression, `A` is one `pdmat` that preserves the parameter
grid, matrix size, Bernstein degree, local coefficient order, and continuity
metadata. For a derivative expression with rate rows, `rows` is a
`1 × P.NumRateRows` cell array of `pdmat` objects, in distinct lower/upper
Cartesian vertex order after fixed axes collapse. Returned `pdmat` objects do
not retain rate metadata.

## Example

```matlab
yalmip('clear')
P = pdvar(1, [0 1], Degree=2);
c = P.coeffs(1);
assign([c{:}], [1 2 3]);
A = value(P);
A.coeffs(1)
```

```text
ans =
  1x3 cell array
    {[1]}    {[2]}    {[3]}
```

## Validation And Errors

Every symbolic coefficient must have an assigned finite real numeric value.
Unassigned, nonnumeric, nonreal, nonfinite, or unavailable values raise
`pdvar:UnassignedValue`.

`value` evaluates stored coefficients, not an arbitrary parameter point. Use
[`pdmat evaluate`](/GriD-LMIA/documents/reference/pdmat/evaluate/) after
conversion when an exact point evaluation is needed.

## See Also

[`rhodiff`](/GriD-LMIA/documents/reference/pdvar/rhodiff/) · [`pdmat`](/GriD-LMIA/documents/reference/pdmat/) · [`bernTable`](/GriD-LMIA/documents/reference/pdvar/berntable/)
