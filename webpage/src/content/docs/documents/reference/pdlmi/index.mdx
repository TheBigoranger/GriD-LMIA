---
title: pdlmi
description: Cell-local YALMIP constraints for PD-LMI expressions.
---

import CertificateChooser from "../../../../../components/CertificateChooser.astro";
import CertificateNavigation from "../../../../../components/CertificateNavigation.astro";

<nav class="manual-trail">
  <a href="/PD-LMI-package/documents/">Documents</a>
  <span>/</span>
  <span>pdlmi</span>
</nav>

`pdlmi` stores finite YALMIP constraints generated from `pdvar` residual
expressions. Inequalities use semidefinite meaning only when every original
coefficient is square Hermitian; otherwise the whole inequality is entry-wise.
Equality is direct coefficient equality and may be rectangular. Direct coefficient assembly is the default; Pólya
degree elevation, fixed-order Putinar box assembly, and fixed-order full box
Bernstein-Gram preordering are explicit alternatives.

<CertificateNavigation />

<CertificateChooser />

The four paths begin with the same stored residual. **Direct** tests every
local coefficient and is the default. **Pólya** exactly elevates that residual
before repeating the direct test. In one parameter, **Putinar** and **Full
box** dispatch to the same parity-specific Markov–Lukács form with minimum
order `floor(m/2)`. In two or more parameters, Putinar uses an unweighted Gram
term plus one generator-weighted term per axis at minimum `ceil(m/2)`, while
Full box includes every generator-subset product. Both Gram paths match exact
box-polynomial identities. The
[SOS certificate chapter](/PD-LMI-package/documents/math/sos-certificates/)
derives these ideas; the API pages below stay focused on callable syntax.

## Reference Index

### Construction and comparisons

| Reference | Task |
| :--- | :--- |
| [`pdlmi constructor`](/PD-LMI-package/documents/reference/pdlmi/constructor/) | Select direct, Pólya-elevated, fixed-order Putinar, or fixed-order full-box assembly. |
| [`pdvar comparisons`](/PD-LMI-package/documents/reference/pdvar/comparisons/) | Create semidefinite or entry-wise inequalities with `<=`/`>=`, and direct equalities with `==`. |
| [`applyPolya`](/PD-LMI-package/documents/reference/pdlmi/applypolya/) | Rebuild the original residual with a selected Pólya degree increment. |
| [`applyPutinar`](/PD-LMI-package/documents/reference/pdlmi/applyputinar/) | Rebuild the original residual with the minimum or a selected absolute Putinar order. |
| [`applyFullBoxPreorder`](/PD-LMI-package/documents/reference/pdlmi/applyfullboxpreorder/) | Rebuild the original residual with the minimum or a selected absolute full-box order. |

### YALMIP export and solver use

| Reference | Task |
| :--- | :--- |
| [`toYalmip`](/PD-LMI-package/documents/reference/pdlmi/toyalmip/) | Concatenate stored constraints for ordinary YALMIP `optimize` calls. |

## Implemented Behavior

Version 0.4.2 assembles `<=`, `>=`, and `==` relations. Inequalities select
one global semidefinite or entry-wise interpretation from all original
coefficients. Equality is always direct and supports rectangular or
non-Hermitian residuals. Direct
coefficient-wise assembly is the default. Pólya assembly performs exact degree
elevation before applying the coefficient test. Putinar and full-box assembly
create independent positive-semidefinite Gram blocks for every physical cell
and active rate row, together with exact Bernstein coefficient identities.

In one parameter, Putinar and full box share the parity-specific
Markov–Lukács interval construction. For two or more parameters, Putinar uses
an unweighted Gram term plus one term for each axis generator
$\alpha_s(1-\alpha_s)$, while full box uses every subset product of those
generators. Both select a fixed absolute order on each local hypercube.
`toYalmip` concatenates the resulting finite constraints for an ordinary
YALMIP solver call.

## Properties

The constructor exposes private-set, read-only properties through dot access:

| Property | Example | Meaning |
| :--- | :--- | :--- |
| `Constraints` | `C.Constraints` | Stored direct conditions, Gram-block conditions, and/or exact identities. May be empty for a proven equality. |
| `Residual` | `C.Residual` | Promoted `lhs-rhs` expression used to rebuild alternative inequality certificates. |
| `Relation` | `C.Relation` | `"<="`, `">="`, or `"=="`. |
| `UsePolya`, `PolyaDegree` | `C.UsePolya`, `C.PolyaDegree` | Pólya selector and degree increment. |
| `UsePutinar`, `PutinarOrder` | `C.UsePutinar`, `C.PutinarOrder` | Putinar selector and absolute order. |
| `UseFullBoxPreorder`, `FullBoxOrder` | `C.UseFullBoxPreorder`, `C.FullBoxOrder` | Full-box selector and absolute order. |

Certificate selectors apply only to inequalities. Any selector on equality raises `pdlmi:UnsupportedEqualityCertificate`.

The [SOS certificate map](/PD-LMI-package/documents/math/sos-certificates/)
derives these coefficient and Gram constructions, including the
one-dimensional Markov–Lukács form and the tensor box generators used in
higher dimensions.

## See Also

[`pdlmi constructor`](/PD-LMI-package/documents/reference/pdlmi/constructor/) · [`applyPolya`](/PD-LMI-package/documents/reference/pdlmi/applypolya/) · [`applyPutinar`](/PD-LMI-package/documents/reference/pdlmi/applyputinar/) · [`applyFullBoxPreorder`](/PD-LMI-package/documents/reference/pdlmi/applyfullboxpreorder/) · [`SOS certificates`](/PD-LMI-package/documents/math/sos-certificates/) · [`pdvar comparisons`](/PD-LMI-package/documents/reference/pdvar/comparisons/) · [`toYalmip`](/PD-LMI-package/documents/reference/pdlmi/toyalmip/)
