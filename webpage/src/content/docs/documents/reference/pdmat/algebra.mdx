---
title: pdmat Algebra
description: Detailed coefficient-algebra syntax, formulas, examples, outputs, and errors for pdmat.
---

import MatrixGlyphDiagram from "../../../../../components/MatrixGlyphDiagram.astro";
import MultiplicationDiagram from "../../../../../components/MultiplicationDiagram.astro";
import ExampleDisclosure from "../../../../../components/ExampleDisclosure.astro";
import AdditionExplorer from "../../../../../components/AdditionExplorer.tsx";
import MultiplicationExplorer from "../../../../../components/MultiplicationExplorer.tsx";

<nav class="manual-trail"><a href="/PD-LMI-package/documents/">Documents</a><span>/</span><a href="/PD-LMI-package/documents/reference/pdmat/">pdmat</a><span>/</span><a href="/PD-LMI-package/documents/reference/pdmat/matrix-operations/">matrix operations</a><span>/</span><span>algebra</span></nav>

Every operation acts on the matrix payload stored at each local Bernstein label. The returned `pdmat` preserves known-data semantics and exposes the aligned grid, degree, and payload shape through its properties.

## <span id="pdmat-plus"></span><span id="pdmat-minus"></span><span id="pdmat-uplus"></span><span id="pdmat-uminus"></span>Add, subtract, and align coefficients

```matlab
S = A + B; D = A - B; U = +A; N = -A;
```

`plus` and `minus` align compatible physical grids and exactly elevate the lower Bernstein degree before combining corresponding coefficients. Numeric constants are promoted to the payload size. `uplus` preserves the object; `uminus` negates every stored coefficient.

```matlab
A = pdmat({[0 1]}, {1, 2}, Degree=1);
B = pdmat({[0 1]}, {10, 20, 30}, Degree=2);
S = A + B; cS = S.coeffs(1);
Z = A - A; N = -A; U = +A;
S.Degree
disp([cS{:}])
Z.evaluate(0.5)
N.evaluate(1)
U.evaluate(0)
```

```text
ans = 2
    11.0000   21.5000   32.0000
ans = 0
ans = -2
ans = 1
```

For example, elevating a degree-one row to degree two gives

$$
[a_0,a_1]
\longmapsto
\left[a_0,\frac{a_0+a_1}{2},a_1\right].
$$

<MatrixGlyphDiagram before={[["a₀", "½(a₀+a₁)", "a₁"], ["b₀", "b₁", "b₂"]]} after={[["a₀+b₀", "½(a₀+a₁)+b₁", "a₁+b₂"]]} beforeLabel="degree-elevated rows" afterLabel="coefficient sum" caption="Degree elevation changes the representation, not the polynomial; addition then combines aligned local labels." />

If compatible operands have different interior nodes, their sorted union is the common refinement. Grid refinement and degree elevation are independent alignment steps.

### <span id="numerical-addition-explorer"></span>Numerical addition explorer

Edit either one-cell coefficient row. The explorer elevates both rows exactly
to their common degree, then displays and plots the aligned operands and sum.

<AdditionExplorer client:visible />

## <span id="pdmat-mtimes"></span>Multiply in Bernstein coefficient space

```matlab
K = A * B;
```

`mtimes` multiplies coefficient-backed functions exactly rather than at sample points. On one cell,

$$
R^{(c)}[\mathbf k]=
\sum_{\mathbf i+\mathbf j=\mathbf k}
\prod_{s=1}^{\ell}
\frac{\binom m{i_s}\binom n{j_s}}{\binom{m+n}{k_s}}
P^{(c)}[\mathbf i]Q^{(c)}[\mathbf j].
$$

Matrix order is preserved. Numeric scalars and matrices use the corresponding MATLAB-compatible left or right product. Degrees and tensor labels add componentwise.

The implementation visits contribution pairs in increasing left label, with
`j = k-i`, and keeps matrix multiplication in that order. A scalar
degree-one check makes the normalized middle coefficient explicit:

```matlab
A = pdmat({[0 1]}, {4, 2}, Degree=1);
B = pdmat({[0 1]}, {0.5, 3}, Degree=1);
K = A * B;
cK = K.coeffs(1);
[cK{:}]
```

```text
ans =
    2.0000    6.5000    6.0000
```

Here `6.5 = 0.5*(4*3) + 0.5*(2*0.5)`; this is an ordered coefficient
convolution, not a pointwise product of sampled rows.

<ExampleDisclosure
  visibleCode={"K = A * B;\ncK = K.coeffs([1 1]);"}
  fullCode={"grid = {[0 1], [10 20]};\nA = pdmat(grid, {1 2; 3 4}, Degree=1);\nB = pdmat(grid, {5 6; 7 8}, Degree=1);\nK = A * B;\ncK = K.coeffs([1 1]);\nassert(K.Degree == 2)\nassert(K.ncoeff() == 9)\ndisp([cK{:}])"}
  summary="Show the complete runnable two-parameter example"
/>

<MultiplicationDiagram />

### <span id="numerical-multiplication-explorer"></span>Numerical multiplication explorer

The weighted convolution below uses the same normalized Bernstein rule as
`pdmat.mtimes` and preserves the written factor order.

<MultiplicationExplorer client:visible />

## Validation and operation boundaries

- Function-only objects without Bernstein evidence are rejected by coefficient algebra.
- Physical bounds and matrix dimensions must be compatible before refinement or multiplication.
- `mtimes` is matrix multiplication; element-wise parameter-dependent products are not a public substitute.
- Ordinary one-row operands broadcast across explicit rate rows. Two
  rate-row operands must have exactly matching grids and nonempty matching
  rate boxes; they are not automatically refined.
- Multiplication preserves rate rows when only one side has them. Rate-row by
  rate-row products, rate-row by ordinary decision products, and
  metadata-only rate dependence in a product are rejected.
- These methods return known `pdmat` data and do not introduce YALMIP decisions.

## See Also

[`Mathematical notation`](/PD-LMI-package/documents/math/notation/) · [`structural operations`](/PD-LMI-package/documents/reference/pdmat/structural-operations/) · [`indexing and inspection`](/PD-LMI-package/documents/reference/pdmat/indexing-and-inspection/) · [`Bernstein utilities`](/PD-LMI-package/documents/reference/bernstein-utilities/)
