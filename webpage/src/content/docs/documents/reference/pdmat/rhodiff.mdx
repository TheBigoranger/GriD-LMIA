---
title: pdmat rhodiff
description: Differentiate coefficient-backed known data into deterministic rate-vertex rows.
---

import CallGraph from "../../../../../components/CallGraph.astro";

<nav class="manual-trail">
  <a href="/GriD-LMIA/documents/">Documents</a>
  <span>/</span>
  <a href="/GriD-LMIA/documents/reference/pdmat/">pdmat</a>
  <span>/</span>
  <span>rhodiff</span>
</nav>

## Purpose

Differentiate coefficient-backed `pdmat` data cell by cell and store the
rate-weighted derivative at every vertex of a scheduling-rate box.

<CallGraph family="differentiation" caption="rhodiff differentiates cell rows, re-elevates active partials, and assembles rate-vertex outputs." links={[
  { href: "/GriD-LMIA/documents/math/coordinates-and-bernstein/coefficient-algebra/#differentiation-and-rate-rows", label: "Differentiation mathematics" },
  { href: "/GriD-LMIA/documents/math/modeling-and-analysis/rate-box-and-interface-analysis/", label: "Rate-box mathematics" },
  { href: "/GriD-LMIA/documents/reference/pdvar/rhodiff/", label: "pdvar rhodiff" },
]} />

## Syntax

```matlab
D = rhodiff(A)
D = rhodiff(A, rateBounds)
```

## Arguments And Returned Object

`A` must have Bernstein coefficient evidence. Omitting explicit bounds uses
`A.RateBounds`. Explicit `rateBounds` supplies missing metadata, but must match
stored bounds when both are present.

For one parameter, $\vect m=(m_1)$ with $m_1>0$ returns degree
$(m_1-1)$. Degree $(0)$ remains zero. For several parameters, the partial along axis `s` has degree
`A.Degree-e_s` when `A.Degree(s)>0`, then is elevated exactly back to the common
vector `A.Degree` before the rate-weighted sum is formed. A zero-degree axis
contributes the zero partial, so multivariate `rhodiff` preserves the complete
direction-wise degree row.
`D` is a `pdmat` with `IsContinuous=false` and one numeric coefficient row per
distinct rate-box vertex in `helper.combRows` order. `D.NumRateRows` is the
exact stored-row count. A fixed axis contributes one value in place of two endpoints.

## MATLAB Transcript

```matlab
A = pdmat([0 1], {0, 2}, Degree=1, RateBounds=[-1 2]);
D = rhodiff(A);
disp(bernTable(D, 1, "oneLine"))
```

```text
    CellSubscript    RateVertexIndex    RateVertex    Expression
    _____________    _______________    __________    __________

        {[1]}               1             {[-1]}         "-2"
        {[1]}               2             {[ 2]}         "4"
```

The two rows evaluate the constant physical derivative `2` at rates `-1` and
`2`.

## Validation And Limitations

- Function-only data raise `pdmat:FunctionOnlyDiff`.
- Missing, malformed, or inconsistent bounds raise
  `pdmat:MissingRateBounds`, `pdmat:InvalidRateBounds`, or
  `pdmat:RateBoundsMismatch`.
- Existing explicit rate rows, including a previous derivative, raise
  `pdmat:InvalidDiff`. Repeated differentiation is unsupported.
- The output is cell-wise and generally discontinuous at shared faces.

## See Also

[`pdmat constructor`](/GriD-LMIA/documents/reference/pdmat/constructor/) ·
[`bernTable`](/GriD-LMIA/documents/reference/pdmat/berntable/) ·
[`plot`](/GriD-LMIA/documents/reference/pdmat/plot/) ·
[`pdvar rhodiff`](/GriD-LMIA/documents/reference/pdvar/rhodiff/) ·
[`pdbase evaluation and elevation`](/GriD-LMIA/documents/reference/pdbase/evaluation-and-elevation/)
