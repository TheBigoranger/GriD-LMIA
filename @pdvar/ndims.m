function n = ndims(~)
    %NDIMS Number of matrix dimensions represented by pdvar.
    %
    %   Syntax:
    %     n = ndims(P)
    %
    %   Example:
    %     P = pdvar(2, {[0 1]});
    %     n = ndims(P);

    n = 2;
end
